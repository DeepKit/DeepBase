# WO-20260902-002 交付报告 · SchemaAdapter 指纹口径改造

| 项 | 内容 |
|---|---|
| 工单 | `docs/WO-20260902-002-开发甲-SchemaAdapter指纹口径改造工单.md` |
| Brief | `docs/brief-WO-20260902-002-开发甲.md` |
| 前置 | WO-20260902-001（hex / virtual Create / Registry 跳过逻辑复用） |
| 交付日 | 2026-09-02 |
| Commit | `571b900` · WO-20260902-002（未 push） |

## 逐项修复

| FIX | 要点 | 证据 |
|-----|------|------|
| FIX-A | `DeepBase.External.Types`：`AppendColumnSignatureEntries` / `FormatColumnSignature` / `HashColumnSignatureFingerprint`（SSOT）；`SQLiteReader.GetMessageColumnSignatureFingerprint` 枚举 `Msg_%`、一致性校验、空/不一致返回空串 | BUG332 `Test_Fixture_MsgColumnSignature_MatchesFrozenSHA256` |
| FIX-B | `ResolveAdapterOrRaise` 仅传列签名；`OpenReadOnly` / schema 变更路径调用；空指纹跳过 Registry；异常消息带 full+column 指纹；`GetSchemaFingerprint` 全量语义保留 | 源码 grep：`TryResolveAdapter(ColFp)` only |
| FIX-C | WeChat4x `FSchemaFingerprintPrefixes := ['26d53fe31f']`；删除 BLOCKED-DATA-P0-001 | BUG332 4x prefix / Registry hit |
| FIX-D | WeChat39x 保留 `e4a7b3c9f1`，标注 **BLOCKED-39X-DATA**；台账已登记 | 注释 + `bugfix.md` / `tasks.md` |
| FIX-E | `Tests/Regression/Fixtures/WeChat4x/`（Msg_sample + 静态表 + meta/README，wxid 脱敏）；BUG332 真实 SHA256→Registry→4x 链路 | XML 见下 |

## 权威值（复算一致）

- Msg 列签名 SHA256：`26d53fe31f389e65779419dc30bfdd73df5f1c299215fa4a0dccd525910cda84`
- 注册前缀：`26d53fe31f`

## 证据索引

| 证据 | 路径 |
|------|------|
| BUG332 直接跑 XML（10/10） | `TestResults/WO-20260902-002/bug332_unit.xml` |
| BUG332 官方 runner | `TestResults/WO-20260902-002/TBUG332_WeChatSchemaRegistryResolveTest.xml` / `UnitTestResults.xml` |
| 全量单测 | `TestResults/WO-20260902-002/full/UnitTestResults.xml` |

BUG332 过滤跑：Found 10 / Passed 10 / Failed 0（含 Fixture SHA256、Registry→4x、Name2Id 不误匹配、空指纹不命中）。

**全量（2026-09-02）：** Found 4396 / Passed 4389 / Failed 3 / Ignored 4。BUG332 fixture 在全量 XML 中为 Success。

全量 3 失败均为已知环境债（非本 WO）：

1. `Test_PreparedPool_ConcurrentSameSql_DoesNotCrossContaminateParams`（tasks CR-608）
2. `StaticPair_InjectedReplay_Unchanged`（CR-606）
3. `InjectedBitmap_FlowsThroughFrameDifferGate`（CR-606）

## 债务

| ID | 状态 |
|----|------|
| BLOCKED-DATA-P0-001 | **已解锁**（本工单 FIX-C） |
| BLOCKED-39X-DATA | 未清：3.9.x 真实列签名前缀待目标机 dump，禁止虚构 |

## 生产部署

| 项 | 值 |
|----|-----|
| 本工单改动 | 工作区；Commit 按授权范围 |
| 生产进程 | 未热更 |
| Git | `571b900` 已落本地 master；未 push |

## 门禁

- G1 Brief 已落。
- FIX-A～E 代码与 BUG332 证据齐。
- WO-20260902-001 与本单可在全量 XML 落档后一并 CLOSE（仍保留 BLOCKED-39X-DATA，不阻塞 4.x CLOSE）。
