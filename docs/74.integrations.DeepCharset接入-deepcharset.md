# DeepCharset - DeepBase AboutFrame 集成规划

## 项目概况

| 属�?| �?|
|------|-----|
| 项目路径 | `D:\_Progs\02Business\DeepCharset` |
| UI 框架 | **VCL** |
| 当前数据�?| **�?* |
| 目标数据�?| `DeepCharsetConfig.db` |
| AboutFrame 状�?| **�?* (需新建) |

## 项目结构概览

```
DeepCharset/
├── DeepCharset.dpr       # 主程序入�?
├── ViewMainCode.pas       # 主窗�?(TForm1)
├── ViewSynEdit.pas        # SynEdit 编辑器窗�?
├── Model*.pas             # 数据模型
├── Controller*.pas        # 控制�?
├── Helper*.pas            # 辅助�?
├── Utils*.pas             # 工具�?
├── Tests/                 # 测试
└── docs/                  # 文档
```

## 当前状态分�?

### 主程�?(DeepCharset.dpr)
- VCL 应用
- 支持命令行模�?(`TCommandLineController`)
- 可选的异常追踪 (madExcept/EurekaLog)
- 无数据库

### 主窗�?(ViewMainCode.pas)
- 文件编码转换工具
- 未使用任�?AboutFrame
- 未集�?DeepBase

### 技术特�?
- 轻量级工具应�?
- 无数据库依赖
- 支持 CLI �?GUI 双模�?

## 目标架构

### 新建数据�?
创建 `DeepCharsetConfig.db` 用于存放 AboutFrame 数据�?

```sql
CREATE TABLE IF NOT EXISTS aboutMeImages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  image_key TEXT NOT NULL UNIQUE,
  image_data BLOB NOT NULL,      -- AES-256 加密的图像数据（SeedTool 写入�?
  address_text TEXT,
  description TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,
  sha256_hash TEXT NOT NULL,
  hmac_sha256 TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 标准 ImageKeys
| Key | 描述 |
|-----|------|
| `official_gzh` | 官方公众�?|
| `wechat` | 微信收款�?|
| `alipay` | 支付宝收款码 |
| `btc` | BTC 钱包地址二维�?|
| `usdt` | USDT 钱包地址二维�?|
| `aboutme` | 个人介绍/名片 |

## 集成方案

### 方案概述
DeepCharset �?VCL 应用，可直接使用 `DeepBase.VCL.AboutFrame`�?

### 实施步骤

#### Phase 1: DeepBase 集成
1. 添加 DeepBase 到项目搜索路�?
2. �?dpr 文件添加必要�?uses:
   ```delphi
   uses
     DeepBase.Manager,
     DeepBase.AntiTamper,
     DeepBase.VCL.AboutFrame,
     ...
   ```

#### Phase 2: 数据库配�?
1. 创建 `DeepCharsetConfig.db`
2. 添加 `aboutMeImages` �?
3. 使用 SeedTool 填充 6 个标准图�?

#### Phase 3: UI 集成
可选方案：

**方案 A: 菜单�?+ 弹窗**
- 在主窗体菜单添加 "帮助" -> "关于"
- 点击弹出包含 AboutFrame 的窗�?

**方案 B: 主窗体内�?Tab**
- 在主窗体添加 TabSheet
- 嵌入 AboutFrame

**推荐: 方案 A** (工具类应用适合弹窗�?

#### Phase 4: 实现代码

```delphi
// 新建 AboutForm.pas
unit AboutForm;

interface

uses
  System.SysUtils,
  Vcl.Forms,
  DeepBase.AntiTamper,
  DeepBase.VCL.AboutFrame;

type
  TAboutForm = class(TForm)
  private
    FAboutFrame: TUniAboutFrame;
  public
    constructor Create(AOwner: TComponent); override;
  end;

implementation

constructor TAboutForm.Create(AOwner: TComponent);
var
  Config: TAntiTamperConfig;
begin
  inherited;
  Caption := '关于 码到成功';
  Width := 500;
  Height := 600;
  Position := poMainFormCenter;

  // AntiTamper 初始化（参数必须�?SeedTool 播种时一致）
  Config := TAntiTamperPackage.GetDefaultConfig;
  Config.EncryptionKey := 'DeepCharset_AntiTamper_Key_2025';
  Config.Salt := 'DeepCharset_Salt_v1';
  Config.KdfIterations := 10000;
  Config.EnableHMAC := True;
  TAntiTamperPackage.Initialize(Config);

  FAboutFrame := TUniAboutFrame.Create(Self);
  FAboutFrame.Parent := Self;
  FAboutFrame.Align := alClient;
  FAboutFrame.DatabasePath := ExtractFilePath(ParamStr(0)) + 'DeepCharsetConfig.db';
  FAboutFrame.ManualInitialize;
end;
```

## SeedTool 工作�?

SeedTool 当前�?GUI 方式使用（未实现 CLI 参数）。建议：
1. 打开 `DeepBase/Tools/SeedTool/SeedTool.exe`
2. 选择/创建 `DeepCharsetConfig.db`
3. 初始�?`aboutMeImages` 表（如不存在�?
4. 播种 6 张图片（ImageKey: official_gzh/wechat/alipay/btc/usdt/aboutme�?

## 测试清单

- [ ] DeepCharsetConfig.db 正确创建
- [ ] aboutMeImages 表包�?6 条记�?
- [ ] DeepBase 集成无冲�?
- [ ] "关于" 菜单项正确显�?
- [ ] 弹窗 AboutFrame 正确加载图片
- [ ] Tab 切换功能正常
- [ ] 地址复制功能正常
- [ ] 不影�?CLI 模式功能

## 时间估算

| 任务 | 预估时间 |
|------|----------|
| DeepBase 集成 | 0.5 小时 |
| 数据库配�?| 0.5 小时 |
| AboutForm 创建 | 0.5 小时 |
| 菜单集成 | 0.5 小时 |
| 测试验证 | 0.5 小时 |
| **总计** | **2.5 小时** |

## 依赖

- `DeepBase.VCL.AboutFrame` - VCL �?AboutFrame
- `DeepBase.AntiTamper` - 防篡改（解密 + sha256 + hmac�?
- `DeepBase.Manager` - 初始化管�?
- SeedTool - 数据填充（GUI�?

## 备注

1. DeepCharset 是轻量级工具，集成方案应保持简�?
2. 弹窗�?AboutFrame 适合工具类应�?
3. 需确保 CLI 模式不加�?AboutFrame 相关代码
4. 可考虑延迟加载 AboutForm 以优化启动速度
