# Stocks/InfoCenter - DeepBase AboutFrame 集成规划

## 项目概况

| 属�?| �?|
|------|-----|
| 项目路径 | `D:\_Progs\02Business\Stocks\07-InformationCenter` |
| UI 框架 | **FMX (FireMonkey)** |
| 当前数据�?| `InfoCenterConfig.db`, `data.db` |
| 目标数据�?| `InfoCenterConfig.db` (复用) |
| AboutFrame 状�?| **�?* (需新建) |

## 项目结构概览

Stocks 是一个大型项目，包含多个子应用：

```
Stocks/
├── 07-InformationCenter/
�?  ├── 01-Admin/           # ConfigCenter 管理工具
�?  ├── 02-Service/         # 后台服务 (ABListener, InfoCenterSvc �?
�?  ├── 03-Frontend/        # InfoCenter 主应�?(FMX)
�?  ├── Common/             # 共享代码
�?  ├── InfoCenterConfig.db # 配置数据�?(已存�?
�?  └── data.db             # 业务数据
├── Middleware/             # 中间件服�?
└── StockAnalysisSystem.dpr # 股票分析系统
```

## 当前状态分�?

### InfoCenter 主应�?(03-Frontend)

**入口:** `InfoCenter.dpr`

**已集�?DeepBase:**
```delphi
uses
  DeepBase.Manager,
  DeepBase.Persistence.Manager.FireDAC,
  DeepBase.Config,
  DeepBase.Logging,
  DeepBase.i18n,
  DeepBase.Security,
  ...
```

**主窗�?** `ViewMain.pas` (TfrmMain)

**现有 Frame:**
- `ViewStockTree` - 股票�?
- `ViewNewsList` - 新闻列表
- `ViewStatusPanel` - 状态面�?
- `ViewNewsCard` - 新闻卡片

**数据库现�?**
- `InfoCenterConfig.db` - 配置存储 (已存�?
- �?`aboutMeImages` �?

### DeepBase 集成程度
InfoCenter 已深度集�?DeepBase，包括：
- `TDeepBaseManager` 初始�?
- 日志 (`DeepBase.Logging`)
- 国际�?(`DeepBase.i18n`)
- 安全 (`DeepBase.Security`)

## 目标架构

### 数据库配�?
复用现有 `InfoCenterConfig.db`，添�?`aboutMeImages` 表：

```sql
CREATE TABLE IF NOT EXISTS aboutMeImages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  image_key TEXT NOT NULL UNIQUE,
  image_data BLOB NOT NULL,      -- AES-256 加密的图像数据（SeedTool 写入�?
  address_text TEXT,
  description TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,
  sha256_hash TEXT NOT NULL,
  hmac_sha256 TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 标准 ImageKeys
| Key | 描述 |
|-----|------|
| `official_gzh` | 官方公众�?|
| `wechat` | 微信收款�?|
| `alipay` | 支付宝收款码 |
| `btc` | BTC 钱包地址二维�?|
| `usdt` | USDT 钱包地址二维�?|
| `aboutme` | 个人介绍/名片 |

## 集成方案

### 挑战：需�?FMX �?AboutFrame

�?DeepSync 相同，InfoCenter 使用 FMX 框架�?
需�?`DeepBase.FMX.AboutFrame` 组件�?

### 依赖：FMX �?AboutFrame

本集成依�?DeepSync 集成任务中创建的 `DeepBase.FMX.AboutFrame.pas`�?

### 实施步骤

#### Phase 1: 数据库扩�?
1. �?`InfoCenterConfig.db` 中添�?`aboutMeImages` �?
2. 使用 SeedTool 填充 6 个标准图�?

#### Phase 2: UI 集成
1. 在主窗体菜单或侧边栏添加 "关于" 入口
2. 创建 About 页面或弹�?
3. 嵌入 `TFMXAboutFrame`（`DeepBase.FMX.AboutFrame`，对齐后复用�?

#### Phase 3: 配置
1. 配置数据库路径指�?`InfoCenterConfig.db`
2. 初始�?`DeepBase.AntiTamper`（与 SeedTool 播种参数一致）
3. 验证图片可正确解密并通过 sha256/hmac 校验

## SeedTool 工作�?

SeedTool 当前�?GUI 方式使用（未实现 CLI 参数）。建议：
1. 打开 `DeepBase/Tools/SeedTool/SeedTool.exe`
2. 选择现有数据�? `InfoCenterConfig.db`
3. 初始�?`aboutMeImages` 表（如不存在�?
4. 播种 6 张图片（ImageKey: official_gzh/wechat/alipay/btc/usdt/aboutme�?

## 测试清单

- [ ] aboutMeImages 表成功添加到 InfoCenterConfig.db
- [ ] 表包�?6 条记�?
- [ ] 图片解密�?sha256/hmac 校验通过
- [ ] 菜单/侧边栏显�?"关于" 入口
- [ ] AboutFrame 正确加载并显示图�?
- [ ] 不影响现�?InfoCenter 功能

## 时间估算

| 任务 | 预估时间 |
|------|----------|
| 数据库扩�?| 0.5 小时 |
| UI 集成 | 1 小时 |
| 测试验证 | 0.5 小时 |
| **总计** | **2 小时** |

*�? 前提�?FMX �?AboutFrame 已完�?

## 依赖

- `DeepBase.FMX.AboutFrame`（对齐后复用�?
- `DeepBase.AntiTamper` - 防篡改（解密 + sha256 + hmac�?
- 现有 `InfoCenterConfig.db`
- SeedTool（GUI�?

## 备注

1. InfoCenter 已深度集�?DeepBase，添�?AboutFrame 相对简�?
2. 主要工作量在�?FMX �?AboutFrame 的开�?(�?DeepSync 共用)
3. 可考虑在设置页面或帮助菜单中添�?"关于" 入口
4. Stocks 项目的其他子应用 (Middleware �? 暂不需�?AboutFrame
