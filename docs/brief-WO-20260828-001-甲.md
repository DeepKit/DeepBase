# 本地契约 Brief · WO-20260828-001

- **工单编号**：WO-20260828-001
- **执行角色**：甲（框架AI，DeepBase 工作台）
- **下发时间**：2026-08-28 10:20:31 (GMT+8)
- **启动时间**：2026-08-28 10:33:30 (GMT+8)
- **目标**：验证并交付 DoQry 对 PostgreSQL 真实连接与四核心函数（`UniDbSelect` / `UniDbExec` / `UniDbInsertReturningId` / `UniDbScalar`）全链路支持，补齐驱动装配、安全凭据接入、事务回滚反事实用例及指南文档。

## 1. 范围（Scope）
1. **连接与驱动装配**：确保 `FireDAC.Phys.PG` / `TFDPhysPgDriverLink` 物理驱动在 DoQry 与测试体系中自动就绪；凭据通过 `DeepBase.Security` / `TCredentialManager` / 安全环境变量接入，禁止任何明文凭据入仓。
2. **真连联测与用例扩展**：在 `Tests\Test.DeepBase.DB.DoQry.pas` 中增加 PostgreSQL 真实连接测试夹具 `TTestDeepBaseDoQryPG`，覆盖四函数、多类型参数绑定（含 >2^53 BigInt、UUID、Numeric、Bool、Null）、事务提交、事务回滚、`UniDbRunInTx` 异常自动回滚（反事实测试）、唯一约束冲突错误码映射。
3. **环境适应与跳过**：无 PG 实例或凭据不可用时显式 skip，有 PG 时真实执行。
4. **文档更新**：更新 `docs\41.api.DoQry指南-doqry-guide.md`，补充 PG 驱动配置、凭据接入、方言特性与已知限制。

## 2. 验收标准（Acceptance Criteria）
1. 真实 PostgreSQL 实例上四函数及事务测试全部通过，测试原始输出归档至 `TestResults/`。
2. 凭据不在任何明文代码、配置文件或测试数据中。
3. 单元测试套件整体保持全绿（无回归）。
4. `docs\41.api.DoQry指南-doqry-guide.md` 索引与增量更新到位。
5. 交付报告留痕并通知主控。

## 3. 契约 Digest
- Base Git Commit: `a0c1582`
- Target Units: `Persistence\DeepBase.DB.DoQry.pas`, `Tests\Test.DeepBase.DB.DoQry.pas`, `docs\41.api.DoQry指南-doqry-guide.md`
