# DeepBase API 参考手�?

> Version 1.0 | 最后更�? 2026-05-03

## 目录

1. [核心管理器](#1-核心管理�?
2. [配置模块](#2-配置模块)
3. [国际化模块](#3-国际化模�?
4. [日志模块](#4-日志模块)
5. [插件系统](#5-插件系统)
6. [数据绑定](#6-数据绑定)
7. [MVVM框架](#7-mvvm框架)
8. [ORM模块](#8-orm模块)
9. [IoC容器](#9-ioc容器)
10. [安全模块](#10-安全模块)
11. [国际化扩展模块](#11-国际化扩展模�?
12. [数学工具模块](#12-数学工具模块)
13. [指标收集模块](#13-指标收集模块)
14. [网络工具模块](#14-网络工具模块)
15. [LLM 模块](#15-llm-模块)
16. [Unlock 解锁与私域引流模块](#16-unlock-解锁与私域引流模块概�?

---

## 1. 核心管理�?

### DeepBase.Manager

管理器单例，所�?DeepBase 功能的入口点�?

#### 属�?

| 属�?| 类型 | 说明 |
|------|------|------|
| `IsInitialized` | `Boolean` | 系统是否已初始化 |
| `Config` | `TDeepBaseConfig` | 配置管理器实�?|
| `I18n` | `TDeepBaseI18n` | 国际化实�?|
| `Logger` | `TDeepBaseLogger` | 日志管理器实�?|
| `ConfigDB` | `TFDConnection` | 配置数据库连�?|
| `ConfigDBPath` | `string` | 配置数据库文件路�?|
| `RootPath` | `string` | 项目根目�?|
| `CurrentLanguage` | `string` | 当前语言代码 |
| `CurrentTheme` | `string` | 当前主题名称 |
| `LastError` | `string` | 最后一次错误信�?|

#### 方法

##### Initialize

```delphi
function Initialize: Boolean;
```

初始�?DeepBase 系统。通过 `root.txt` 机制自动定位项目根目录和配置数据库，无需手动指定路径�?

调用 `Initialize*` 前，最终程序必须链接 `DeepBase.Persistence.Manager.FireDAC`。缺少该单元时 Manager 无法注册 DB1 连接适配器，会返回 `No DB connection adapter registered`。

**返回�?** 初始化是否成�?

**示例:**
```delphi
uses
  DeepBase.Manager,
  DeepBase.Persistence.Manager.FireDAC;

begin
  DeepBase.InitializeOrRaise;
end;
```

##### InitializeWithDB

```delphi
function InitializeWithDB(const DBPath: string): Boolean;
```

使用指定数据库路径初始化（支�?`:memory:` 用于测试）�?

**参数:**
- `DBPath`: 配置数据库路径，�?`':memory:'` 使用内存数据�?

**返回�?** 初始化是否成�?

**示例:**
```delphi
// 测试场景：使用内存数据库
DeepBase.InitializeWithDBOrRaise(':memory:');

// 指定配置数据库路�?
DeepBase.InitializeWithDBOrRaise('C:\MyApp\MyAppConfig.db');
```

##### WhenReady

```delphi
procedure WhenReady(ACallback: TProc);
```

注册初始化完成后的回调。如果已触发 `FireReadyCallbacks`，则异步执行回调。用于解决窗�?`FormShow` 中访�?DeepBase 功能时对象未就绪的问题�?

**参数:**
- `ACallback`: 回调过程

**示例:**
```delphi
procedure TMyForm.FormShow(Sender: TObject);
begin
  DeepBase.WhenReady(procedure
  begin
    LoadProviders;  // 安全：此�?DeepBase 已完全初始化
  end);
end;
```

##### FireReadyCallbacks

```delphi
procedure FireReadyCallbacks;
```

触发所有已注册�?Ready 回调（由 `Application.Run` 前调用）�?

**示例:**
```delphi
begin
  Application.Initialize;
  DeepBase.InitializeOrRaise;
  Application.CreateForm(TMainForm, MainForm);
  DeepBase.FireReadyCallbacks;
  Application.Run;
  DeepBase.Finalize;
end;
```

##### Finalize

```delphi
class procedure Finalize;
```

释放所�?DeepBase 资源�?

**示例:**
```delphi
procedure TMainForm.FormDestroy(Sender: TObject);
begin
  DeepBase.Finalize;
end;
```

##### GetConfig / SetConfig (便捷方法)

```delphi
function GetConfig(const Key: string; const Default: string = ''): string;
procedure SetConfig(const Key, Value: string);
```

配置读写的便捷方法�?

---

## 2. 配置模块

### TDeepBaseConfig

配置管理类，提供类型安全的配置存取�?

#### 方法

##### GetConfig

```delphi
function GetConfig(const Key: string; const Default: string = ''): string;
```

获取字符串配置值�?

**参数:**
- `Key`: 配置键名
- `Default`: 默认值（键不存在时返回）

**返回�?** 配置值或默认�?

##### SetConfig

```delphi
procedure SetConfig(const Key, Value: string);
```

设置字符串配置值�?

##### GetConfigInt / SetConfigInt

```delphi
function GetConfigInt(const Key: string; Default: Integer = 0): Integer;
procedure SetConfigInt(const Key: string; Value: Integer);
```

整数类型配置存取�?

##### GetConfigBool / SetConfigBool

```delphi
function GetConfigBool(const Key: string; Default: Boolean = False): Boolean;
procedure SetConfigBool(const Key: string; Value: Boolean);
```

布尔类型配置存取�?

##### GetConfigFloat / SetConfigFloat

```delphi
function GetConfigFloat(const Key: string; Default: Double = 0.0): Double;
procedure SetConfigFloat(const Key: string; Value: Double);
```

浮点类型配置存取�?

##### GetConfigEncrypted / SetConfigEncrypted (deprecated)

```delphi
function GetConfigEncrypted(const Key: string; const Default: string = ''): string;
procedure SetConfigEncrypted(const Key, Value: string);
```

兼容保留接口；新代码应使�?`DeepBase.Security.SaveSecret/LoadSecret`。LLM API Key �?`TDeepBaseLLM.SaveConfig` 写入 Windows Credential Manager，数据库仅保�?`credman:` 引用�?

##### DeleteConfig

```delphi
procedure DeleteConfig(const Key: string);
```

删除配置项�?

##### ConfigExists

```delphi
function ConfigExists(const Key: string): Boolean;
```

检查配置是否存在�?

##### GetAllConfigs

```delphi
function GetAllConfigs(const Category: string = ''): TDictionary<string, string>;
```

获取所有配置（可按分类过滤）�?

##### ClearCache

```delphi
procedure ClearCache;
```

清除配置缓存�?

#### 事件

##### OnConfigChanged

```delphi
property OnConfigChanged: TConfigChangedEvent;
// TConfigChangedEvent = procedure(const Key, NewValue: string) of object;
```

配置变更时触发�?

**示例:**
```delphi
DeepBase.Config.OnConfigChanged := procedure(const Key, Value: string)
begin
  if Key = 'app.theme' then
    ApplyTheme(Value);
end;
```

---

## 3. 国际化模�?

### TDeepBasei18n

国际化模块，提供翻译和语言管理功能�?

#### 属�?

| 属�?| 类型 | 说明 |
|------|------|------|
| `CurrentLanguage` | `string` | 当前语言代码 (�?'zh-CN') |
| `AvailableLanguages` | `TArray<string>` | 可用语言列表 |
| `FallbackLanguage` | `string` | 回退语言 |

#### 方法

##### Translate (T)

```delphi
function Translate(const Key: string; const Default: string = ''): string;
// 全局快捷函数
function T(const Key: string): string;
```

翻译文本�?

**参数:**
- `Key`: 翻译�?
- `Default`: 默认值（无翻译时返回�?

**示例:**
```delphi
Label.Caption := T('Welcome');
Button.Caption := T('OK');
```

##### TranslateFormat (TFmt)

```delphi
function TranslateFormat(const Key: string; const Args: array of const): string;
// 全局快捷函数
function TFmt(const Key: string; const Args: array of const): string;
```

带参数格式化翻译�?

**示例:**
```delphi
ShowMessage(TFmt('Hello, %s!', [Username]));
ShowMessage(TFmt('You have %d new messages', [Count]));
```

##### TranslatePlural (TPlural)

```delphi
function TranslatePlural(const Singular, Plural: string; Count: Integer): string;
// 全局快捷函数
function TPlural(const Singular, Plural: string; Count: Integer): string;
```

复数形式翻译�?

**示例:**
```delphi
Label.Caption := TPlural('1 item', '%d items', ItemCount);
```

##### SetLanguage

```delphi
procedure SetLanguage(const LanguageCode: string);
```

设置当前语言�?

**参数:**
- `LanguageCode`: 语言代码 (�?'en-US', 'zh-CN')

##### TranslateForm

```delphi
procedure TranslateForm(Form: TForm);
```

自动翻译窗体上所有控件�?

##### AddTranslation

```delphi
procedure AddTranslation(const Key, Language, Value: string);
```

添加翻译条目�?

##### LoadTranslations

```delphi
procedure LoadTranslations(const FileName: string);
```

�?JSON 文件加载翻译�?

**JSON 格式:**
```json
{
  "Welcome": {
    "en-US": "Welcome",
    "zh-CN": "欢迎"
  },
  "Hello, %s!": {
    "en-US": "Hello, %s!",
    "zh-CN": "你好�?s�?
  }
}
```

#### 事件

##### OnLanguageChanged

```delphi
property OnLanguageChanged: TLanguageChangedEvent;
```

语言切换时触发�?

---

## 4. 日志模块

### TDeepBaseLogger

日志模块，支持多级别日志和多种输出目标�?

#### 日志级别

```delphi
TLogLevel = (llDebug, llInfo, llWarn, llError, llFatal);
```

#### 方法

##### Debug / Info / Warn / Error / Fatal

```delphi
procedure Debug(const Msg: string; const Source: string = '');
procedure Info(const Msg: string; const Source: string = '');
procedure Warn(const Msg: string; const Source: string = '');
procedure Error(const Msg: string; const Source: string = '');
procedure Fatal(const Msg: string; const Source: string = '');
```

记录不同级别的日志�?

**示例:**
```delphi
Logger.Debug('Processing started');
Logger.Info('User logged in: ' + Username);
Logger.Warn('Configuration file not found, using defaults');
Logger.Error('Database connection failed');
Logger.Fatal('Critical system error');
```

##### Log

```delphi
procedure Log(const Msg: string; Level: TLogLevel = llInfo; const Source: string = ''); overload;
procedure Log(const Msg: string; const Args: array of const; Level: TLogLevel = llInfo); overload;
```

通用日志方法�?

##### LogException

```delphi
procedure LogException(E: Exception; const Msg: string = ''; Level: TLogLevel = llError);
```

记录异常日志�?

##### BeginTiming

```delphi
function BeginTiming(const Operation: string): ILogTimer;
```

开始性能计时�?

**示例:**
```delphi
var Timer := Logger.BeginTiming('DataLoad');
try
  LoadData;
finally
  Timer.Stop;  // 自动记录: "DataLoad completed in 123 ms"
end;
```

##### Query

```delphi
function Query(const Filter: TLogFilter): TArray<TLogEntry>;
```

查询日志记录�?

##### Clear

```delphi
procedure Clear(BeforeDate: TDateTime = 0);
```

清除日志（可指定日期前的日志）�?

#### 属�?

| 属�?| 类型 | 说明 |
|------|------|------|
| `MinLevel` | `TLogLevel` | 最低日志级�?|
| `StorageMode` | `TLogStorageMode` | 存储模式 (`lsmDatabase`/`lsmFile`/`lsmBoth`) |
| `LogFormat` | `TLogFormat` | 文件日志格式 (`lfText`/`lfJson`) |
| `MaxLogFileSizeMB` | `Integer` | 日志文件最大大�?(MB，默�?10) |
| `AggregatorEnabled` | `Boolean` | 是否启用日志聚合 |

---

## 5. 插件系统

### IDeepBasePlugin

插件接口，所有插件必须实现�?

```delphi
IDeepBasePlugin = interface
  ['{...}']
  function GetPluginInfo: TPluginInfo;
  procedure Initialize(const Host: IPluginHost);
  procedure Finalize;
end;
```

### TPluginManager

插件管理器�?

#### 方法

##### LoadPlugins

```delphi
procedure LoadPlugins(const Directory: string = 'Plugins');
```

从目录加载所有插件�?

##### LoadPlugin

```delphi
function LoadPlugin(const FileName: string): Boolean;
```

加载单个插件�?

##### UnloadPlugin

```delphi
procedure UnloadPlugin(const PluginId: string);
```

卸载插件�?

##### GetPlugin

```delphi
function GetPlugin<T: IInterface>(const PluginId: string = ''): T;
```

获取插件实例�?

##### GetAllPlugins

```delphi
function GetAllPlugins: TArray<TPluginInfo>;
```

获取所有已加载插件信息�?

---

## 6. 数据绑定

### TDataBinder

数据绑定器�?

#### 方法

##### Bind

```delphi
procedure Bind(Source: TObject; const SourceProperty: string;
  Target: TObject; const TargetProperty: string;
  Mode: TBindingMode = bmOneWay;
  Converter: TValueConverter = nil);
```

绑定属性�?

**参数:**
- `Source`: 数据源对�?
- `SourceProperty`: 源属性名
- `Target`: 目标对象（通常是控件）
- `TargetProperty`: 目标属性名
- `Mode`: 绑定模式 (OneWay, TwoWay, OneWayToSource)
- `Converter`: 值转换器

**示例:**
```delphi
// 单向绑定
Binder.Bind(User, 'Name', edtName, 'Text');

// 双向绑定
Binder.Bind(User, 'Active', chkActive, 'Checked', bmTwoWay);

// 带转换器
Binder.Bind(User, 'BirthDate', lblAge, 'Caption', bmOneWay,
  function(Value: TValue): TValue
  begin
    Result := FormatDateTime('yyyy-mm-dd', Value.AsType<TDateTime>);
  end);
```

##### Unbind

```delphi
procedure Unbind(Target: TObject; const TargetProperty: string = '');
```

解除绑定�?

##### UnbindAll

```delphi
procedure UnbindAll;
```

解除所有绑定�?

##### UpdateSource

```delphi
procedure UpdateSource;
```

手动更新源对象�?

##### UpdateTarget

```delphi
procedure UpdateTarget;
```

手动更新目标对象�?

---

## 7. MVVM框架

### TViewModelBase

ViewModel 基类�?

#### 方法

##### RaisePropertyChanged

```delphi
procedure RaisePropertyChanged(const PropertyName: string);
```

触发属性变更通知�?

**示例:**
```delphi
procedure TUserViewModel.SetName(const Value: string);
begin
  if FName <> Value then
  begin
    FName := Value;
    RaisePropertyChanged('Name');
  end;
end;
```

### ICommand

命令接口�?

```delphi
ICommand = interface
  procedure Execute;
  function CanExecute: Boolean;
end;
```

### TRelayCommand

通用命令实现�?

```delphi
constructor Create(ExecuteProc: TProc; CanExecuteFunc: TFunc<Boolean> = nil);
```

**示例:**
```delphi
FSaveCommand := TRelayCommand.Create(
  procedure
  begin
    SaveUser;
  end,
  function: Boolean
  begin
    Result := IsValid;
  end);
```

---

## 8. ORM模块

### 属性标�?

| 属�?| 说明 |
|------|------|
| `[Table('name')]` | 指定表名 |
| `[Column('name')]` | 指定列名 |
| `[PrimaryKey]` | 主键 |
| `[AutoIncrement]` | 自增 |
| `[NotNull]` | 非空 |
| `[Unique]` | 唯一 |
| `[Default('value')]` | 默认�?|

### TDeepBaseORM

ORM 管理器�?

#### 方法

##### Find

```delphi
function Find<T: class>(Id: Variant): T;
```

按主键查找�?

##### Query

```delphi
function Query<T: class>: IQuery<T>;
```

创建查询构建器�?

##### Save

```delphi
procedure Save<T: class>(Entity: T);
```

保存实体（插入或更新）�?

##### Delete

```delphi
procedure Delete<T: class>(Entity: T); overload;
procedure Delete<T: class>(Id: Variant); overload;
```

删除实体�?

### IQuery<T>

查询构建器接口�?

```delphi
IQuery<T> = interface
  function Where(const Condition: string; const Args: array of const): IQuery<T>;
  function OrderBy(const Field: string; Descending: Boolean = False): IQuery<T>;
  function Skip(Count: Integer): IQuery<T>;
  function Take(Count: Integer): IQuery<T>;
  function ToList: TObjectList<T>;
  function First: T;
  function FirstOrDefault: T;
  function Count: Integer;
end;
```

**示例:**
```delphi
var Users := ORM.Query<TUser>
  .Where('Role = ? AND Active = ?', ['admin', True])
  .OrderBy('Username')
  .Skip(10)
  .Take(20)
  .ToList;
```

---

## 9. IoC容器

### TContainer

依赖注入容器�?

#### 方法

##### RegisterType

```delphi
procedure RegisterType<TInterface: IInterface; TImpl: class>;
```

注册类型映射�?

##### RegisterSingleton

```delphi
procedure RegisterSingleton<TInterface: IInterface; TImpl: class>;
```

注册单例�?

##### RegisterInstance

```delphi
procedure RegisterInstance<TInterface: IInterface>(Instance: TInterface);
```

注册现有实例�?

##### RegisterFactory

```delphi
procedure RegisterFactory<TInterface: IInterface>(Factory: TFunc<TInterface>);
```

注册工厂方法�?

##### Resolve

```delphi
function Resolve<T: IInterface>: T;
```

解析依赖�?

##### IsRegistered

```delphi
function IsRegistered<T: IInterface>: Boolean;
```

检查是否已注册�?

**示例:**
```delphi
// 注册
Container.RegisterType<IUserRepository, TUserRepository>;
Container.RegisterSingleton<ILogger, TFileLogger>;
Container.RegisterFactory<IDbConnection>(
  function: IDbConnection
  begin
    Result := TDbConnection.Create(ConnectionString);
  end);

// 解析
var Repo := Container.Resolve<IUserRepository>;
var Logger := Container.Resolve<ILogger>;
```

---

## 10. 安全模块

### TEncryption

加密工具类�?

#### 方法

##### Encrypt / Decrypt

```delphi
class function Encrypt(const PlainText: string; const Key: TBytes): string;
class function Decrypt(const CipherText: string; const Key: TBytes): string;
```

AES-256 加解密�?

##### ProtectData / UnprotectData

```delphi
class function ProtectData(const Data: string): string;
class function UnprotectData(const ProtectedData: string): string;
```

Windows DPAPI 加解密（绑定当前用户）�?

##### Hash

```delphi
class function Hash(const Data: string; Algorithm: THashAlgorithm = htSHA256): string;
```

计算哈希值�?

##### VerifyHash

```delphi
class function VerifyHash(const Data, Hash: string; 
  Algorithm: THashAlgorithm = htSHA256): Boolean;
```

验证哈希�?

##### GenerateKey

```delphi
class function GenerateKey(Length: Integer = 32): TBytes;
```

生成随机密钥�?

### TAuthorizationManager

授权管理器�?

#### 方法

##### DefineRole

```delphi
procedure DefineRole(const RoleName: string; Permissions: TArray<string>);
```

定义角色及其权限�?

##### AssignRole / RevokeRole

```delphi
procedure AssignRole(const UserId, RoleName: string);
procedure RevokeRole(const UserId, RoleName: string);
```

分配/撤销角色�?

##### HasPermission

```delphi
function HasPermission(const UserId, Permission: string): Boolean;
```

检查用户是否有权限�?

##### GetUserRoles

```delphi
function GetUserRoles(const UserId: string): TArray<string>;
```

获取用户角色列表�?

##### GetRolePermissions

```delphi
function GetRolePermissions(const RoleName: string): TArray<string>;
```

获取角色权限列表�?

---

---

## 11. 国际化扩展模�?

### DeepBase.i18n.Gender

性别和大小写变体支持模块�?

#### TGenderVariant

性别变体选择和转换类�?

##### HasGender / HasCases

```delphi
class function HasGender(const ALangCode: string): Boolean;
class function HasCases(const ALangCode: string): Boolean;
```

检查语言是否支持语法性别/格�?

##### IsRTL

```delphi
class function IsRTL(const ALangCode: string): Boolean;
```

检查语言是否为从右到左书写�?

##### Select

```delphi
class function Select(const ALangCode: string; AGender: TGrammaticalGender;
  const AForms: array of string): string;
```

根据性别选择对应的文本变体�?

**示例:**
```delphi
// 德语：阳�?阴�?中�?
Text := TGenderVariant.Select('de', ggMasculine, 
  ['der Mann', 'die Frau', 'das Kind']);
```

##### Transform

```delphi
class function Transform(const ALangCode: string; const AText: string;
  AGender: TGrammaticalGender): string;
```

应用性别转换规则（如法语添加 e、西班牙�?o→a）�?

#### TRTLUtils

RTL 语言工具类�?

| 方法 | 说明 |
|------|------|
| `IsRTLChar(Char)` | 检查字符是否为 RTL 字符 |
| `ContainsRTL(Text)` | 检查文本是否包�?RTL 字符 |
| `EmbedRTL(Text)` | 添加 RTL 嵌入标记 |
| `EmbedLTR(Text)` | 添加 LTR 嵌入标记 |

#### TCaseUtils

大小写工具类�?

| 方法 | 说明 |
|------|------|
| `ToLower(Text)` | 转小�?|
| `ToUpper(Text)` | 转大�?|
| `ToTitleCase(Text)` | 转标题大小写 |
| `ToSentenceCase(Text)` | 转句子大小写 |
| `Capitalize(Text)` | 首字母大�?|

---

## 12. 数学工具模块

### DeepBase.Math

数学计算工具，包括向量、矩阵、统计、插值等�?

#### TVector2 / TVector3

2D/3D 向量类型�?

```delphi
var V1 := TVector2.Create(3.0, 4.0);
var Len := V1.Length;           // 5.0
var Norm := V1.Normalize;       // (0.6, 0.8)
var Dot := V1.Dot(V2);          // 点积
```

#### TStatistics

统计函数类�?

| 方法 | 说明 |
|------|------|
| `Mean(Values)` | 平均�?|
| `Median(Values)` | 中位�?|
| `Mode(Values)` | 众数 |
| `Variance(Values)` | 方差 |
| `StdDev(Values)` | 标准�?|
| `Percentile(Values, P)` | 百分位数 |
| `Correlation(X, Y)` | 相关系数 |
| `LinearRegression(X, Y, ...)` | 线性回�?|

#### TInterpolation

插值算法类�?

| 方法 | 说明 |
|------|------|
| `Linear(A, B, T)` | 线性插�?|
| `Cosine(A, B, T)` | 余弦插�?|
| `Cubic(...)` | 三次插�?|
| `Smoothstep(...)` | 平滑插�?|

#### TEasing

缓动函数类�?

| 方法 | 说明 |
|------|------|
| `Linear(T)` | 线�?|
| `QuadIn/Out/InOut(T)` | 二次缓动 |
| `CubicIn/Out/InOut(T)` | 三次缓动 |
| `SineIn/Out/InOut(T)` | 正弦缓动 |
| `ElasticIn/Out(T)` | 弹性缓�?|
| `BounceIn/Out(T)` | 弹跳缓动 |

#### TMathUtils

数值工具类�?

| 方法 | 说明 |
|------|------|
| `Clamp(V, Min, Max)` | 限制范围 |
| `GCD(A, B)` | 最大公约数 |
| `LCM(A, B)` | 最小公倍数 |
| `Factorial(N)` | 阶乘 |
| `IsPrime(N)` | 素数判断 |
| `Fibonacci(N)` | 斐波那契�?|

---

## 13. 指标收集模块

### DeepBase.Metrics

应用指标收集系统�?

#### TCounter

计数器指标（单调递增）�?

```delphi
var Counter := TMetrics.Counter('http_requests_total');
Counter.Inc;       // +1
Counter.Inc(5);    // +5
```

#### TGauge

仪表盘指标（即时值）�?

```delphi
var Gauge := TMetrics.Gauge('memory_usage_bytes');
Gauge.SetValue(1024 * 1024);
Gauge.Inc(100);
Gauge.Dec(50);
```

#### THistogram

直方图指标（分布统计）�?

```delphi
var Hist := TMetrics.Histogram('response_time_seconds');
Hist.Observe(0.25);
var Mean := Hist.Mean;
var P99 := Hist.Buckets;
```

#### TMetricsRegistry

指标注册表�?

```delphi
var JSON := Registry.ToJSON;           // JSON 格式
var Prom := Registry.ToPrometheus;     // Prometheus 格式
var Influx := Registry.ToInfluxLines;  // InfluxDB 行协�?
```

---

## 14. 网络工具模块

### DeepBase.Net

网络工具集�?

#### THttpClient_

HTTP 客户端�?

```delphi
var Client := THttpClient_.Create('https://api.example.com');
var Response := Client.Get('/users');
if Response.IsSuccess then
  ShowMessage(Response.Body);
```

#### TIPv4Address

IPv4 地址解析�?

```delphi
var IP := TIPv4Address.Parse('192.168.1.1');
if IP.IsPrivate then ...
if IP.IsLoopback then ...
```

#### TIPv4Subnet

子网计算�?

```delphi
var Subnet := TIPv4Subnet.Parse('192.168.1.0/24');
if Subnet.Contains(IP) then ...
var HostCount := Subnet.GetHostCount;  // 254
```

#### TNetworkUtils

| 方法 | 说明 |
|------|------|
| `UrlEncode(Text)` | URL 编码 |
| `UrlDecode(Text)` | URL 解码 |
| `IsValidIPv4(IP)` | 验证 IPv4 地址 |
| `IsValidHostname(Host)` | 验证主机�?|
| `GetServicePort(Name)` | 获取服务端口 |

---

## 15. LLM 模块

LLM 模块提供两层 API�?

- `TDeepBaseLLM`：直接模型调用、模型配置、调用历史与用量统计�?
- `TLLMManager`：基于提示词 `InternalCode` 的版本管理、变量替换、元提示词和 BoundQuery 上下文�?

底层模型配置使用 canonical �?`LLMConfig`�?

### 15.1 TDeepBaseLLM

#### 配置结构

| 字段 | 类型 | 说明 |
|------|------|------|
| `Name` | `string` | 配置名，�?`Default`、`CodeGen` |
| `Provider` | `TLLMProvider` | `lpOpenAI`、`lpAnthropic`、`lpAzure`、`lpLiteLLM`、`lpOllama`、`lpCustom` |
| `BaseUrl` | `string` | API Base URL |
| `ApiKey` | `string` | API Key 或代理密�?|
| `Model` | `string` | 模型�?|
| `MaxTokens` | `Integer` | 最大输�?Token |
| `Temperature` | `Double` | 采样温度 |
| `SystemPrompt` | `string` | 系统提示�?|
| `InputTokenPrice` | `Double` | �?1K 输入 Token 价格 |
| `OutputTokenPrice` | `Double` | �?1K 输出 Token 价格 |
| `IsEnabled` | `Boolean` | 是否启用 |
| `IsDefault` | `Boolean` | 是否默认配置 |

#### 关键方法

| 方法 | 说明 |
|------|------|
| `GetConfig(ConfigName)` | 获取模型配置 |
| `GetAllConfigs` | 获取全部模型配置 |
| `SaveConfig(Config)` | 保存模型配置 |
| `DeleteConfig(ConfigName)` | 删除模型配置 |
| `TestConnection(ConfigName, DurationMs, ErrorMsg)` | 测试配置连通�?|
| `Chat(Prompt, Response, ConfigName)` | 单轮对话 |
| `ChatWithMessages(Messages, Response, ConfigName)` | 多轮消息对话 |
| `ChatAsync(Prompt, OnComplete, ConfigName)` | 异步对话 |
| `ChatStream(Prompt, OnChunk, ConfigName)` | 流式回调 |
| `GetCallHiDeepDeepDeepDeepDeepStory(Limit, ConfigName)` | 查询调用历史 |
| `GetUsageStats(ConfigName, DaysBack, TotalCalls, TotalTokens, TotalCost)` | 查询用量统计 |

#### 保存配置示例

```delphi
uses
  System.SysUtils,
  FireDAC.Comp.Client,
  DeepBase.LLM;

procedure SaveDefaultLLMConfig(AConnection: TFDConnection);
var
  LLMClient: TDeepBaseLLM;
  Config: TLLMConfig;
begin
  LLMClient := TDeepBaseLLM.Create(AConnection);
  try
    Config.Init;
    Config.Name := 'Default';
    Config.Provider := lpOpenAI;
    Config.BaseUrl := 'https://api.openai.com/v1';
    Config.ApiKey := GetEnvironmentVariable('OPENAI_API_KEY');
    Config.Model := 'gpt-4o-mini';
    Config.MaxTokens := 4096;
    Config.Temperature := 0.7;
    Config.SystemPrompt := 'You are a concise assistant.';
    Config.InputTokenPrice := 0.005;
    Config.OutputTokenPrice := 0.015;
    Config.IsEnabled := True;
    Config.IsDefault := True;

    LLMClient.SaveConfig(Config);
  finally
    LLMClient.Free;
  end;
end;
```

#### 请求/响应示例

```delphi
uses
  System.SysUtils,
  FireDAC.Comp.Client,
  DeepBase.LLM;

procedure ChatOnce(AConnection: TFDConnection);
var
  LLMClient: TDeepBaseLLM;
  Response: TLLMChatResponse;
begin
  LLMClient := TDeepBaseLLM.Create(AConnection);
  try
    if not LLMClient.Chat('用一句话介绍 DeepBase�?, Response, 'Default') then
      raise ELLMException.Create(Response.ErrorMessage);

    Writeln(Response.Content);
    Writeln(Format('tokens=%d duration=%dms',
      [Response.TotalTokens, Response.DurationMs]));
  finally
    LLMClient.Free;
  end;
end;
```

`TLLMChatResponse` 字段�?

| 字段 | 类型 | 说明 |
|------|------|------|
| `Success` | `Boolean` | 调用是否成功 |
| `Content` | `string` | 模型返回文本 |
| `FinishReason` | `string` | 结束原因 |
| `InputTokens` / `OutputTokens` / `TotalTokens` | `Integer` | Token 统计 |
| `DurationMs` | `Int64` | 调用耗时 |
| `ErrorCode` / `ErrorMessage` | `string` | 失败信息 |

### 15.2 TLLMManager

`TLLMManager` 面向提示词库调用。它会读�?`Prompts`、`PromptVersions`、`PromptMeta`、`PromptMetaBinding` �?`LLMConfig`，执行时�?`InternalCode` 选择生产版本或指定版本�?

#### 关键方法

| 方法 | 说明 |
|------|------|
| `Initialize` | 加载提示词、分类和元提示词缓存 |
| `RefreshCache` | 重新加载缓存 |
| `GetPrompt(InternalCode)` | 获取提示�?|
| `SavePrompt(Prompt)` | 保存提示�?|
| `GetVersion(InternalCode, VersionNum)` | 获取指定版本 |
| `SaveVersion(InternalCode, Version)` | 保存版本 |
| `SetProductionVersion(InternalCode, VersionNum)` | 设置生产版本 |
| `BindMetaPrompt(PromptCode, MetaCode, OrderIndex)` | 绑定元提示词 |
| `Execute(InternalCode)` | 执行生产版本 |
| `Execute(InternalCode, Params)` | 带变量执行生产版�?|
| `Execute(InternalCode, Params, VersionNum, ConfigName)` | 指定版本和配置执�?|
| `ExecuteAsync(InternalCode, Params, OnComplete, VersionNum, ConfigName)` | 异步执行 |
| `BuildFinalPrompt(InternalCode, Params, VersionNum)` | 预览最终提示词 |
| `TestVersion(InternalCode, VersionNum, Params, ConfigName)` | 测试单个版本 |
| `CompareVersions(InternalCode, VersionNums, Params, ConfigName)` | 对比多个版本 |

#### 提示词执行示�?

```delphi
uses
  System.SysUtils,
  System.Generics.Collections,
  System.Variants,
  FireDAC.Comp.Client,
  DeepBase.LLM,
  DeepBase.LLM.Manager;

procedure ExecuteManagedPrompt(AConnection: TFDConnection);
var
  LLMManager: TLLMManager;
  Params: TDictionary<string, Variant>;
  Response: TLLMResponse;
begin
  LLMManager := TLLMManager.Create(AConnection, False);
  try
    LLMManager.Initialize;

    Params := TDictionary<string, Variant>.Create;
    try
      Params.Add('customer_id', 1001);
      Params.Add('language', 'zh-CN');

      Response := LLMManager.Execute('01-01-001', Params, 3, 'Default');
      if not Response.Success then
        raise ELLMException.Create(Response.ErrorMessage);

      Writeln(Response.Content);
      Writeln(Format('tokens=%d cost=%.6f',
        [Response.TotalTokens, Response.Cost]));
    finally
      Params.Free;
    end;
  finally
    LLMManager.Free;
  end;
end;
```

`TLLMResponse` 字段�?

| 字段 | 类型 | 说明 |
|------|------|------|
| `Success` | `Boolean` | 调用是否成功 |
| `Content` | `string` | LLM 返回内容 |
| `FinishReason` | `string` | 结束原因 |
| `InputTokens` / `OutputTokens` / `TotalTokens` | `Integer` | Token 统计 |
| `DurationMs` | `Int64` | 调用耗时 |
| `PromptId` | `Integer` | 提示�?ID |
| `VersionNumber` | `Integer` | 实际执行版本 |
| `ConfigName` | `string` | 实际使用配置 |
| `Cost` | `Double` | 估算费用 |
| `ErrorCode` / `ErrorMessage` | `string` | 失败信息 |

#### 异步执行示例

```delphi
uses
  System.SysUtils,
  System.Generics.Collections,
  System.Threading,
  System.Variants,
  FireDAC.Comp.Client,
  DeepBase.LLM,
  DeepBase.LLM.Manager;

procedure ExecuteManagedPromptAsync(ALLMManager: TLLMManager);
var
  Params: TDictionary<string, Variant>;
begin
  Params := TDictionary<string, Variant>.Create;
  Params.Add('customer_id', 1001);

  ALLMManager.ExecuteAsync(
    '01-01-001',
    Params,
    procedure(const Response: TLLMResponse)
    begin
      try
        TThread.Queue(nil,
          procedure
          begin
            if Response.Success then
              Writeln(Response.Content)
            else
              Writeln('LLM error: ' + Response.ErrorMessage);
          end);
      finally
        Params.Free;
      end;
    end,
    0,
    'Default');
end;
```

### 15.3 TLLMImportExport

提示词库导入导出�?`DeepBase.LLM.ImportExport` 提供�?

| 方法 | 说明 |
|------|------|
| `ExportPrompts(FilePath, Format)` | 导出全部提示�?|
| `ExportSelectedPrompts(FilePath, InternalCodes, Format)` | 导出指定提示�?|
| `ExportToString(Format)` | 导出到字符串 |
| `ImportPrompts(FilePath, Mode)` | 从文件导�?|
| `ImportFromString(Content, Mode)` | 从字符串导入 |
| `ValidateImportFile(FilePath, ErrorMsg)` | 校验导入文件 |

---

## 16. Commerce 用户/订单/支付/权益模块（概览）

Commerce 位于 [DeepBaseFeatures.dpk](../DeepBaseFeatures.dpk)，提供“先识别用户，再创建订单，再发起支付，最后发放权益”的统一流程。真实后端与支付渠道通过接口适配，不写死在框架里�?
### 16.1 关键单元

| 单元 | 说明 |
|------|------|
| `DeepBase.Commerce.Types` | 用户、身份、商品、订单、支付、权益数据结�?|
| `DeepBase.Commerce.Storage` | `ICommerceStorage` 契约�?`TInMemoryCommerceStorage` |
| `DeepBase.Commerce.Service` | `TDeepBaseCommerceService` �?`ICommercePaymentGateway` |
| `DeepBase.Commerce.Backend.Contract` | 后端路由�?snake_case JSON 字段常量 |
| `DeepBase.Commerce.Backend.Http` | `TCommerceHttpStorage` �?`TCommerceHttpPaymentGateway` 生产 HTTP 后端适配�?|

### 16.2 主流�?API

| 方法 | 说明 |
|------|------|
| `EnsureUserForIdentity(Provider, ProviderUserId, AppId, UnionId)` | 按微�?邮箱/设备等外部身份创建或复用内部用户 |
| `RegisterProduct(Product)` | 注册可售商品和对应权�?|
| `CreateOrder(UserId, AppId, ProductId)` | 创建订单并生�?`OutTradeNo` |
| `BeginPayment(OrderId, Provider, Channel, PayerOpenId)` | 通过已注册网关创建支付意�?|
| `ConfirmPayment(Notification)` | 校验支付通知并发放权�?|
| `HasEntitlement(UserId, AppId, Code)` | 判断用户是否拥有有效权益 |
| `ConsumeEntitlement(UserId, AppId, Code, Count)` | 扣减次数型权�?|

### 16.3 生产接入约束

- 生产环境优先使用 `TCommerceHttpStorage` 接入统一后端；如不用 HTTP 后端，也必须提供真实 `ICommerceStorage`，保存用户、身份、商品、订单、支付记录、权益�?- 生产环境优先使用 `TCommerceHttpPaymentGateway` 调统一后端创建支付参数；如不用统一后端，也必须实现 `ICommercePaymentGateway`，并保证密钥和验签安全�?- 客户端不得直接把订单改为已支付，也不得直接发放权益�?- 支付确认以可信通知为准，并必须校验金额、币种和订单号�?
下游接入流程�?`docs/02.quickstart.下游接入流程-downstream-integration.md`�?
---

## 17. Unlock 解锁与私域引流模块（概览�?
> 本节保留本地工具的解�?API 概览。新的用�?付款/权益闭环优先使用 Commerce 模块�?
### 17.1 Unlock 管理器（TUnlockManager�?
解锁与私域引流的核心管理类，通常通过全局实例 `UnlockMgr` 使用�?

#### 枚举类型

```delphi
type
  TUnlockLevel = (ulFree, ulFollow, ulShare);
```

- `ulFree`  : 免费版（未解锁）
- `ulFollow`: 关注版（通过关注公众号获取解锁码�?
- `ulShare` : 分享版（通过分享获取更高级解锁码�?

#### 关键属�?

| 属�?| 类型 | 说明 |
|------|------|------|
| `ProductCode` | `string` | 产品代码（如 'TK'�?OS'，对应解锁码前缀�?|
| `ProductName` | `string` | 软件中文名（如『双键快启』『全能同步』） |
| `WeChatAccountId` | `string` | 公众号微信号（如 `aizhihuilab`�?|
| `WeChatKeyword` | `string` | 用户在公众号内需要回复的关键词（如『双键』『同步』） |
| `QRCodeImage` | `string` | 用于展示在解锁弹窗中的公众号二维码图片路�?|

#### 关键方法

```delphi
function GetUnlockLevel: TUnlockLevel;
function IsFullUnlocked: Boolean;
procedure ShowUnlockDialog;
```

- `GetUnlockLevel`：根据当前设备已输入的解锁码，返回对应解锁等级�?
- `IsFullUnlocked`：是否已处于最高解锁等级（通常对应 ulShare 或自定义最高级）�?
- `ShowUnlockDialog`：弹出统一的解锁对话框，引导用户扫码关注公众号并输入解锁码�?

#### 使用示例

```delphi
uses DeepBase.Unlock;

procedure TMainForm.FormCreate(Sender: TObject);
begin
  // 初始�?Unlock 参数（建议在程序启动时配置一次）
  UnlockMgr.ProductCode    := 'TK';          // TwoKeyRun
  UnlockMgr.ProductName    := '双键快启';
  UnlockMgr.WeChatAccountId:= 'aizhihuilab';
  UnlockMgr.WeChatKeyword  := '双键';
  UnlockMgr.QRCodeImage    := 'res\\wechat_qr.png';
end;

procedure TMainForm.UpdateSlotsByUnlock;
begin
  case UnlockMgr.GetUnlockLevel of
    ulFree:   Slots := 24;
    ulFollow: Slots := 58;  // 关注�?
    ulShare:  Slots := 60;  // 分享版（完整功能�?
  end;
end;

procedure TMainForm.BtnUnlockClick(Sender: TObject);
begin
  if not UnlockMgr.IsFullUnlocked then
    UnlockMgr.ShowUnlockDialog;
end;
```

> 约定�?
> - `ProductCode` 必须与解锁码前缀一致（�?TK2601A / OS2601A）�?
> - `WeChatKeyword` 必须与公众号自动回复中配置的关键词一致（如『双键』『同步』）�?
> - 所有新程序应在主窗口或核心功能处调�?`ShowUnlockDialog`，保证用户易于发现解锁入口�?

---

## 附录：全局函数

| 函数 | 说明 |
|------|------|
| `T(Key)` | 翻译文本 |
| `TFmt(Key, Args)` | 格式化翻�?|
| `TPlural(Singular, Plural, Count)` | 复数翻译 |
| `GenderSelect(Lang, Gender, Forms)` | 性别变体选择 |
| `IsRTLLanguage(Lang)` | RTL 语言检�?|
| `DeepBase` | 获取管理器实�?|
| `TMetrics.Counter(Name)` | 获取/创建计数�?|
| `TMetrics.Gauge(Name)` | 获取/创建仪表�?|

## 附录：异常类�?

| 异常 | 说明 |
|------|------|
| `EDeepBaseException` | 基础异常�?|
| `EConfigException` | 配置错误 |
| `EConfigKeyNotFound` | 配置键不存在 |
| `Ei18nException` | 国际化错�?|
| `EPluginException` | 插件错误 |
| `ESecurityException` | 安全错误 |
| `EMathException` | 数学计算错误 |
| `EMetricsException` | 指标收集错误 |
| `ENetException` | 网络错误 |
