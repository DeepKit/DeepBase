# UniFlow MVP 开发规�?

> **版本**: 1.0 | **更新日期**: 2025-12-03
> MVP 核心命题：证�?UniFlow 能力超越 DevDirector，首个验证场景为「洞见�?

---

## 1. MVP 目标

### 1.1 核心命题

证明 UniFlow 作为「智能应用操作系统」的能力�?
- 11 角色架构级分工（vs DevDirector �?4 AI 角色 Prompt 级分工）
- 任意拓扑 Workflow（vs DevDirector 的线�?00-xx 步骤�?
- Skill 可扩展（vs DevDirector 的固定能力）
- 硬执行安全（vs DevDirector �?Prompt 引导软执行）

### 1.2 验证场景

**洞见·思维显影�?* 的核心流程：
1. 用户输入决策问题
2. 多角色（决策教练、诘问者、心理镜�?..）并行推�?
3. 生成决策胶片（结构化输出�?
4. 生成 X 光片（觉察记录）
5. 存入持久记忆

### 1.3 验收标准

1. �?能承载「洞见」的多角色决策流�?
2. �?支持至少 3 个角色并行推�?
3. �?完整的推演过程可追溯
4. �?守卫能阻止危险操�?
5. �?失败能恢�?

---

## 2. 功能清单

### 2.1 11 角色运行�?

| 角色 | MVP 实现 | 核心职责 |
|------|----------|----------|
| 引擎 Engine | �?完整 | 系统生命周期、角色注�?|
| 督察 Inspector | �?完整 | 调试接口、状态查�?|
| 总指�?Commander | �?完整 | 任务分解、分�?|
| 调度�?Dispatcher | �?完整 | 流程编排、并行调�?|
| 智囊 Advisor | �?基础 | LLM 推理（单后端�?|
| 执行�?Executor | �?完整 | Skill 调用 |
| 守卫 Guard | �?完整 | 三重校验 |
| 军械�?Quartermaster | �?完整 | 资源与配额管理 |
| 后勤 Logistics | �?完整 | 资源管理、记忆读�?|
| 记录�?Chronicler | �?完整 | 日志、审�?|
| 通信�?SignalOfficer | �?基础 | HTTP 通信 |

### 2.2 Workflow 引擎

**支持的任务类�?*�?
| 类型 | 说明 | MVP |
|------|------|-----|
| SystemTask | 内置任务（变量、条件） | �?|
| SkillTask | Skill 调用 | �?|
| AgentTask | AI 推理 | �?|
| HumanTask | 人工介入 | �?|

**支持的流程结�?*�?
| 结构 | 说明 | MVP |
|------|------|-----|
| 顺序 | A �?B �?C | �?|
| 条件分支 | if/else | �?|
| 并行 | fork/join | �?|
| 循环 | while | ⚠️ 简单支�?|
| 子流�?| 嵌套 | �?1.0 |

### 2.3 Core Skills (6�?

| Skill | 说明 | 实现 |
|-------|------|------|
| SkillHTTP | HTTP 调用 | Delphi |
| SkillDatabase | 数据库读�?| Delphi (UniBase.DB) |
| SkillFile | 文件操作 | Delphi |
| SkillLLM | LLM 调用 | Delphi (UniBase.LLM) |
| SkillTimer | 定时触发 | Delphi (UniBase.Scheduler) |
| SkillHuman | 人工任务 | Delphi |
| SkillPython | Python 桥接 | Delphi �?HTTP |

### 2.4 Python Skill 服务

**服务端点**（与 05.01.API-UniFlow接口规范 保持一致）�?
|| 端点 | 方法 | 说明 |
||------|------|------|
|| `/skills` | GET | 获取可用 Skill 列表 |
|| `/skills/{name}` | GET | 获取 Skill 元信息（�?Schema�?|
|| `/skills/{name}/execute` | POST | 执行 Skill |
|| `/skills/{name}/execute/stream` | POST | 流式执行（SSE�?|
|| `/health` | GET | 健康检�?|

**MVP Python Skills**（洞见场景）�?
| Skill | 说明 |
|-------|------|
| decision_coach | 决策教练角色 |
| critic | 诘问者角�?|
| mirror | 心理镜子角色 |
| observer | 旁观者角�?|
| film_generator | 决策胶片生成 |
| xray_generator | X 光片生成 |

### 2.5 安全机制

**三重守卫**�?
```
请求 ──> [门卫] ──> 处理 ──> [监工] ──> 输出 ──> [审核官] ──> 响应
         入口校验      过程监控           出口校验
```

**信任边界**�?
| 区域 | 角色 | MVP 实现 |
|------|------|----------|
| 完全信任 | 引擎/督察/后勤/记录�?| �?无校�?|
| 有限信任 | 总指�?调度�?守卫/军械�?通信�?| �?权限检�?|
| 不信�?| 智囊/执行�?| �?输出必校�?|

### 2.6 记忆系统

| 层级 | 生命周期 | 存储 | MVP |
|------|----------|------|-----|
| 工作记忆 | 单次请求 | 内存 | �?|
| 会话记忆 | 单次会话 | 内存 | �?|
| 持久记忆 | 长期 | SQLite | �?|
| 知识�?| 永久 | SQLite + 全文搜索 | ⚠️ 简�?|

---

## 3. 接口定义

### 3.1 角色基础接口

```pascal
type
  /// 角色基础接口
  IRole = interface
    ['{GUID}']
    function GetRoleId: string;
    function GetRoleName: string;
    function GetTrustLevel: TTrustLevel;
    procedure Initialize;
    procedure Finalize;
  end;

  /// 信任级别
  TTrustLevel = (tlFull, tlLimited, tlUntrusted);
```

### 3.2 核心角色接口

```pascal
/// 引擎接口
IEngine = interface(IRole)
  procedure Start;
  procedure Stop;
  procedure RegisterRole(const ARole: IRole);
  function GetRole(const ARoleId: string): IRole;
end;

/// 总指挥接�?
ICommander = interface(IRole)
  function DecomposeTask(const ATask: TTask): TTaskList;
  procedure AssignTask(const ATask: TTask; const ARoleId: string);
end;

/// 调度员接�?
IDispatcher = interface(IRole)
  function CreateWorkflow(const ADefinition: TWorkflowDef): IWorkflow;
  procedure ExecuteWorkflow(const AWorkflow: IWorkflow);
  procedure PauseWorkflow(const AWorkflowId: string);
  procedure ResumeWorkflow(const AWorkflowId: string);
end;

/// 智囊接口
IAdvisor = interface(IRole)
  function Reason(const APrompt: string; const AContext: TContext): TReasonResult;
  function ReasonAsync(const APrompt: string; const AContext: TContext;
    const ACallback: TReasonCallback): IFuture;
end;

/// 执行者接�?
IExecutor = interface(IRole)
  function ExecuteSkill(const ASkillName: string; 
    const AInput: TSkillInput): TSkillOutput;
end;

/// 守卫接口
IGuard = interface(IRole)
  function ValidateInput(const AInput: TValue): TValidationResult;
  function ValidateOutput(const AOutput: TValue): TValidationResult;
  function CheckPermission(const AAction: string; const AContext: TContext): Boolean;
end;

/// 军需官接�?
IQuartermaster = interface(IRole)
  procedure RegisterSkill(const ASkill: ISkill);
  function GetSkill(const AName: string): ISkill;
  function ListSkills: TSkillMetaArray;
end;

/// 记录员接�?
IChronicler = interface(IRole)
  procedure Log(const ALevel: TLogLevel; const AMsg: string; const AContext: TContext);
  procedure RecordAction(const AAction: TActionRecord);
  function QueryLogs(const AFilter: TLogFilter): TLogArray;
end;
```

### 3.3 Skill 接口

```pascal
type
  /// Skill 元数�?
  TSkillMeta = record
    Name: string;
    Version: string;
    Description: string;
    InputSchema: TJSONSchema;
    OutputSchema: TJSONSchema;
    TrustLevel: TSkillTrustLevel;
    Language: TSkillLanguage;  // slDelphi, slPython
  end;

  /// Skill 接口
  ISkill = interface
    ['{GUID}']
    function GetMeta: TSkillMeta;
    function Validate(const AInput: TSkillInput): TValidationResult;
    function Execute(const AInput: TSkillInput): TSkillOutput;
  end;

  /// Skill 信任级别
  TSkillTrustLevel = (stlCore, stlOfficial, stlCommunity, stlUser);
  
  /// Skill 语言
  TSkillLanguage = (slDelphi, slPython);
```

### 3.4 Workflow 接口

```pascal
type
  /// Workflow 状�?
  TWorkflowState = (wsNew, wsPending, wsRunning, wsPaused, wsCompleted, wsFailed);

  /// 任务节点
  TTaskNode = record
    Id: string;
    TaskType: TTaskType;  // ttSystem, ttSkill, ttAgent, ttHuman
    SkillName: string;
    Input: TJSONObject;
    DependsOn: TArray<string>;
    Timeout: Integer;
  end;

  /// Workflow 定义
  TWorkflowDef = record
    Id: string;
    Name: string;
    Nodes: TArray<TTaskNode>;
    StartNodeId: string;
  end;

  /// Workflow 接口
  IWorkflow = interface
    function GetId: string;
    function GetState: TWorkflowState;
    function GetCurrentNode: string;
    function GetResult: TJSONObject;
    procedure Start;
    procedure Pause;
    procedure Resume;
    procedure Cancel;
  end;
```

### 3.5 消息协议

```pascal
type
  /// 角色消息基类
  TRoleMessage = record
    MsgId: string;
    FromRole: string;
    ToRole: string;
    MsgType: string;
    Timestamp: TDateTime;
    Payload: TJSONObject;
  end;

  /// 任务分配消息
  TTaskAssignMsg = record
    Task: TTask;
    Priority: TTaskPriority;
    Deadline: TDateTime;
  end;

  /// 任务完成消息
  TTaskCompleteMsg = record
    TaskId: string;
    Success: Boolean;
    Result: TJSONObject;
    Error: string;
  end;

  /// 校验请求消息
  TValidateRequestMsg = record
    DataType: string;
    Data: TValue;
    Rules: TArray<string>;
  end;
```

---

## 4. 数据模型

### 4.1 核心�?

```sql
-- Workflow 定义�?
CREATE TABLE workflow_definitions (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT,
    definition_json TEXT NOT NULL,  -- TWorkflowDef JSON
    is_active INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Workflow 实例�?
CREATE TABLE workflow_instances (
    id TEXT PRIMARY KEY,
    definition_id TEXT NOT NULL,
    state TEXT NOT NULL,  -- wsNew, wsPending, wsRunning...
    current_node TEXT,
    context_json TEXT,
    result_json TEXT,
    error_message TEXT,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (definition_id) REFERENCES workflow_definitions(id)
);

-- 任务执行记录�?
CREATE TABLE task_executions (
    id TEXT PRIMARY KEY,
    workflow_id TEXT NOT NULL,
    node_id TEXT NOT NULL,
    task_type TEXT NOT NULL,
    skill_name TEXT,
    input_json TEXT,
    output_json TEXT,
    state TEXT NOT NULL,
    error_message TEXT,
    started_at TIMESTAMP,
    completed_at TIMESTAMP,
    duration_ms INTEGER,
    FOREIGN KEY (workflow_id) REFERENCES workflow_instances(id)
);

-- Skill 注册�?
CREATE TABLE skill_registry (
    name TEXT PRIMARY KEY,
    version TEXT NOT NULL,
    description TEXT,
    input_schema TEXT,
    output_schema TEXT,
    trust_level TEXT NOT NULL,  -- Core, Official, Community, User
    language TEXT NOT NULL,  -- Delphi, Python
    endpoint TEXT,  -- Python Skill �?HTTP 端点
    is_active INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 4.2 记忆�?

```sql
-- 会话记忆�?
CREATE TABLE session_memory (
    id TEXT PRIMARY KEY,
    session_id TEXT NOT NULL,
    key TEXT NOT NULL,
    value_json TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    expires_at TIMESTAMP,
    UNIQUE(session_id, key)
);

-- 持久记忆�?
CREATE TABLE persistent_memory (
    id TEXT PRIMARY KEY,
    category TEXT NOT NULL,  -- decision, insight, preference...
    key TEXT NOT NULL,
    value_json TEXT NOT NULL,
    tags TEXT,  -- JSON array
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(category, key)
);

-- 审计日志�?
CREATE TABLE audit_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    role_id TEXT NOT NULL,
    action TEXT NOT NULL,
    target TEXT,
    input_summary TEXT,
    output_summary TEXT,
    success INTEGER,
    error_message TEXT,
    duration_ms INTEGER,
    context_json TEXT
);
```

---

## 5. Python Skill 服务规范

### 5.1 项目结构

```
uniFlow_py/
├── main.py                 # FastAPI 入口
├── config.py               # 配置
├── requirements.txt
├── skills/
�?  ├── __init__.py
�?  ├── base.py             # Skill 基类
�?  ├── registry.py         # Skill 注册
�?  ├── decision_coach.py   # 决策教练
�?  ├── critic.py           # 诘问�?
�?  ├── mirror.py           # 心理镜子
�?  ├── observer.py         # 旁观�?
�?  ├── film_generator.py   # 决策胶片生成
�?  └── xray_generator.py   # X光片生成
├── models/
�?  ├── __init__.py
�?  ├── request.py          # 请求模型
�?  └── response.py         # 响应模型
└── tests/
    └── ...
```

### 5.2 Skill 基类

```python
from abc import ABC, abstractmethod
from pydantic import BaseModel
from typing import Any, Dict

class SkillMeta(BaseModel):
    name: str
    version: str
    description: str
    input_schema: Dict[str, Any]
    output_schema: Dict[str, Any]

class SkillInput(BaseModel):
    data: Dict[str, Any]
    context: Dict[str, Any] = {}

class SkillOutput(BaseModel):
    success: bool
    data: Dict[str, Any] = {}
    error: str = ""

class BaseSkill(ABC):
    @abstractmethod
    def get_meta(self) -> SkillMeta:
        pass
    
    @abstractmethod
    async def execute(self, input: SkillInput) -> SkillOutput:
        pass
    
    def validate(self, input: SkillInput) -> bool:
        return True
```

### 5.3 API 端点

```python
from fastapi import FastAPI, HTTPException
from fastapi.responses import StreamingResponse
from skills.registry import SkillRegistry

app = FastAPI(title="UniFlow Python Skills")
registry = SkillRegistry()

@app.get("/skills")
async def list_skills():
    return {"skills": [s.get_meta().dict() for s in registry.list_all()]}

@app.get("/skills/{name}")
async def get_schema(name: str):
    skill = registry.get(name)
    if not skill:
        raise HTTPException(404, f"Skill '{name}' not found")
    meta = skill.get_meta()
    return {
        "input_schema": meta.input_schema,
        "output_schema": meta.output_schema
    }

@app.post("/skills/{name}/execute")
async def execute_skill(name: str, request: ExecuteRequest):
    skill = registry.get(name)
    if not skill:
        raise HTTPException(404, f"Skill '{name}' not found")
    
    try:
        result = await skill.execute(request.input)
        return result.dict()
    except Exception as e:
        return SkillOutput(success=False, error=str(e)).dict()

@app.post("/skills/{name}/execute/stream")
async def stream_skill(name: str, request: ExecuteRequest):
    skill = registry.get(name)
    if not skill:
        raise HTTPException(404, f"Skill '{request.skill_name}' not found")
    
    async def generate():
        async for chunk in skill.stream(request.input):
            yield f"data: {chunk}\n\n"
    
    return StreamingResponse(generate(), media_type="text/event-stream")

@app.get("/health")
async def health():
    return {"status": "ok"}
```

---

## 6. 实现步骤

### 6.1 第一阶段：基础框架�?周）

1. 创建 UniFlow.Types.pas - 公共类型定义
2. 创建 UniFlow.Message.pas - 消息协议
3. 创建 IRole 基础接口
4. 实现 Engine（引擎）角色
5. 实现 Inspector（督察）角色
6. 创建数据库表

### 6.2 第二阶段：核心角色（3周）

1. 实现 Commander（总指挥）
2. 实现 Dispatcher（调度员�? Workflow 引擎
3. 实现 Executor（执行者）
4. 实现 Guard（守卫）
5. 实现 Quartermaster（军需官�?

### 6.3 第三阶段：支撑角色（2周）

1. 实现 Advisor（智囊）- 包装 UniBase.LLM
2. 实现 Logistics（后勤）
3. 实现 Chronicler（记录员�?
4. 实现 SignalOfficer（通信官）
5. 实现 Memory 系统

### 6.4 第四阶段：Skill 体系�?周）

1. 实现 ISkill 接口和基�?
2. 实现 6 �?Core Skills（Delphi�?
3. 搭建 Python Skill 服务
4. 实现 SkillPython 桥接
5. 实现洞见场景�?Python Skills

### 6.5 第五阶段：集成验证（3周）

1. 集成洞见应用
2. 实现多角色并行推�?
3. 实现决策胶片生成
4. 端到端测�?
5. 性能优化

---

## 7. 验收检查清�?

### 7.1 功能验收

- [ ] 11 角色全部实现并可正常通信
- [ ] Workflow 支持顺序、条件、并行执�?
- [ ] 6 �?Core Skills 正常工作
- [ ] Python Skill 服务可正常调�?
- [ ] 守卫能阻止非法输�?输出
- [ ] 记忆系统支持工作/会话/持久三层
- [ ] 审计日志完整记录所有操�?

### 7.2 洞见场景验收

- [ ] 用户可输入决策问�?
- [ ] 多角色（�?）可并行推演
- [ ] 推演过程可在 Inspector 中查�?
- [ ] 可生成决策胶片（结构化）
- [ ] 可生�?X 光片（觉察记录）
- [ ] 历史决策可查�?

### 7.3 质量验收

- [ ] 单元测试覆盖�?�?60%
- [ ] 无严重内存泄�?
- [ ] 单次决策流程 �?30 �?
- [ ] 文档完整

---

## 8. 相关文档

- **开发总览**：`06.01.Dev-UniFlow开发总览-v1.0.md`
- **数据模型**：`04.01.Data-UniFlow数据模型-v1.0.md`
- **接口规范**：`05.01.API-UniFlow接口规范-v1.0.md`
- **洞见产品**：`D:\_Progs\02Business\Insight\docs\01-01B.总览-洞见·思维显影室·梦想版 产品蓝图.md`

---

*文档版本: 1.0*
*更新日期: 2025-12-03*
