# UniFlow 架构关系说明

> 明确 UniFlow 与 SayDone、SoIs 的层次关系、角色映射与术语统一。

创建日期: 2025-12-04  版本: 1.0

---

## 1. 架构定位

### 1.1 三层产品架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户应用层                                │
│  ┌───────────────────┐  ┌───────────────────┐  ┌─────────────┐ │
│  │   SoIs（如是）     │  │  DevDirector      │  │  其他应用    │ │
│  │   对话交互系统      │  │  导演台           │  │             │ │
│  └─────────┬─────────┘  └─────────┬─────────┘  └──────┬──────┘ │
└────────────┼──────────────────────┼───────────────────┼────────┘
             │                      │                   │
             ▼                      ▼                   ▼
┌─────────────────────────────────────────────────────────────────┐
│                        能力转换层                                │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                 SayDone（述成）                             │ │
│  │              自然语言 → 结构化数据                           │ │
│  └───────────────────────────────────────────────────────────┘ │
└────────────────────────────────────┬────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                        运行时基础层                              │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    UniFlow                                 │ │
│  │              智能应用操作系统                               │ │
│  │   角色调度 · 消息路由 · 安全守卫 · 可观测性                 │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 各层职责

| 层次 | 产品 | 核心职责 | 用户感知 |
|------|------|----------|----------|
| **用户应用层** | SoIs、DevDirector | 用户交互、业务场景 | 直接可见 |
| **能力转换层** | SayDone | NL→结构化转换 | 间接（转换结果） |
| **运行时基础层** | UniFlow | 运行时调度、安全、监控 | 不可见 |

### 1.3 层间关系

```
┌──────────────────────────────────────────────────────────────┐
│  关系类型：                                                   │
│                                                              │
│  SoIs ─────uses────▶ SayDone ─────runs-on────▶ UniFlow     │
│                                                              │
│  • SoIs 调用 SayDone 的转换能力                              │
│  • SayDone 基于 UniFlow 运行时实现                           │
│  • UniFlow 提供角色框架，SayDone 实例化具体角色               │
└──────────────────────────────────────────────────────────────┘
```

### 1.4 技术栈定义

| 产品 | MVP 实现语言 | V2.0 可选 | 说明 |
|------|-------------|---------|------|
| **UniFlow** | Delphi (Object Pascal) | - | 与 SayDone/SoIs 保持一致 |
| **SayDone** | Delphi (FMX) | - | 现有实现 |
| **SoIs** | Delphi (FMX) | - | 现有实现 |

**技术栈统一决策**：

1. **MVP 阶段**：全部使用 **Delphi** 实现，确保与现有 SayDone/SoIs 无缝集成
2. **接口定义**：文档中 TypeScript 定义作为参考规范，实际实现用 Delphi Interface
3. **消息交换**：使用 JSON 格式，配合 JSON Schema 验证
4. **跨语言扩展**（V2.0）：如需其他语言实现，通过 IPC/HTTP 集成

**Delphi 核心依赖**：

```
┌────────────────────────────────────────────────────────────┐
│  Delphi 技术栈                                              │
├────────────────────────────────────────────────────────────┤
│  UI 框架         │  FMX (FireMonkey)                        │
│  JSON 处理       │  System.JSON / SuperObject               │
│  HTTP 客户端     │  System.Net.HttpClient                   │
│  数据库          │  FireDAC + SQLite                        │
│  YAML 解析       │  Neslib.Yaml / 自实现                    │
│  正则表达式      │  System.RegularExpressions               │
└────────────────────────────────────────────────────────────┘
```

> 详细实现指南参见《UniFlow-MVP定义与实现指南.md》

---

## 2. 角色映射

### 2.1 角色对照表

| UniFlow 角色 | SayDone 角色 | SoIs 角色 | 说明 |
|-------------|-------------|-----------|------|
| **引擎** | - | - | UniFlow 独有，系统驱动 |
| **督察** | 监制 | 监工 | 观测与质量控制 |
| **总指挥** | 总导演 | - | 全局调度决策 |
| **调度员** | 总导演(部分) | 进度官 | Workflow 编排 |
| **智囊** | AI代理 | 澄清官/草图师/方案师 | AI 能力封装 |
| **执行者** | - | - | Skill 执行 |
| **守卫** | 守卫员 | 守卫员 | 安全校验（完全复用） |
| **军械官** | - | - | 资产管理 |
| **后勤** | - | - | 基础服务 |
| **记录员** | 记忆官 | 记忆官 | 记忆与审计（完全复用） |
| **通信官** | - | - | 外部通信 |
| - | 共情官 | 共情官 | SayDone 特有，情感感知 |
| - | 自检员 | 自检员 | SayDone 特有，AI 自审 |
| - | Diff分析员 | 比对员 | SayDone 特有，变更分析 |
| - | 回退官 | 回滚员 | SayDone 特有，版本回退 |
| - | - | 澄清官 | SoIs 特有，需求澄清 |
| - | - | 草图师 | SoIs 特有，界面草图 |
| - | - | 定位员 | SoIs 特有，问题定位 |
| - | - | 默认官 | SoIs 特有，智能默认值 |

### 2.2 角色继承关系

```
┌─────────────────────────────────────────────────────────────────┐
│                      角色继承关系图                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   UniFlow 基础角色                                              │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │  IRole (基类)                                             │ │
│   │    ├── IAdvisor (智囊)                                    │ │
│   │    ├── IGuard (守卫)                                      │ │
│   │    ├── IChronicler (记录员)                               │ │
│   │    └── ...                                                │ │
│   └──────────────────────────────────────────────────────────┘ │
│                            ▲                                    │
│                            │ extends                            │
│                            │                                    │
│   SayDone 扩展角色                                              │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │  SayDoneAdvisor extends IAdvisor                          │ │
│   │    • 增加 共情分析 能力                                    │ │
│   │    • 增加 自检 能力                                        │ │
│   │                                                           │ │
│   │  DiffAnalyzer (新角色)                                    │ │
│   │    • 变更计算                                              │ │
│   │    • 冲突检测                                              │ │
│   │                                                           │ │
│   │  RollbackManager (新角色)                                 │ │
│   │    • 版本管理                                              │ │
│   │    • 状态回退                                              │ │
│   └──────────────────────────────────────────────────────────┘ │
│                            ▲                                    │
│                            │ extends                            │
│                            │                                    │
│   SoIs 扩展角色                                                 │
│   ┌──────────────────────────────────────────────────────────┐ │
│   │  Clarifier extends SayDoneAdvisor                         │ │
│   │    • 需求分析                                              │ │
│   │    • 点选选项生成                                          │ │
│   │                                                           │ │
│   │  Sketcher (新角色)                                        │ │
│   │    • 界面草图生成                                          │ │
│   │    • SVG 渲染                                             │ │
│   └──────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.3 角色复用规则

**完全复用**（代码级复用）：
- 守卫员 → Guard
- 记忆官 → Chronicler
- 监工 → Inspector (观测功能)

**扩展复用**（继承扩展）：
- AI代理 → Advisor + 共情/自检能力
- 总导演 → Commander + Dispatcher 组合

**新增角色**（SayDone/SoIs 特有）：
- 共情官、自检员、Diff分析员、回退官
- 澄清官、草图师、定位员、默认官

---

## 3. 术语统一

### 3.1 术语对照表

| 概念 | UniFlow 术语 | SayDone 术语 | SoIs 术语 | 统一建议 |
|------|-------------|-------------|-----------|----------|
| 运行时核心 | Engine | - | - | **引擎** |
| 全局调度 | Commander | 总导演 | - | **总指挥** |
| 任务编排 | Dispatcher | (总导演) | 进度官 | **调度员** |
| AI 能力 | Advisor | AI代理 | 多个角色 | **智囊** |
| 安全校验 | Guard | 守卫员 | 守卫员 | **守卫** |
| 记录存储 | Chronicler | 记忆官 | 记忆官 | **记录员** |
| 质量监控 | Inspector | 监制 | 监工 | **督察** |
| 执行单元 | Skill | Skill | - | **技能/Skill** |
| 编排流程 | Workflow | 流程 | Loop | **工作流** |
| 用户输入 | ExternalRequest | 用户指令 | 需求 | **请求** |
| AI 输出问题 | Hallucination | 幻觉 | - | **幻觉** |

### 3.2 命名规范

**角色命名**：
- 文档描述使用中文名（如"智囊"）
- 代码中使用英文名（如 `IAdvisor`）
- 日志/指标使用英文小写（如 `advisor`）

**消息命名**：
- Action 使用大写下划线（如 `UNDERSTAND_INTENT`）
- 字段使用小驼峰（如 `correlationId`）

---

## 4. 集成方式

### 4.1 SayDone 集成 UniFlow

```typescript
// SayDone 启动配置
const sayDoneConfig: UniFlowConfig = {
  // 复用 UniFlow 基础角色
  roles: {
    engine: new UniFlowEngine(),
    guard: new UniFlowGuard(),
    chronicler: new UniFlowChronicler(),
    logistics: new UniFlowLogistics(),
    signalOfficer: new UniFlowSignalOfficer(),
    
    // SayDone 扩展角色
    advisor: new SayDoneAdvisor({
      empathyEnabled: true,
      selfCheckEnabled: true
    }),
    commander: new SayDoneDirector(),
    
    // SayDone 新增角色
    diffAnalyzer: new DiffAnalyzer(),
    rollbackManager: new RollbackManager()
  },
  
  // 注册 SayDone 特有 Workflows
  workflows: [
    'saydone-understand-workflow',
    'saydone-transform-workflow',
    'saydone-validate-workflow'
  ]
}

const uniflow = await UniFlow.create(sayDoneConfig)
```

### 4.2 SoIs 集成 SayDone

```typescript
// SoIs 启动配置（基于 SayDone）
const soisConfig: SayDoneConfig = {
  // 继承 SayDone 配置
  ...sayDoneBaseConfig,
  
  // SoIs 扩展角色
  additionalRoles: {
    clarifier: new Clarifier(),
    sketcher: new Sketcher(),
    locator: new Locator(),
    defaulter: new Defaulter(),
    tracker: new Tracker()
  },
  
  // SoIs 特有 Workflows
  workflows: [
    'sois-clarify-workflow',      // 需求澄清
    'sois-sketch-workflow',       // 草图生成
    'sois-locate-workflow',       // 问题定位
    'sois-propose-workflow'       // 方案建议
  ],
  
  // 运行模式
  mode: 'integrated' | 'standalone'
}
```

### 4.3 独立使用 UniFlow

```typescript
// 纯 UniFlow 使用（不依赖 SayDone/SoIs）
const pureUniFlowConfig: UniFlowConfig = {
  roles: {
    // 仅使用 UniFlow 11 个核心角色
    engine: new UniFlowEngine(),
    inspector: new UniFlowInspector(),
    commander: new UniFlowCommander(),
    dispatcher: new UniFlowDispatcher(),
    advisor: new UniFlowAdvisor(),
    executor: new UniFlowExecutor(),
    guard: new UniFlowGuard(),
    quartermaster: new UniFlowQuartermaster(),
    logistics: new UniFlowLogistics(),
    chronicler: new UniFlowChronicler(),
    signalOfficer: new UniFlowSignalOfficer()
  },
  
  // 自定义 Workflows
  workflows: [
    'my-custom-workflow'
  ],
  
  // 自定义 Skills
  skills: [
    myCustomSkill1,
    myCustomSkill2
  ]
}
```

---

## 5. 数据流

### 5.1 请求处理流程（完整栈）

```
┌─────────────────────────────────────────────────────────────────┐
│  用户: "帮我创建一个用户注册功能"                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  SoIs 层                                                         │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  1. 澄清官分析需求，生成点选选项                            ││
│  │  2. 用户选择/确认选项                                       ││
│  │  3. 草图师生成界面草图                                      ││
│  │  4. 用户确认/调整                                           ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │ 结构化需求
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  SayDone 层                                                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  1. 总导演接收结构化需求                                    ││
│  │  2. AI代理理解并补充细节                                    ││
│  │  3. 守卫员校验数据格式                                      ││
│  │  4. Diff分析员计算变更                                      ││
│  │  5. 记忆官存储上下文                                        ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │ 结构化数据
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  UniFlow 层                                                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  1. 引擎路由消息到各角色                                    ││
│  │  2. 调度员执行 Workflow                                     ││
│  │  3. 执行者调用 Skills                                       ││
│  │  4. 通信官与外部服务交互                                    ││
│  │  5. 守卫全程校验                                            ││
│  │  6. 记录员记录审计日志                                      ││
│  │  7. 督察提供可观测性                                        ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                              │ 执行结果
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  返回用户                                                        │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 消息在层间传递

```typescript
// SoIs → SayDone
interface SoIsToSayDoneMessage {
  type: 'TRANSFORM_REQUEST'
  payload: {
    clarifiedRequirements: ClarifiedRequirement[]
    userSelections: Selection[]
    sketchApproval: boolean
  }
  context: {
    sessionId: string
    loopType: 'A' | 'B'  // 构建循环 or 维护循环
  }
}

// SayDone → UniFlow
interface SayDoneToUniFlowMessage extends UniFlowMessage {
  // 标准 UniFlow 消息格式
  header: {
    from: 'saydone-director'
    to: ['dispatcher']
    // ...
  }
  body: {
    action: 'START_WORKFLOW'
    payload: {
      workflowId: 'saydone-transform-workflow'
      input: StructuredData
    }
  }
}
```

---

## 6. 版本兼容

### 6.1 版本策略

| 产品 | 版本 | UniFlow 兼容 | SayDone 兼容 |
|------|------|-------------|-------------|
| UniFlow | 1.x | - | - |
| SayDone | 1.x | UniFlow 1.x | - |
| SoIs | 1.x | UniFlow 1.x | SayDone 1.x |

### 6.2 升级路径

```
UniFlow 1.0 → 1.1（向后兼容）
  │
  ├── SayDone 1.0 可继续使用
  │
  └── SayDone 1.1（可选升级，使用新特性）
        │
        ├── SoIs 1.0 可继续使用
        │
        └── SoIs 1.1（可选升级）
```

---

## 7. 设计原则

### 7.1 UniFlow 作为基础层的设计原则

1. **通用性**：UniFlow 不包含任何业务特定逻辑
2. **可扩展**：角色接口支持扩展，Workflow 支持自定义
3. **稳定性**：核心接口保持向后兼容
4. **独立性**：可独立使用，不依赖 SayDone/SoIs

### 7.2 SayDone/SoIs 扩展原则

1. **继承优先**：优先继承 UniFlow 角色，避免重复实现
2. **职责单一**：新增角色应有明确单一职责
3. **松耦合**：与 UniFlow 通过标准消息协议交互
4. **可替换**：SayDone 角色实现可被替换

---

## 8. 文档索引

### 8.1 UniFlow 文档
- [UniFlow-接口契约定义.md](./UniFlow-接口契约定义.md)
- [UniFlow-角色图谱.md](./UniFlow-角色图谱.md)
- [UniFlow-消息协议规范.md](./UniFlow-消息协议规范.md)
- [UniFlow-Workflow定义规范.md](./UniFlow-Workflow定义规范.md)
- [UniFlow-MVP定义与实现指南.md](./UniFlow-MVP定义与实现指南.md)
- [UniFlow-风险登记簿.md](./UniFlow-风险登记簿.md)

### 8.2 SayDone 文档
- [SayDone/README.md](./SayDone/README.md)
- [SayDone/SayDone-设计原则.md](./SayDone/SayDone-设计原则.md)

### 8.3 SoIs 文档
- [SoIs/README.md](./SoIs/README.md)
- [SoIs/SoIs-用户流程.md](./SoIs/SoIs-用户流程.md)
