# SVGThing - DeepBase AboutFrame 集成规划

## 项目概况

| 属�?| �?|
|------|-----|
| 项目路径 | `D:\_Progs\02Business\SVGThing` |
| UI 框架 | **VCL** |
| 当前数据�?| `data.db` |
| 目标数据�?| `SVGThingConfig.db` |
| AboutFrame 状�?| **已有** `FrameAboutMe.pas` |

## 当前状态分�?

### 项目结构
```
SVGThing/
├── DeepSVG.dpr             # 主程序入�?
├── data.db                # 配置/数据存储
├── FrameAboutMe.pas       # 现有 About 页面 (~110�?
├── ViewMainSVG.pas        # 主窗�?
└── tests/                 # 测试
```

### 现有 FrameAboutMe.pas 分析

**使用的组�?**
- `TPageControl` + 5 �?`TTabSheet` (Wechat/Alipay/BTC/USDT/AboutMe)
- `TImage` - 显示二维码图�?
- `TLabel` - 显示提示和地址
- `TButton` - 复制 BTC/USDT 地址
- `TFDConnection` + `TFDTable` - FireDAC SQLite 连接

**安全机制:**
- 使用 `uAntiTamperPackage`, `uBasicProtection` (旧版 DeepBase 组件)
- 图片从数据库加载，带 HMAC 签名验证
- 设计时初始化 + Timer 延迟加载

**数据�?**
- 表名: `aboutMeImages` (已是标准�?
- 使用 `GetProjectRootPath` 确定 `data.db` 位置

### 技术特�?
- Tab 自动轮播 (60�?�?
- 机器码显�?(点击复制)
- 迁移旧加密数据方�?(`MigrateEncryptedImages`)

## 目标架构

### 数据库迁�?
| 项目 | 现状 | 目标 |
|------|------|------|
| 数据库名 | `data.db` | `SVGThingConfig.db` |
| 表名 | `aboutMeImages` | `aboutMeImages` (保持不变) |

### 标准 ImageKeys
需验证现有 key 是否与标准匹配：
- `official_gzh` - 官方公众�?
- `wechat` - 微信收款�?
- `alipay` - 支付宝收款码
- `btc` - BTC 钱包地址
- `usdt` - USDT 钱包地址
- `aboutme` - 个人介绍

## 集成方案

### 方案 A: 替换�?DeepBase.VCL.AboutFrame (推荐)

**优点:**
- 统一代码，减少维护负�?
- 自动获取 DeepBase 新功�?
- 标准�?Tab 页和布局

**步骤:**
1. 备份现有 `FrameAboutMe.pas`
2. 创建 `SVGThingConfig.db`，迁�?`aboutMeImages` 数据
3. 在主窗体中替�?`TFrameAboutMe` �?`TUniAboutFrame`（`DeepBase.VCL.AboutFrame`�?
4. 初始�?`DeepBase.AntiTamper`（与 SeedTool 播种参数一致）
5. 测试验证

### 方案 B: 保留 UI，更换数据源

**优点:**
- 保留自定�?UI (如机器码显示、Tab 轮播)
- 最小改�?

**步骤:**
1. 创建 `SVGThingConfig.db`
2. 修改 `FDConnection1` 指向新数据库
3. 移除旧版 AntiTamper 依赖
4. 使用 `TAntiTamperPackage`（`DeepBase.AntiTamper`）加载图�?

### 推荐方案: B (保留 UI)

SVGThing �?AboutFrame 有独特功�?(机器码、轮�?，建议保�?UI 结构，仅更换数据源�?

## 实施步骤

### Phase 1: 数据库重�?
1. 创建 `SVGThingConfig.db`
2. �?`data.db` 迁移 `aboutMeImages` 表数�?
3. 验证数据完整�?

### Phase 2: 代码修改
1. 修改 `GetProjectRootPath` 或数据库路径
2. 更新 `uses`:
   ```delphi
   // 移除
   // uAntiTamperPackage, uBasicProtection
   
   // 添加
   DeepBase.AntiTamper
   // 如需直接复用统一 UI，可再添�?
   // DeepBase.VCL.AboutFrame
   ```
3. 使用 `TAntiTamperPackage.LoadSecureImage`（来�?`DeepBase.AntiTamper`�?

### Phase 3: 清理
1. 移除 `MigrateEncryptedImages` (旧迁移代�?
2. 移除对旧�?DeepBase 组件的引�?
3. 更新项目搜索路径

## SeedTool 工作�?

SeedTool 当前�?GUI 方式使用（未实现 CLI 参数）。如需重新填充数据�?
1. 打开 `DeepBase/Tools/SeedTool/SeedTool.exe`
2. 选择/创建 `SVGThingConfig.db`
3. 初始�?`aboutMeImages` 表（如不存在�?
4. 导入图片并填�?ImageKey / 地址文本（BTC/USDT�? enabled

## 测试清单

- [ ] SVGThingConfig.db 正确创建
- [ ] data.db 中的 aboutMeImages 数据成功迁移
- [ ] DeepBase.AntiTamper 初始化与集成工作正常
- [ ] 图片解密�?sha256/hmac 校验通过
- [ ] Tab 轮播功能正常
- [ ] BTC/USDT 地址复制功能正常
- [ ] 机器码显示正�?
- [ ] CEF 功能不受影响

## 时间估算

| 任务 | 预估时间 |
|------|----------|
| 数据库迁�?| 0.5 小时 |
| 代码修改 | 1 小时 |
| 测试验证 | 0.5 小时 |
| **总计** | **2 小时** |

## 依赖

- `DeepBase.AntiTamper` - 防篡改（解密 + sha256 + hmac�?
- （可选）`DeepBase.VCL.AboutFrame` - 如选择替换为统一 UI
- FireDAC SQLite 驱动
- 现有 `data.db` 数据 (迁移�?

## 备注

SVGThing �?`FrameAboutMe.pas` 已经实现了相当完善的功能，与 TwoKeyRun 类似�?
主要工作是数据库重组和依赖更新，UI 层改动较小�?
