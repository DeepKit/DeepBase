# DeepBase Bug 修复记录

## 2025-12-01

### BUG-001: AES 加密实现不安�?
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Crypto.pas`
- **问题**: TAESCrypto.Encrypt/Decrypt 使用简�?XOR 模拟，不是真正的 AES 加密
- **修复**: 使用 Windows BCrypt API 实现真正�?AES-256-CBC 加密
- **状�?*: �?已修�?

### BUG-002: 随机数生成不安全
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Crypto.pas`
- **问题**: TRandomGenerator.RandomBytes 使用标准 Random() 函数，不适合加密用�?
- **修复**: 使用 BCryptGenRandom 生成密码学安全的随机�?
- **状�?*: �?已修�?

### BUG-003: XOR 加密密钥硬编�?
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Config.pas`
- **问题**: XOR 加密方法存在安全隐患
- **修复**: 添加 {$MESSAGE WARN} 编译器警�?
- **状�?*: �?已修�?

### BUG-004: RegisterSingleton 接口处理错误
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.IoC.pas`
- **问题**: QueryInterface 返回的是接口引用，不能直�?as TObject
- **修复**: 重新设计接口实例存储逻辑，区分接口和类类型处�?
- **状�?*: �?已修�?

### BUG-005: TQueryBuilder 内存泄漏风险
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.ORM.pas`
- **问题**: Query<T> 返回的对象由调用方负责释放，Fluent API 风格易忘�?
- **修复**: 添加 IQueryBuilder<T> 接口，TQueryBuilder<T> 继承 TInterfacedObject 实现自动引用计数
- **状�?*: �?已修�?

### BUG-006: RTTI 类型检查不安全
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Cache.pas`
- **问题**: 取地址后强制转换不安全
- **修复**: 添加 FreeValueIfOwned 方法，使�?PPointer 安全获取对象指针
- **状�?*: �?已修�?

### BUG-007: UniDbSelect 类型不兼�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.DB.DoQry.pas`
- **问题**: TClientDataSet �?TFDQuery 类型不兼�?
- **修复**: 添加 CopyQueryToClientDataSet 辅助函数，正确复制数�?
- **状�?*: �?已修�?

### BUG-008: SQL 语句拼接问题
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Schema.pas`
- **问题**: 多条 INSERT 语句直接拼接，FireDAC TFDScript 解析失败 (`near "cn": syntax error`)
- **修复**: 在所�?SQL 语句之间添加 `#13#10` 换行�?
- **状�?*: �?已修�?

### BUG-009: datetime('now') 参数化问�?
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.i18n.pas`, `DeepBase.Security.pas`, `DeepBase.Manager.pas`
- **问题**: 直接�?SQL 中使�?`datetime('now')`，FireDAC 可能误解析为参数 (`Parameter [TIME] data type is unknown`)
- **修复**: 改用参数化查询，�?Delphi 代码中计算时间后传入 ISO8601 格式字符�?
- **影响方法**: `RecordMissingTranslation`, `AddTranslation`, `SaveSecret`, `MigrateSchemaInternal`
- **状�?*: �?已修�?

### BUG-010: Studio datetime('now') 问题
- **严重程度**: 🟡 �?
- **文件**: `Studio.QueriesFrame.pas`
- **问题**: `SaveCurrent` 中使�?`datetime('now')`
- **修复**: 改用参数化查�?`:UpdatedAt`, `:CreatedAt`
- **状�?*: �?已修�?

### BUG-011: Studio 资源泄漏
- **严重程度**: 🟡 �?
- **文件**: `Studio.QueriesFrame.pas`
- **问题**: `SaveCurrent` �?`Q.Free` 不在 `finally` 块中，异常时内存泄漏
- **修复**: 改为 `try-try-except-end-finally-end` 结构
- **状�?*: �?已修�?

### BUG-012: FMX 应用崩溃
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Theme.pas`
- **问题**: `TStyleManager.ActiveStyle` �?VCL 专用，FMX 应用中返�?nil 导致崩溃
- **修复**: 添加 `{$IFNDEF FMX}` 条件编译，FMX 中使用安全默认值和空操�?
- **影响方法**: `Create`, `ApplyTheme`, `ApplyThemeSync`, `GetAvailableThemes`, `IsThemeAvailable`
- **状�?*: �?已修�?

### BUG-013: Schema INSERT �?datetime('now') 问题
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Schema.pas`
- **问题**: `SQL_TIER0_SCHEMA_INFO_DATA` 中的 `datetime('now')` �?FireDAC 误解�?
- **修复**: 改用 SQLite �?`CURRENT_TIMESTAMP`
- **状�?*: �?已修�?

### BUG-014: VCL-only 模块缺少 FMX 保护
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.SplashScreen.pas`, `DeepBase.TestHelper.pas`
- **问题**: 这些�?VCL 模块如果�?FMX 应用引用会导致编译错�?
- **修复**: 添加 `{$IFDEF FMX} {$MESSAGE FATAL ...} {$ENDIF}` 编译时错误提�?
- **状�?*: �?已修�?

### BUG-015: datetime('now') 全代码库修复
- **严重程度**: 🔴 �?
- **文件**: 多个文件
- **问题**: 多个模块中的 INSERT/UPDATE SQL 使用 `datetime('now', 'localtime')`，FireDAC 误解析为参数
- **修复**: 改用参数化查询和 `FormatDateTime('yyyy-mm-dd"T"hh:nn:ss', Now)`
- **影响文件**:
  - `DeepBase.LLM.pas`: `SaveConfig`, `RecordCall`
  - `DeepBase.LLM.Manager.pas`: `RecordLLMCall`, `UpdateVersionStats`, `SaveCategory`, `SavePrompt`, `SaveVersion`, `SaveMetaPrompt`
  - `DeepBase.MRU.pas`: `AddItem`, `Touch`
  - `Tray.Database.pas`: `UpdateDevLog`, `IncrementCommandUsage`, `AddProjectHiDeepDeepDeepDeepDeepStory`, `SetSetting`
  - `Tray.NotesFrame.pas`: `SaveNote`, `MarkComplete`
  - `Tray.ProjectsFrame.pas`: `AddProject`, `OpenProject`, `OpenInIDE`
  - `Tray.SchedulerFrame.pas`: `RunTask`
- **状�?*: �?已修�?

### BUG-016: SQL 语句拼接分隔符缺�?
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Schema.pas`
- **问题**: `GetTier0SchemaSQL`, `GetTier1SchemaSQL`, `GetTier2SchemaSQL`, `GetFullSchemaSQL` 拼接 SQL 常量时没有换行分�?
- **表现**: `near "cn": syntax error`, `near "CREATE": syntax error` �?
- **修复**: 在所�?SQL 常量之间添加 `#13#10` 换行�?
- **状�?*: �?已修�?

### BUG-017: SQL 脚本�?Pascal 代码不一�?
- **严重程度**: 🔴 �?
- **文件**: `sql/tier1_init.sql`, `sql/tier2_init.sql`
- **问题**: SQL 脚本文件中的表结构与 `DeepBase.Schema.pas` 中的定义不一�?
- **影响**:
  - `Logs` �? 列名 `Level` vs `LogLevel`，类�?INTEGER vs TEXT，缺�?ExceptionClass/ExceptionMessage/UserId �?
  - `MRU` �? 列顺序不同，多余索引
  - `Hotkeys` �? Shortcut 类型 INTEGER vs TEXT，多余列
  - `Themes` �? 不同的列 (PreviewImage/Extra vs AccentColor/CustomCSS)
  - 版本号不一�?(1.0/2.0 vs 0.3)
  - 使用 `datetime('now')` 而不�?`CURRENT_TIMESTAMP`
- **修复**: 统一 SQL 脚本�?Pascal 代码，以 `DeepBase.Schema.pas` 为准
- **状�?*: �?已修�?

### BUG-018~024: Schema 初始化列数不匹配 (2025-12-01)
- **严重程度**: 🔴 �?(阻塞应用启动)
- **报告来源**: DeepDeepDeepDeepDeepInsight 项目集成测试
- **文件**: `DeepBase.Schema.pas`, `DeepBase.Manager.pas`
- **问题描述**:
  - BUG-018: SchemaInfo INSERT 列数不匹�?
  - BUG-019: ProjectInfo INSERT 列数不匹�?
  - BUG-020: Settings 表缺�?ValueType �?
  - BUG-021: Languages INSERT 提供 7 个值但旧表只有 5 �?
  - BUG-022: datetime('now') �?FireDAC 误解�?(已在 BUG-015 修复)
  - BUG-023: MRU 表缺�?DisplayName �?
  - BUG-024: Logs 表缺�?LogTime �?
- **根本原因**: 旧版数据库表结构与新�?Schema 定义不兼�?
- **修复方案**:
  1. `SQL_TIER0_LANGUAGES_DATA` 改用显式列名 INSERT
  2. 新增 `EnsureSchemaColumns` 方法自动检测并添加缺失�?
  3. `CreateSchema` 修改为两次尝试机制：首次失败后调�?`EnsureSchemaColumns` 修复后重�?
  4. 创建 `sql/upgrade_v0_2_to_v0_3.sql` 迁移脚本
- **影响方法**:
  - `TDeepBaseManager.CreateSchema` - 重构为容错机�?
  - `TDeepBaseManager.EnsureSchemaColumns` - 新增自动修复方法
- **状�?*: �?已修�?

### BUG-025: FMX I18n 控件未使用语言订阅
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.FMX.I18nControls.pas`
- **问题**: `TFMXi18nLabel` �?`TFMXi18nButton` 通过直接事件赋值处理语言变更，可能导致多实例冲突且释放时无法正确注销�?
- **修复**: 改为使用 `DeepBase.Manager.DeepBase.I18n.SubscribeLanguageChange` / `UnsubscribeLanguageChange` 订阅语言变更事件，并在销毁时取消订阅�?
- **状�?*: �?已修�?

## 2025-12-02

### BUG-026: XOR 加密遗留实现仍可被调�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Config.pas`
- **问题**: 配置模块仍然通过 XOR + Base64 方式实现 `GetConfigEncrypted`/`SetConfigEncrypted`，即使已经提供了基于 DPAPI 的安全模块�?
- **修复**:
  - 移除 XOR 密钥常量�?EncryptValue/DecryptValue 实现
  - �?`GetConfigEncrypted`/`SetConfigEncrypted` 修改为调�?`DeepBase.Security.SaveSecret` / `LoadSecret`（DPAPI + Secrets 表）
  - 更新配置单元测试，验证密文存储在 Secrets 表中而非 Settings �?
- **状�?*: �?已修�?

### BUG-027: TFDScript 参数解析导致 SQL 语法错误 (2025-12-02/03)
- **严重程度**: 🔴 �?(阻塞应用启动)
- **报告来源**: TheLot, DeepDeepDeepDeepDeepInsight 等多个集成应�?
- **文件**: `DeepBase.Schema.pas`, `DeepBase.Manager.pas`
- **问题描述**:
  - 错误信息: `near "now": syntax error`, `near "cn": syntax error`, `near "SYS": syntax error`
  - 原因 1: CREATE TABLE 中使�?`datetime('now', 'localtime')` DEFAULT 值，FireDAC TFDScript �?`'now'` 误解析为参数 `:now`
  - 原因 2: TFDScript 对多语句 SQL 的解析不稳定，可能与 Unicode 字符有关
  - 原因 3: `SQL_TIER2_PROMPT_VERSIONS_DATA` �?Content 字段包含 `#13#10`，导�?SQL 语句被分�?
- **修复方案** (2025-12-03 最终版):
  1. **完全移除 TFDScript**，改�?TFDQuery 逐条执行 SQL
  2. 新增 `SplitSQLStatements` 函数，按分号分割 SQL，但尊重字符串字面量内的分号
  3. 在执�?SQL 前设�?`Query.ResourceOptions.ParamCreate := False`
  4. �?`SQL_TIER2_PROMPT_VERSIONS_DATA` 中字符串内的 `#13#10` 改为 `|| char(10) ||`
  5. 将所�?`datetime('now')` 改为 `CURRENT_TIMESTAMP`
- **影响方法**:
  - `TDeepBaseManager.CreateSchema` - 重写为逐条执行
  - `TDeepBaseManager.RunMigrationScript` - 重写为逐条执行
  - `TDeepBaseManager.EnsureSchemaColumns` - 添加 ParamCreate := False
- **状�?*: �?已修�?

## 2025-12-05 Delphi 12 编译兼容性修�?

### BUG-028: TRttiEnumerationType 未声�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.HttpServer.pas`, `DeepBase.LogQuery.pas`, `DeepBase.LogAlert.pas`, `DeepBase.LogDashboard.pas`
- **问题**: Delphi 12 �?`TRttiEnumerationType` 需要显式引�?`System.Rtti` 单元
- **修复**: �?uses 子句中添�?`System.Rtti`
- **状�?*: �?已修�?

### BUG-029: Record 方法名与保留字冲�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Metrics.pas`
- **问题**: `Record` �?Delphi 保留字，不能作为方法�?
- **修复**: �?`Record` 方法重命名为 `RecordDuration`
- **状�?*: �?已修�?

### BUG-030: TStringDynArray 未声�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Updater.pas`, `DeepBase.Feedback.pas`, `DeepBase.CloudBackup.pas`
- **问题**: Delphi 12 �?`TStringDynArray` 需要显式引�?`System.Types`
- **修复**: �?uses 子句中添�?`System.Types`
- **状�?*: �?已修�?

### BUG-031: TTask.Run 不兼�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Updater.pas`, `DeepBase.Feedback.pas`
- **问题**: Delphi 12 �?`TTask.Run` 返回值类型变更，需要显式启�?
- **修复**: 改用 `TTask.Create(proc).Start`
- **状�?*: �?已修�?

### BUG-032: TThread.Synchronize(nil, proc) 不兼�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Updater.pas`, `DeepBase.Feedback.pas`, `DeepBase.CloudBackup.pas`
- **问题**: Delphi 12 �?`TThread.Synchronize(nil, proc)` 调用方式不兼�?
- **修复**: 改用 `TThread.Queue(TThread.CurrentThread, proc)` 或直接调�?
- **状�?*: �?已修�?

### BUG-033: TArray.Sort<T> 缺少比较器参�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Updater.pas`
- **问题**: Delphi 12 �?`TArray.Sort<T>` 需要显式传入比较器
- **修复**: 添加 `TComparer<T>.Default` 参数，并引用 `Generics.Defaults`
- **状�?*: �?已修�?

### BUG-034: TLogDashboard 类型不匹�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.LogDashboard.pas`
- **问题**: 使用了未定义�?`TKeyCount` 类型�?`Point.Count` 属�?
- **修复**: 改用 `TCountResult` 类型�?`Point.Value` 属�?
- **状�?*: �?已修�?

### BUG-035: TZCompressionStream.Create 不兼�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.CloudBackup.pas`
- **问题**: Delphi 12 �?`TZCompressionStream.Create` 参数不兼�?
- **修复**: 改用 `ZCompressStream` 函数
- **状�?*: �?已修�?

### BUG-036: 记录属性直接赋值不兼容
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.LogDashboard.pas`
- **问题**: 对嵌套记录属性直接赋�?(`Widget.Config.Title :=`) �?Delphi 12 中不兼容
- **修复**: 使用局部变量更新记录后再赋�?
- **状�?*: �?已修�?

### BUG-037: overload 指令缺失
- **严重程度**: 🟢 �?
- **文件**: `DeepBase.LogQuery.pas`
- **问题**: 重载方法缺少 `overload` 指令
- **修复**: 添加 `overload` 指令
- **状�?*: �?已修�?

### BUG-038: TTemplateEngine 属性访问器未实�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Template.pas`
- **问题**: `GetHtmlEscape/SetHtmlEscape/GetStrictMode/SetStrictMode` 方法�?interface 中声明但未实�?
- **修复**: 添加访问器实现，委托�?FRenderer 对应属�?
- **状�?*: �?已修�?

### BUG-039: TTemplateEngine 头部注释语法错误
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Template.pas`
- **问题**: 文件头使�?`{*...*}` 格式，`{` �?`}` �?Delphi 12 中被误认为字符常�?
- **修复**: 改用 `(*...*)`
- **状�?*: �?已修�?

### BUG-040: TTemplateRenderer 内联变量声明不兼�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Template.pas`
- **问题**: `TryStrToInt(LArg, var LInt: Integer)` 内联变量声明�?Delphi 12 中不兼容
- **修复**: 先声明变量再调用 `TryStrToInt(LArg, LInt)`
- **状�?*: �?已修�?

### BUG-041: TCloudConfigSync.DoProgress TThread.Queue 参数
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.CloudSync.pas`
- **问题**: `TThread.Queue(TThread.CurrentThread, proc)` �?Delphi 12 中不兼容
- **修复**: 改用 `TThread.Queue(nil, proc)` 并先将变量复制到局部变�?
- **状�?*: �?已修�?

### BUG-042: TCloudConfigSync 记录属性直接赋�?
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.CloudSync.pas`
- **问题**: `LItem.LocalVersion.Version := ...` �?Delphi 12 中不能直接赋值给记录属性的子字�?
- **修复**: 使用局部变�?`LVersion := LItem.LocalVersion; LVersion.Version := ...; LItem.LocalVersion := LVersion`
- **状�?*: �?已修�?

### BUG-043: THTTPClient.Post/Put 空体参数
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.CloudSync.pas`
- **问题**: `FHttpClient.Post(LURL, nil, LHeaders)` �?Delphi 12 中不兼容
- **修复**: 改用 `FHttpClient.Post(LURL, TStringStream.Create('', TEncoding.UTF8), nil, LHeaders)`
- **状�?*: �?已修�?

---

## 代码质量检查结�?(2025-12-01)

### QA-001: �?Windows 平台加密不安�?
- **严重程度**: 🔴 �?
- **文件**: `DeepBase.Security.pas`
- **问题**: �?Windows 平台之前使用简�?UTF-8 编码作为 DPAPI 回退
- **修复**:
  - 新增 `DeepBase.Crypto.OpenSSL.pas`：动态加�?libcrypto，实�?AES-256-GCM + PBKDF2
  - 修改 `DeepBase.Security.pas` �?Windows 分支，调�?OpenSSL 后端，使�?UBS2 格式
- **状�?*: �?已修�?(2025-12-02)

### QA-002: 泛型异常类型
- **严重程度**: 🟢 �?
- **文件**: 多个文件
- **问题**: 使用 `raise Exception.Create(...)` 而非特定异常类型
- **修复**:
  - `DeepBase.DB.ConnectionPool.pas`: 新增 `EConnectionPoolException` / `EConnectionPoolTimeout`
  - `DeepBase.LLM.pas`: 新增 `ELLMException`
  - `DeepBase.Memory.pas`: 新增 `EMemoryException` / `EMemoryPoolException` / `EMemoryCacheException`
  - `DeepBase.Cache.pas`: 新增 `ECacheException`
- **状�?*: �?已修�?(2025-12-02, OPT-002)

### QA-003: �?except �?
- **严重程度**: 🟢 �?
- **文件**: 多个核心文件
- **统计**: �?40+ 处空 except �?
- **分析**:
  - �?大多数是有意设计的容错机�?(Destructor 清理、连接重试、回退逻辑)
  - �?DEBUG 模式下有 `OutputDebugString` 输出
  - ⚠️ 少数可能隐藏真正错误
- **主要位置**:
  - `DeepBase.Manager.pas`: 12+ �?(多为初始�?清理代码)
  - `DeepBase.Logging.pas`: 8 �?(写入失败回退)
  - `DeepBase.DB.DoQry.pas`: 6 �?(连接/查询失败处理)
- **状�?*: 🟢 可接�?(设计意图明确，不影响功能)

### QA-004: TODO 标记待处�?
- **严重程度**: 🟢 �?
- **位置**:
  - `DeepBase.Manager.pas`: INI 格式解析 TODO 已改为明确说明“当前不支持 INI 格式�?
  - `DeepBase.Config.pas`: 日志集成 TODO 已移�?
  - `DeepBase.FMX.Platform.pas`: 平台特定功能
  - `DeepBase.CloudSync.pas`: 云同步功�?
  - `DeepBase.CLI.SSH.pas`: SSH 功能
- **状�?*: �?核心 TODO 已清�?(2025-12-02, OPT-004)

### QA-005: XOR 加密遗留代码
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Config.pas`
- **问题**: 早期版本使用 XOR + Base64 作为加密实现，安全性不�?
- **当前状�?*: 已在 2025-12-02 完全移除，改为委�?DeepBase.Security（DPAPI + Secrets 表），详�?BUG-026
- **建议**: 使用 `DeepBase.Security.SaveSecret/LoadSecret` �?`GetConfigEncrypted/SetConfigEncrypted` 封装存取敏感配置
- **状�?*: �?已解�?

### BUG-044 ~ BUG-053: Delphi 12 剩余模块兼容性修�?(2025-12-05)
- **严重程度**: 🟡 �?
- **文件**: `DeepBase.Diff.pas`, `DeepBase.FileWatcher.pas`, `DeepBase.IoC.pas`, `DeepBase.StateMachine.pas`, `DeepBase.Graph.pas`, `DeepBase.Net.pas`, `DeepBase.Serialization.pas`
- **修复内容**:

#### BUG-044: Diff.pas 泛型约束
- **问题**: `TDiffComparer<T>` 泛型约束 `T must be class type`
- **修复**: 增加 `T: class` 约束，调整记录属性赋�?

#### BUG-045: FileWatcher.pas 缺少单元引用
- **问题**: 缺少 `Vcl.ExtCtrls` (TTimer), `PFILE_NOTIFY_INFORMATION` 未声�?
- **修复**: 添加 `Vcl.ExtCtrls`，将 `PFILE_NOTIFY_INFORMATION` 改为 `^TFileNotifyInformation`

#### BUG-046: IoC.pas 泛型类型约束
- **问题**: `TypeInfo(T).Kind` 在泛型方法中不能使用 (E2018)
- **修复**: 使用 PTypeInfo 局部变量存储，改用 TValue.AsType<T>

#### BUG-047: StateMachine.pas 本地过程
- **问题**: NI19024 - 泛型方法中的本地过程不支�?
- **修复**: �?`StateToString`/`TriggerToString` 提取为私有类方法

#### BUG-048 ~ BUG-051: Graph.pas 迭代重构
- **问题**: NI19024 - 多个方法使用本地过程或匿名方法回�?
- **修复**: 
  - `TTree.Traverse`, `ToArray`, `NodeCount`, `Find` 改为迭代实现
  - `TGraph.FindCycle`, `IsConnected`, `ConnectedComponents` 改为迭代实现
  - `StronglyConnectedComponents` FillOrder/DFSUtil 改为迭代 DFS
  - `IsReachable`, `ReachableFrom` 直接使用 BFS 而非回调

#### BUG-052: Net.pas Indy DNS API 变更
- **问题**: `QueryTimeout`, `QueryRecords` �?Delphi 12 Indy 中不存在
- **修复**:
  - `QueryTimeout` �?`WaitingTime`
  - `QueryRecords` �?`QueryType`
  - `qtCNAME` �?`qtName`, `TCNAMERecord` �?`TCNRecord`

#### BUG-053: Serialization.pas 接口泛型方法
- **问题**: E2535 - 接口不能有参数化（泛型）方法
- **修复**:
  - �?`ISerializer` 接口中移除泛型方�?(`Serialize<T>`, `Deserialize<T>` �?
  - 泛型方法仅在 `TBaseSerializer` 类中保留
  - 使用局部变量存�?`TValue.Kind` �?`TValue.AsObject` 避免 E2036
  - `TSerializer` 静态方法改为创建具体序列化器实�?

- **状�?*: �?已修�?
- **结果**: 78/78 (100%) Core 模块编译成功

---

## 代码质量总结

**整体评价**: 良好 �?

**优点**:
- 核心模块有完整的 XMLDoc 文档
- 异常处理模式一�?(DEBUG 输出 + 回退机制)
- `DeepBase.Reflection.pas` 等模块正确使用特定异常类�?
- 线程安全设计合理 (锁、事件、原子操�?

**建议改进** (2025-12-02 已完�?:
1. �?为连接池、LLM、Memory、Cache 等模块添加了特定异常类型 (OPT-002)
2. �?实现了跨平台加密方案：OpenSSL AES-256-GCM (OPT-001)
3. �?清理了核心模块中过时�?TODO 注释 (OPT-004)

---

## 2025-12-07 DeepBase Core Bug 修复

### BUG-041: Scheduler 并发控制计数竞争 �?
- **严重程度**: 🔴 �?
- **文件**: `Core/DeepBase.Scheduler.pas`
- **问题**: `ExecuteTask` 中对 `FRunningCount`, `FStats.RunningTasks`, `FStats.PendingTasks` 的修改未加锁保护，多线程并发时产生数据竞�?
- **修复**: �?`ExecuteTask` 开头使�?`FLock.Enter/Leave` 包裹计数器更�?
- **状�?*: �?已修�?

### BUG-042: MRU 无法清理 UNC 路径无效�?�?
- **严重程度**: 🟢 �?
- **文件**: `Core/DeepBase.MRU.pas`
- **问题**: `RemoveInvalidMRU` 仅检�?`/` 开头和盘符路径，无法识�?`\\server\share` 格式�?UNC 路径
- **修复**: 扩展路径检测条件，同时接受 `\` �?`/` 前缀
- **状�?*: �?已修�?

### BUG-043: EventBus 泛型 Subscribe<T> Filter 参数被忽�?�?
- **严重程度**: 🟡 �?
- **文件**: `Core/DeepBase.EventBus.pas`
- **问题**: 泛型 `Subscribe<T>` 重载�?Filter 参数被注释掉�?due to compiler limitations"），实际设为 nil
- **修复**: 将泛�?`TEventFilter<T>` 包装为非泛型 `TFunc<TValue, Boolean>`
- **状�?*: �?已修�?

### BUG-044: UniDbSetCacheTTL 空锁对象 �?
- **严重程度**: 🟡 �?
- **文件**: `Persistence/DeepBase.DB.DoQry.pas`
- **问题**: �?`UniDbInit` 之前调用 `UniDbSetCacheTTL` 会对 nil �?`GQueryCacheLock` 加锁，导�?Access Violation
- **修复**: 添加 `Assigned(GQueryCacheLock)` 检查，未初始化时直接更新变�?
- **状�?*: �?已修�?

### BUG-045: TokenBucket 除零错误 �?
- **严重程度**: 🟡 �?
- **文件**: `Core/DeepBase.RateLimiter.pas`
- **问题**: `TTokenBucketLimiter.Create` 未验证参数，传入 `ACapacity <= 0` �?`ARefillRate <= 0` 会导致后续除零错�?
- **修复**: 在构造函数中添加参数验证，非法参数抛�?`EArgumentException`
- **状�?*: �?已修�?

### BUG-046: WaitForCompletion 只等待挂起队�?�?
- **严重程度**: 🟡 �?
- **文件**: `Core/DeepBase.WorkerQueue.pas`
- **问题**: `WaitForCompletion` 仅检�?`GetPendingCount > 0`，当所有作业被取出进入 Running 状态后会提前返�?
- **修复**: 改用 `GetStats` 同时检�?`PendingJobs` �?`RunningJobs`，确保两者均�?0 才返�?
- **状�?*: �?已修�?

### BUG-047: 统计累加非原�?�?
- **严重程度**: 🟢 �?
- **文件**: `Core/DeepBase.WorkerQueue.pas`
- **问题**: `FTotalProcessingTime` 累加使用 `AtomicExchange(old + delta)`，多线程并发时读取旧值会互相覆盖
- **修复**: 使用 `TInterlocked.Add` 进行真正的原子加�?
- **状�?*: �?已修�?

### BUG-048: GetAuditLog Action 映射错误 �?
- **严重程度**: 🟡 �?
- **文件**: `Core/DeepBase.Authorization.pas`
- **问题**: `GetAuditLog` 虽定义了 `ACTION_MAP` 数组，但实际始终返回 `aaResourceAccessed`
- **修复**: 添加 `MapActionString` 内部函数，根据数据库 `action` 字段映射回正确的 `TAuditAction` 枚举
- **状�?*: �?已修�?

### BUG-049: HttpServer 静态文件二进制损坏 �?
- **严重程度**: 🟡 �?
- **文件**: `Features/DeepBase.HttpServer.pas`
- **问题**: `TStaticFileMiddleware` 使用 `TFile.ReadAllText` 读取所有文件，二进制文件（图片、PDF 等）�?UTF-8 编码后损�?
- **修复**: 
  1. �?`THttpResponse` 添加 `Bytes` 方法、`FBodyBytes`、`FIsBinary` 字段
  2. `TStaticFileMiddleware` 改用 `TFile.ReadAllBytes`
  3. 响应发送时检�?`IsBinary` 并使�?`ContentStream` 传输
- **状�?*: �?已修�?


---

## 2026-05-11 Governance 接入轮 Bug 修复

> 以下记录来自 `docs/bugfix.md`，Governance (OCGS) 下游接入期间发现并修复的问题。

### BUG-G001: 盘古误判 Governance 类型不存在
- **严重程度**: 🟡 中
- **现象**: Lifecycle.pas / Registration.pas 引用 TObserveGateResolver / TConfigRegistrar / TEvidenceRecorder 等类型，编译报"找不到"，盘古误判为幻觉类型并降级
- **根因**: 搜索路径没覆盖到新增文件，不是类型不存在
- **修复**: 还原完整版本，确认所有类型真实存在于 `DeepBase/Governance/` 目录
- **状态**: ✅ 已修复

### BUG-G002: 违反 DeepBase 配置铁律
- **严重程度**: 🔴 高
- **现象**: 给每个接入项目创建了 `config/governance/*.json`
- **根因**: 未确认配置铁律（唯一外部配置文件 = root.txt）
- **修复**: 删除所有 JSON 配置，改为代码回调注册（TGovernanceSetupProc / TGovernanceConfigSetupProc）
- **状态**: ✅ 已修复

### BUG-G003: DeepFlow 误放进 DeepBase/docs
- **严重程度**: 🟢 低
- **修复**: 移出到独立目录
- **状态**: ✅ 已修复

### BUG-G004: ObserveGateResolver 覆盖 State 时不还原 AvailableActions
- **严重程度**: 🔴 高
- **现象**: observe 模式下条件失败的 action 报 "No action resolved" 而非成功放行
- **根因**: GateResolver.Resolve 只在 gsOpen 时填充 AvailableActions，ObserveGateResolver 覆盖 State 为 gsOpen 但未恢复 AvailableActions
- **修复**: GateResolver 无论 State 如何都填充 AvailableActions
- **状态**: ✅ 已修复并验证
