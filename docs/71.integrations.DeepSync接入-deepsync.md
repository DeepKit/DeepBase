# DeepSync - DeepBase AboutFrame 集成规划

## 项目概况

| 属�?| �?|
|------|-----|
| 项目路径 | `D:\_Progs\02Business\DeepSync` |
| UI 框架 | **FMX (FireMonkey)** |
| 当前数据�?| `SyncLocal.db` |
| 目标数据�?| `DeepSyncConfig.db` |
| AboutFrame 状�?| **�?* (需新建) |

## 当前状态分�?

### 项目结构
```
DeepSync/
├── DeepSync.dpr           # 主程序入�?
├── SyncLocal.db           # 同步数据�?
├── src/
�?  ├── core/              # 核心逻辑 (P2P, Syncthing, ScreenShare)
�?  ├── forms/             # 窗体 (uMainForm, uSettingsForm �?
�?  └── frames/            # Frame (uTaskCardFrame, uSidePanelFrame)
└── tests/                 # 测试
```

### 主窗�?(uMainForm.pas)
- 使用 FMX 框架
- 已使�?DeepBase: `DeepBase.FormState`, `DeepBase.i18n`, `DeepBase.Manager`
- 侧边栏导�? `TSidePanelFrame`
- 支持系统托盘 (Windows)

### 数据库现�?
- `SyncLocal.db`: 用于存储同步任务、设备信�?
- 无独立的配置数据�?
- �?`aboutMeImages` �?

## 目标架构

### 新建数据�?
创建 `DeepSyncConfig.db` 用于存放应用配置，与业务数据库分离：

```sql
-- aboutMeImages 表（�?DeepBase.AntiTamper / SeedTool 实现为准�?
CREATE TABLE IF NOT EXISTS aboutMeImages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  image_key TEXT NOT NULL UNIQUE,
  image_data BLOB NOT NULL,      -- AES-256 加密的图像数据（SeedTool 写入�?
  address_text TEXT,
  description TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,
  sha256_hash TEXT NOT NULL,
  hmac_sha256 TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 标准 ImageKeys
| Key | 描述 |
|-----|------|
| `official_gzh` | 官方公众�?|
| `wechat` | 微信收款�?|
| `alipay` | 支付宝收款码 |
| `btc` | BTC 钱包地址二维�?|
| `usdt` | USDT 钱包地址二维�?|
| `aboutme` | 个人介绍/名片 |

## 集成方案

### 挑战：VCL vs FMX
`DeepBase.VCL.AboutFrame` �?VCL 组件，�?DeepSync 使用 FMX�?

当前仓库已有 `FMX/DeepBase.FMX.AboutFrame.pas`（`TFMXAboutFrame`），�?*尚需�?SeedTool/DeepBase.AntiTamper 的数据协议对�?*后再用于 DeepSync/Stocks�?
- 字段名应�?`sha256_hash` + `hmac_sha256`（不�?`hmac_signature`�?
- `image_data` �?**AES-256 加密**�?BLOB（需要解密后再加载图像）
- 校验应以 `DeepBase.AntiTamper` 的逻辑为准（SHA-256 + HMAC�?

**方案 A: 使用并对齐现�?DeepBase.FMX.AboutFrame** (推荐)
- 对齐 `FMX/DeepBase.FMX.AboutFrame.pas` 的字段名/解密/校验逻辑
- DeepSync 直接复用�?FMX Frame

**方案 B: 混合模式**
- 使用 VCL 窗体承载 AboutFrame
- �?FMX 主程序调�?
- 不推�?- 增加复杂�?

### 推荐实施步骤

#### Phase 1: 对齐现有 FMX AboutFrame
1. 对齐 `FMX/DeepBase.FMX.AboutFrame.pas` �?`Features/DeepBase.AntiTamper.pas` / SeedTool 的字段与解密/校验逻辑
2. 验证可正确读�?SeedTool 播种的数据（解密 + sha256 + hmac�?
3. �?DeepSync 内集成显�?

#### Phase 2: 数据库配�?
1. 创建 `DeepSyncConfig.db`
2. 添加 `aboutMeImages` �?
3. 使用 SeedTool 填充 6 个标准图�?

#### Phase 3: 集成�?DeepSync
1. �?`uSidePanelFrame` 添加 "关于" 导航�?
2. �?`uMainForm` 添加 AboutFrame 容器区域
3. 导航点击时显�?AboutFrame

## SeedTool 工作�?

SeedTool 当前�?GUI 方式使用（未实现 CLI 参数）。建议：
1. 打开 `DeepBase/Tools/SeedTool/SeedTool.exe`
2. 选择/创建 `DeepSyncConfig.db`
3. 初始�?`aboutMeImages` 表（如不存在�?
4. 导入 6 张图片并填写 ImageKey / 地址文本（BTC/USDT�? enabled

## 测试清单

- [ ] DeepSyncConfig.db 正确创建
- [ ] aboutMeImages 表包�?6 条记�?
- [ ] 图片解密�?sha256/hmac 校验通过（由 DeepBase.AntiTamper 负责�?
- [ ] 侧边栏导航正确显�?"关于"
- [ ] AboutFrame 正确加载并显示图�?
- [ ] Tab 切换功能正常
- [ ] 地址复制功能正常 (BTC/USDT)

## 时间估算

| 任务 | 预估时间 |
|------|----------|
| 创建 FMX �?AboutFrame | 2-3 小时 |
| 数据库配�?| 0.5 小时 |
| SeedTool 数据填充 | 0.5 小时 |
| 集成�?DeepSync | 1 小时 |
| 测试验证 | 0.5 小时 |
| **总计** | **4.5-5.5 小时** |

## 依赖

- `DeepBase.AntiTamper` - 防篡改（解密 + sha256 + hmac�?
- `DeepBase.FMX.AboutFrame` - FMX �?AboutFrame（对齐后复用�?
- SeedTool - 图片数据填充（GUI�?
- FireDAC SQLite
- FMX 框架
