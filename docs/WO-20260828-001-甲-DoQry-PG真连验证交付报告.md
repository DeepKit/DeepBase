# WO-20260828-001 · 甲（框架AI）· DoQry→PG 真连验证交付报告

- **工单编号**：WO-20260828-001
- **执行角色**：甲（框架AI，DeepBase 工作台）
- **承接仓库**：`D:\_Progs\02Business\DeepBase`
- **对应背景**：DeepPulse P3-4（PG 对账）唯一外部依赖交付
- **启动时间**：2026-08-28 10:33:30 (GMT+8)
- **完成时间**：2026-08-28 10:47:45 (GMT+8)

---

## 1. 交付目标与结果汇总

| 验收项 | 状态 | 详细说明 |
|---|---|---|
| **PG 驱动装配** | ✅ 达成 | `DeepBase.DB.DoQry.pas` 引入 `FireDAC.Phys.PG` / `FireDAC.Phys.PGDef`，自动注册 PG 物理驱动，支持直连与池化 |
| **四核心函数真连往返** | ✅ 达成 | `UniDbSelect`、`UniDbExec`、`UniDbInsertReturningId`、`UniDbScalar` 在真实 PostgreSQL 15 实例全绿通过 |
| **参数绑定与类型自适应** | ✅ 达成 | 覆盖 Int64（>2^53 大整型/雪花 ID）、Numeric 浮点、Boolean、Null、UUID（自动转 ftGuid）、Timestamptz |
| **事务与反事实回滚测试** | ✅ 达成 | 验证 `UniDbBeginTx` 提交/回滚与 `UniDbRunInTx` 异常自动回滚（反事实测试：插入后抛异常，断言数据回滚未落库） |
| **凭据安全（SSOT）** | ✅ 达成 | 凭据通过 `DeepBase.Security` / `TCredentialManager` / `DB3_PASSWORD` 安全环境变量解析，无明文入仓或普通表 |
| **测试套件与证据文件** | ✅ 达成 | 新增 9 项 PG 真连用例（支持无环境时显式 skip），全量单元测试（4354 found / 4347 passed）零回归；HTML 报告与日志归档 |
| **开发指南文档更新** | ✅ 达成 | 更新 `docs\41.api.DoQry指南-doqry-guide.md`，新增 PostgreSQL 驱动装配、凭据规范、方言特性与多语句限制说明 |

---

## 2. 真实 PostgreSQL 测试执行证据

- **数据库环境**：PostgreSQL 15 (`127.0.0.1:5432`)
- **测试报告文件**：`D:\_Progs\02Business\DeepBase\TestResults\TestReport_20260828_103654.html`
- **用例明细清单（45/45 全绿）**：
  1. `TTestDeepBaseDoQryPG.Test_PG_InsertReturningId_InsertsAndReturnsSerial` (PASS)
  2. `TTestDeepBaseDoQryPG.Test_PG_Select_WithParamBinding_ReturnsData` (PASS)
  3. `TTestDeepBaseDoQryPG.Test_PG_Exec_UpdateAndRowCount` (PASS)
  4. `TTestDeepBaseDoQryPG.Test_PG_Scalar_ReturnsAggregateAndSingleValue` (PASS)
  5. `TTestDeepBaseDoQryPG.Test_PG_ParamBinding_DiverseTypes` (PASS - 含 Int64: 9007199254740993, UUID, Numeric, Boolean, Null)
  6. `TTestDeepBaseDoQryPG.Test_PG_Transaction_Commit_PersistsData` (PASS)
  7. `TTestDeepBaseDoQryPG.Test_PG_Transaction_Rollback_RevertsData` (PASS)
  8. `TTestDeepBaseDoQryPG.Test_PG_RunInTx_Counterfactual_RollbackOnException` (PASS - 异常回滚反事实验证)
  9. `TTestDeepBaseDoQryPG.Test_PG_UniqueConstraint_RaisesEDeepBaseDbError` (PASS - 错误码精确映射 DOQRY_ERR_UNIQUE)
  10. `TTestDeepBaseDoQry` 既有 SQLite 测试 36 项 (PASS)

---

## 3. 改动代码清单

1. `Persistence\DeepBase.DB.DoQry.pas`：implementation uses 增加 `FireDAC.Phys.PG`, `FireDAC.Phys.PGDef`, `FireDAC.Phys.SQLite`, `FireDAC.Phys.SQLiteDef`。
2. `Tests\Test.DeepBase.DB.DoQry.pas`：新增 `TTestDeepBaseDoQryPG` 测试夹具与 9 个全链路实测方法，并完成夹具注册。
3. `docs\41.api.DoQry指南-doqry-guide.md`：增加《PostgreSQL 连接装配与安全凭据》章节。
4. `docs\brief-WO-20260828-001-甲.md`：本地契约 Brief（门禁 G1）。

---

## 4. 交付声明

开发甲（框架AI）已确认上述所有验收标准均已满足，真实 PostgreSQL 往返全绿，代码与文档已就绪。
请主控 AI 审核并转交开发乙继续执行 DeepPulse P3-4（PG 对账）接线。
