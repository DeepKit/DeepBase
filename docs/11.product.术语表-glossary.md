# DeepBase / uniFlow 术语�?
> 版本: 1.0.0  
> 更新日期: 2026-05-04  
> 目的: 统一 DeepBase 核心术语�?uniFlow 术语，避�?API 和文档混�?
---

## 使用规则

- 代码、文档、日志、配置项统一使用本表“规范术语”�?- 历史别名仅用于迁移说明，不用于新增代码�?- 新术语先登记本文件，再进入实现与对外文档�?
---

## A. DeepBase 核心术语

| 规范术语 | 定义 | 禁用同义�?| 关联模块 |
|---|---|---|---|
| ConfigDB (DB1) | DeepBase 配置数据库（每个应用一份） | MainDB, MetaDB | `DeepBase.Manager` |
| DB2 / DB3 | 业务数据库（本地/远程�?| ConfigDB2 | `DeepBase.DB.DoQry` |
| SchemaInfo.SchemaVersion | ConfigDB 架构版本号（统一�?`1.0.0`�?| DBVersion, v0.3 | Schema |
| UniDbInit | DoQry 初始化入�?| DoQryInit | `DeepBase.DB.DoQry` |
| TUniQueryContext | DoQry 执行上下�?| TDoQryContext | `DeepBase.DB.DoQry` |
| UniDbSelect / UniDbExec / UniDbScalar | 统一查询/写入/标量 API | DoQryExecSelect | `DeepBase.DB.DoQry` |
| UniDbInsertReturningId | 插入并返回主�?ID | InsertAndGetId | `DeepBase.DB.DoQry` |
| UniDbRunInTx | 事务包装（支持嵌�?SAVEPOINT�?| RunInTransaction | `DeepBase.DB.DoQry` |
| Settings | 通用配置�?| Configs, AppSettings | `DeepBase.Config` |
| FormStates | 窗体状态表 | WindowStates | `DeepBase.FormState` |
| I18nTexts | 翻译文本�?| Translations | `DeepBase.i18n` |
| MRU.ItemKey | 最近使用项标识字段 | ItemPath | `DeepBase.MRU` |
| Logger() | 全局日志入口 | Log(), LogUtil | `DeepBase.Logging` |
| DiagnoseAll / AutoFix | Schema 诊断与修复入�?| CheckDB | `DeepBase.Diagnose` |
| TDeepBaseLLM | 直接 LLM 调用 API | AIClient | `DeepBase.LLM` |
| TLLMManager | Prompt/版本/元提示词管理 API | PromptManagerV1 | `DeepBase.LLM.Manager` |
| LLM() Facade | Features 便捷门面 API | DefaultLLM | `Features/DeepBase.LLM.Service` |
| LLMConfig.ApiKeyRef | LLM 凭据引用字段（`credman:`�?| ApiKey plaintext | LLM Schema |
| LLMApiKeys | LLM 凭据引用表（不存明文�?| ApiKeyStore | LLM Schema |
| aboutMeImages | About/打赏/公众号图像表 | AboutImagesLegacy | Schema Tier2 |

---

## B. uniFlow 术语

| 规范术语 | 定义 | 禁用同义�?| 备注 |
|---|---|---|---|
| FlowDefinition | 流程定义 | Workflow, ProcessDef | `TWorkflowDefinition` |
| FlowInstance | 流程实例 | Process, Job, Task | `TFlowInstance` |
| Step | 流程步骤 | Stage, Phase, Node | `{Verb}{Noun}` |
| TUniFlowEvent | 状态变化事�?| Activity, ActionLog | 事件溯源 |
| Snapshot | 实例快照 | StateBackup | `TSnapshot` |
| Source | 事件来源 | Origin, Sender | `SoIs/SayDone/...` |
| Payload | 事件载荷 JSON | Data, Body | JSON 对象 |
| IEventStore | 事件存储接口 | EventRepo | `Append/Read...` |
| IPluginActionExecutor | 插件执行接口 | SkillExecutor | `Execute/ValidateInput` |

---

## 命名约束

1. DeepBase 文档必须使用 `UniDb*` 前缀，不再新�?`DoQry*` 命名�?2. MRU 字段统一�?`ItemKey`，历�?`ItemPath` 仅用于迁移脚本说明�?3. LLM API 说明必须明确三套入口的使用场景和迁移路径�?