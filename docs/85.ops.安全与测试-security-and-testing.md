# 04. 安全与测试指�?

> 本文档整合了安全防护策略和测试自动化规范�?

---

## 第一部分：安全防�?

### 1. 敏感配置与凭据存�?
LLM API Key �?`TDeepBaseLLM.SaveConfig` 写入 Windows Credential Manager，数据库中的 `LLMConfig.ApiKeyRef` 只保�?`credman:<target>` 引用�?
```pascal
Config.Init;
Config.Name := 'Default';
Config.ApiKey := 'sk-123456';
LLM.SaveConfig(Config);

var Key := LLM.GetConfig('Default').ApiKey;
```

通用 Secret 仍使�?`DeepBase.Security.SaveSecret/LoadSecret`。旧库中直接写在 `LLMConfig.ApiKeyRef` �?`LLMConfiguration.ApiKey` 的明�?key 可运行：

```powershell
.\Scripts\migrate_llm_credentials.ps1 -DatabasePath .\config.db
```

**优势**：`config.db` 不再保存真实 LLM API Key；Credential Manager 凭据绑定当前 Windows 用户/机器�?
### 2. 日志脱敏

```pascal
// 自动脱敏：password, key, token 等关键词自动替换�?******
// 手动脱敏�?
LogInfo('User login: ' + Sanitize(UserName));
```

**默认关键�?*：`password`, `secret`, `apikey`, `token`, `bearer`, `authorization`

### 3. 凭据迁移（换�?换用户）

1. 旧环境：运行 Studio "安全迁移向导"，解密后暂存内存
2. 新环境：逐项通过 `TDeepBaseLLM.SaveConfig` �?`DeepBase.Security.SaveSecret` 写回
3. 完成后清空内存缓�?
### 4. License 验证

```pascal
// 验证流程
// 1. 用户输入激活码
// 2. 本地检查格式、签�?
// 3. 在线验证状�?
// 4. 本地缓存（加密）
```

**机器指纹**：`HMAC_SHA256(CPU_ID + BIOS_ID + MAC)`

### 5. 防篡�?

- **Config 完整�?*：启动时校验 `Settings` �?Hash
- **EXE 签名**：使用代码签名证�?
- **运行时校�?*：`TBasicProtection` 新数据使�?AES-256-GCM 认证加密（`UBG1` 格式），�?AES-256-CBC 密文保留只读解密兼容；完整性校验使�?HMAC-SHA256�?
### 6. 安全编码规范

```pascal
// �?SQL 注入
Query.SQL.Text := 'SELECT * FROM Users WHERE Name = ' + Edit1.Text;

// �?参数�?
Query.SQL.Text := 'SELECT * FROM Users WHERE Name = :Name';
Query.ParamByName('Name').AsString := Edit1.Text;
```

**SQL 模板校验（Studio�?*�?
- 禁止�?WHERE �?UPDATE/DELETE
- 强制参数�?
- SELECT 默认 LIMIT

---

## 第二部分：测试自动化

### 1. 测试框架

使用 **DUnitX**�?
- Attribute 风格（`[TestFixture]`, `[Test]`�?
- NUnit XML 输出（CI/CD 友好�?
- 内置内存泄漏检�?

### 2. 目录结构

```
Tests/
├── DeepBaseTests.dpr           # 主测试项�?
├── Test.DeepBase.Config.pas    # 单元测试
├── Test.DeepBase.i18n.pas
├── Integration/               # 集成测试
└── Snapshots/                 # GUI 快照
```

### 3. 测试模板

```pascal
unit Test.DeepBase.Config;

interface

uses
  DUnitX.TestFramework,
  DeepBase.Manager,
  DeepBase.Persistence.Manager.FireDAC;

type
  [TestFixture]
  TTestConfig = class
  public
    [Setup]
    procedure Setup;
    
    [TearDown]
    procedure TearDown;
    
    [Test]
    procedure Test_GetConfig_WithDefault_ReturnsDefault;
  end;

implementation

procedure TTestConfig.Setup;
begin
  DeepBase.InitializeWithDBOrRaise(':memory:');  // 内存数据库隔�?
end;

procedure TTestConfig.TearDown;
begin
  DeepBase.Finalize;
end;

procedure TTestConfig.Test_GetConfig_WithDefault_ReturnsDefault;
begin
  Assert.AreEqual('Default', DeepBase.Config.GetConfig('Key', 'Default'));
end;
```

### 4. 命名规范

```pascal
// 格式：Test_<方法�?_<场景>_<预期结果>
procedure Test_Initialize_WithMemoryDB_ReturnsTrue;
procedure Test_GetConfig_KeyNotExists_ReturnsDefault;
```

### 5. 隔离测试（非单例�?

```pascal
var M: TDeepBaseManager;
begin
  M := TDeepBaseManager.Create(nil);
  try
    M.InitializeWithDBOrRaise(':memory:');
    // 测试逻辑
  finally
    M.Free;
  end;
end;
```

### 6. 命令行编�?(MSBuild)

```batch
:: 编译
msbuild Tests\DeepBaseTests.dproj /p:Config=Release /p:Platform=Win32

:: 运行测试
DeepBaseTests.exe -exitbehavior:Continue

:: 指定输出格式
DeepBaseTests.exe -format:xml -output:test-results.xml
```

### 7. CI/CD 集成 (GitHub Actions)

CI 使用 Windows self-hosted runner，统一调用 `Scripts/run_tests.ps1`�?
```yaml
name: Delphi CI
on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      run_db_integration:
        type: boolean
        default: false

jobs:
  unit-tests:
    runs-on: [self-hosted, delphi-windows]
    steps:
      - uses: actions/checkout@v4
      - run: powershell -ExecutionPolicy Bypass -File .\Scripts\run_tests.ps1 -Type Unit -CI -Platform Win64

  integration-tests:
    runs-on: [self-hosted, delphi-windows]
    steps:
      - uses: actions/checkout@v4
      - run: powershell -ExecutionPolicy Bypass -File .\Scripts\run_tests.ps1 -Type Integration -CI -Platform Win64

  db-integration-tests:
    runs-on: [self-hosted, delphi-windows]
    if: github.event_name == 'workflow_dispatch' && inputs.run_db_integration == true
    env:
      DeepBase_RUN_DB_INTEGRATION: '1'
    steps:
      - uses: actions/checkout@v4
      - run: powershell -ExecutionPolicy Bypass -File .\Scripts\run_tests.ps1 -Type Integration -CI -Platform Win64
```

默认 Integration 会排�?`DBEnv` 分类。需要数据库环境的集成测试必须显式设�?`DeepBase_RUN_DB_INTEGRATION=1`，例�?workflow_dispatch 勾�?`run_db_integration`�?
Runner 要求�?
- RAD Studio/Delphi 12 Win64 编译器可用：`C:\Program Files (x86)\Embarcadero\Studio\23.0\bin\dcc64.exe`�?- 安装 FireDAC SQLite 相关单元：`FireDAC.Phys.SQLite`、`FireDAC.Phys.SQLiteDef`、`FireDAC.Stan.ExprFuncs`�?- 运行目录或脚本候选路径中存在位宽匹配�?`sqlite3.dll`；`Scripts/run_tests.ps1` 会复制到 `Tests\Integration` 并在测试后清理�?- WebAPI 本地回环测试由脚本临时设�?`DeepBase_ALLOW_LOCALHOST_HTTP=1`，无需�?CI 全局开启�?
### 8. 测试覆盖率目�?
| 模块 | 最低覆盖率 |
|------|-----------|
| DeepBase.Manager | 90% |
| DeepBase.Config | 90% |
| DeepBase.i18n | 85% |
| DeepBase.Logging | 80% |
| VCL Controls | 70% |

---

## 检查清�?

### 安全检�?- [ ] LLM API Key 使用 Credential Manager 引用，通用 Secret 使用 `DeepBase.Security.SaveSecret`
- [ ] 日志无明文密�?
- [ ] SQL 全部参数�?
- [ ] EXE 已签�?

### 测试检�?
- [ ] 每个模块有对应测�?
- [ ] 测试使用 `:memory:` 隔离
- [ ] CI/CD 自动运行测试
- [ ] 测试覆盖率达�?
