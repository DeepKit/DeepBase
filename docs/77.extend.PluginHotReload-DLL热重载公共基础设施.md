# DeepBase.Plugins — DLL 热重载公共基础设施开发文档

> 编号：77.extend.PluginHotReload
> 日期：2026-08-10
> 状态：**开发已授权；实现验证完成，但 ABI r2 完成前暂不发布 SDK Preview**（四核心单元与 41/41 动态验收已存在，2026-08-12 多模型再审要求先迁移到纯 C ABI）
> 首个消费方：DeepCompare Gateway P6（DLL 插件引擎，见 `D:\_Progs\02Business\DeepCompare\docs\GATEWAY_CLOUD_CONFIG_DESIGN.md` v1.2 §9 P6）
> 关联：toWangwei #097（配套上传通道）、Assayer ASY-DLL-002/003/004/019 工单

---

## 1. 背景与目标

Assayer 项目已实现完整、经过编译验证的 DLL 插件系统（插件生命周期管理 + 崩溃防护 + 签名校验 + 接口契约）。DeepCompare Gateway 的 P6 阶段需要同样的能力（用 DLL 承接 actions/op 序列表达不了的特殊站点反代逻辑）。

**按老板指示，不自研、不复制粘贴，把 Assayer 插件系统升级为 DeepBase 公共基础设施**：

1. 将插件系统核心代码从 Assayer 命名空间迁入 DeepBase（`DeepBase.Plugins.*`），与 `DeepBase.AutoUpdate`/`DeepBase.AutoFix` 同级；
2. Assayer 改为消费 DeepBase.Plugins（删除自有副本，改引用公共单元，行为不变）；
3. DeepCompare Gateway 作为第二消费方接入，新增站点适配器插件契约 `ISiteAdapterPlugin`。

## 2. 既有资产清单（迁移来源）

源目录：`D:\_Progs\02Business\Assayer\src\core\plugins\`（合约在 `src\contracts\`）

| Assayer 单元 | 行数 | 职责 | 迁移目标单元 |
|--------------|------|------|--------------|
| `Assayer.Contracts.Plugin.pas` | 206 | 插件契约：IPluginContract 基接口 + Router/Pipeline/Auth/Audit/Stats 5 类业务接口 + stdcall 导出函数类型 + 元数据/状态/健康记录类型 | `DeepBase.Plugins.Contracts.pas`（**只迁公共部分**，业务接口留在 Assayer） |
| `Assayer.PluginManager.pas` | 734 | TPluginManager：Register/EnsureLoaded（懒加载 ASY-DLL-004）/Load/Unload/Reload/List + 全局单例（ASY-DLL-019） | `DeepBase.Plugins.Manager.pas` |
| `Assayer.PluginSafeGuard.pas` | 336 | TPluginSafeGuard：插件调用崩溃防护、故障计数、熔断与降级 | `DeepBase.Plugins.SafeGuard.pas` |
| `Assayer.PluginVerifier.pas` | 198 | TPluginVerifier：DLL 加载前完整性/签名校验 | `DeepBase.Plugins.Verifier.pas` |
| `Assayer.PluginManager.BROKEN.pas` | 700 | 历史损坏版本 | **不迁移，删除** |

插件实现样例（留在 Assayer，作为接入参考）：`plugins\core\{Auth,Pipeline,Router}\`、`plugins\extended\{Audit,Stats}\`，构建脚本 `build_plugins.bat` / `build_dll_sandbox.bat`。

## 3. 公共部分 vs 业务部分拆分

**迁入 DeepBase（公共基础设施）**：

- `IPluginContract` 基接口（Initialize / Shutdown / ReloadConfig / GetMetadata / GetApiVersion / GetHealthStatus / IsHealthy，全 stdcall）；
- `TPluginType` / `TPluginState` / `TPluginMetadata` / `TPluginHealthStatus`；
- `PLUGIN_API_VERSION_*` 常量（主版本必须匹配才允许加载）；
- 当前实现保留 `TCreatePluginFunc = function: IPluginContract; stdcall;` 作为**实验性 Delphi 同编译器 ABI**；该接口不得作为产品线稳定 ABI 发布；`IPluginContract`、string、TBytes 和 Delphi 对象只能存在于宿主内部包装层；`r2` 必须迁移为纯 C 数据面：固定导出函数、不透明句柄、固定宽度整数、`PByte + Length + Capacity`、显式错误码及同模块 `FreeBuffer`。
- TPluginManager（泛化：插件类别从硬编码 5 类改为**注册制**——宿主自行 RegisterContractClass）；
- TPluginSafeGuard、TPluginVerifier。

**留在各宿主（业务契约）**：Assayer 的 IRouterPlugin/IPipelinePlugin/IAuthPlugin/IAuditPlugin/IStatsPlugin；DeepCompare 的 ISiteAdapterPlugin（见 §6）。

## 4. TPluginManager 泛化改造要点

现有实现的 5 个 `Get*Plugin` 便捷方法是 Assayer 业务，公共版改为：

```pascal
// 宿主注册业务契约类别（名称 -> 期望的导出函数名规则）
procedure RegisterPluginKind(const AKind: string;
  const ACreateFuncPrefix: string);  // 如 'Create' + Kind 名

// 按名称取已加载插件，宿主自行 Supports/QueryInterface 到业务接口
function GetPlugin(const APluginName: string): IPluginContract;
```

其余能力原样保留：
- **懒加载**（ASY-DLL-004）：RegisterPlugin 只声明，EnsureLoaded 首次使用时加载；
- **ReloadPlugin**：Unload → Load 原子序列，TCriticalSection 保护；
- **SafeGuard 联动**：加载失败/连续崩溃 → 状态 psError，自动降级不再加载；
- **事件钩子**：OnPluginLoaded / OnPluginUnloaded 保留（供宿主做 /admin/plugins 状态端点，Assayer 已有先例）。

新增能力（泛化改造同步引入）：
- **插件依赖声明**：RegisterPluginKind 增加可选参数 `ADependsOn: TArray<string>`，Manager 加载插件时先递归加载依赖项；依赖加载失败则当前插件跳过并标记 psError；**注册时对 ADependsOn 做 DFS 拓扑环检测，发现环（A→B→A）拒绝注册并告警，禁止运行时死递归**；
- **在途调用计数**：每个插件实例维护 `AtomicIncrement/Decrement` 调用计数，供 Unload 等待归零与 SafeGuard 熔断判断；
- **共享内存管理器**：宿主与插件 DLL 必须链接同一共享内存管理器（BorlndMM/ShareMem 模式），跨边界传递 string/TJSONObject 时由调用方分配、被调方只读，禁止被调方释放调用方内存。
- **Lease/Invoke 门禁**：Manager 不直接暴露 `IPluginContract` 接口引用给宿主业务代码，改为通过 `TPluginLease` 中间层：调用方 `AcquireLease(PluginName, Timeout)` 获得临时引用，用完 `ReleaseLease` 归还；**Lease drain 超时由 Manifest 配置，宿主强制限制在 10–300 秒，默认 300 秒；超时进入 psPendingRestart/psError、停止新调用并告警，禁止强制 FreeLibrary**——Windows 进程内无法安全地强制卸载引用非零的 DLL，强制卸载会导致后续访问野内存崩溃；

## 5. Delphi DLL 热重载工程约束（实现时必须遵守）

1. **接口 GUID 即 ABI**：宿主与 DLL 必须编译**同一份** Contracts 单元（公共化后天然满足）；GUID 一旦发布禁止改动；扩展契约**只能通过接口继承**新增（新接口新 GUID，旧插件无需改动即可继续加载），禁止修改已有接口方法签名；Contracts 单元须嵌入 `PLUGIN_ABI_MAJOR` / `PLUGIN_ABI_MINOR` 常量（主版本必须完全匹配，次版本宿主 ≥ 插件即可），加载时校验宿主与 DLL 的编译器版本与 RTL 标识，不匹配则拒绝加载并告警；
2. **同版本 RTL**：宿主与插件 DLL 用同一 Delphi 编译器版本构建（当前 Delphi 13，编译器路径见项目记忆），跨版本传递 string/TJSONObject 不保证兼容；
3. **FreeLibrary 前置条件**：卸载前必须确认接口引用全部归零（管理器通过 Lease 门禁持有插件引用，Unload 时先调 Shutdown 再等待所有 Lease 释放，**等待上限取 Manifest 的 `unload_timeout_ms`，宿主钳制在 10–300 秒，默认 300 秒；超时进入 pending restart 并告警，禁止强制 FreeLibrary**——Windows 进程内无法安全地强制卸载引用非零的 DLL，强制卸载会导致后续访问野内存崩溃）；**禁止插件向宿主传递跨边界长期引用**（如将自身接口存入宿主全局对象、注册长期回调），所有跨边界引用必须通过 Lease 门禁限定在单次调用生命周期内；
4. **禁止在 DLL 中使用 VCL 全局状态**（类注册表、Application 对象）——Gateway 的站点适配器插件只做无头逻辑 + 通过宿主回调驱动 WebView2，不自建窗口；
5. **调用约定与跨边界内存**：稳定 ABI 必须是纯 C 数据面并禁止异常越界传播。禁止跨 DLL 边界出现 Delphi interface、string、TBytes、动态数组、对象、Variant 或由另一模块释放的内存。变长输出采用两次调用：第一次返回所需长度，第二次由宿主分配 `PByte + Capacity`；或者由插件分配并由同一插件导出的 `FreeBuffer` 释放。现有 `IPluginContract/TBytes` 仅可作为同编译器、同 RTL、同共享内存管理器下的实验适配层，不得标为稳定 ABI。
6. **热重载时序**：新 DLL 文件先写到旁路目录 → 校验签名 → **暂停该插件新调用请求（排队或返回 503）** → 等待所有 Lease 释放（**等待上限 300 秒，与 Unload 语义一致；超时标记 psError 并告警人工介入，禁止强制卸载**） → 备份旧 DLL 到临时目录 → Unload 旧版 → 文件替换 → Load 新版 → Initialize → 恢复调用；失败则回滚旧文件并 psError；
7. **多线程安全**：Reload 全程持 TCriticalSection 排他锁；Reload 期间新调用请求返回 `PLUGIN_RELOADING` 错误码（宿主侧降级处理），确保不会在 Unload→Load 窗口期调用已卸载代码；每个插件实例通过 Lease 计数器管理并发调用，Unload 前必须等待所有 Lease 释放。

## 6. DeepCompare Gateway 消费契约（新增，随本基础设施一并冻结）

```pascal
// DeepCompare.Plugins.SiteAdapter.pas（放 DeepCompare 侧，不放 DeepBase）
ISiteAdapterPlugin = interface(IPluginContract)
  ['{生成新 GUID}']
  // 入站：OpenAI 兼容请求 JSON -> 返回浏览器自动化指令序列（或直接返回响应）
  // 仅为宿主内部 Delphi 包装接口；不得直接作为稳定 DLL ABI。稳定 ABI 使用 PByte+Length+Capacity/FreeBuffer。
  function BuildActions(const ARequestBytes: TBytes;
    var AActionsBytes: TBytes): Boolean; stdcall;
  // 出站：从页面提取的原始结果 -> OpenAI 兼容响应 JSON
  function ParseResponse(const APageDataBytes: TBytes;
    var AResponseBytes: TBytes): Boolean; stdcall;
end;
```

Gateway 集成方式：配置包内 provider 的 `engine` 字段为 `dll:<plugin_name>` 时，经 GlobalPluginManager 懒加载对应 DLL（签名未过校验的拒载，见设计文档 §7.7）。

## 7. 验收标准

- [ ] `DeepBase.Plugins.{Contracts,Manager,SafeGuard,Verifier}.pas` 四单元随 DeepBase 编译通过（零警告目标）；
- [ ] Assayer 切换到公共单元后全量插件构建（build_plugins.bat）+ 现有测试（tests/Test.ProxyPlugin.pas）通过，行为无回归；
- [ ] 热重载用例：运行中替换 Audit 插件 DLL → ReloadPlugin → 旧请求收尾、新请求走新代码，无内存增长（循环 100 次 Reload 验证句柄不泄漏）;
- [ ] 崩溃隔离用例：插件内人为 AV → SafeGuard 捕获，宿主不崩，插件标记 psError；
- [ ] 签名校验用例：篡改 DLL 字节 → Verifier 拒载并告警；
- [ ] Gateway 侧 ISiteAdapterPlugin 冒烟：示例 hello 站点 DLL 被 /v1/chat/completions 走通。

## 8. 实施拆分与预估

**重要提示**：以下排期前提是先行完成两份 ADR 冻结（`ADR-Plugin-ABI-and-Lifetime.md` + `ADR-Config-Artifact-Publish-StateMachine.md`），ADR 通过前不进入编码。

| 步骤 | 内容 | 预估 | 依赖 |
|------|------|------|------|
| S0 | 契约冻结：C ABI/对象所有权/错误码/ABI 版本模型/字节缓冲区协议/**zip 包格式规范（含 plugin_manifest.json 结构、签名规则、DLL 制品 URL 引用）——包格式归口本文档，78 严格按此规范打包** | 0.5 天 | 无 |
| S1 | DeepBase.Plugins 四单元创建 + Contracts 公共/业务拆分 + Lease 门禁 | 1 天 | S0 |
| S2 | TPluginManager 泛化（注册制）+ Assayer 切换 + 回归 | 1.5 天 | S1 |
| S3 | 热重载/崩溃/签名/卸载泄漏(Lease 超时等待)/跨语言 JCS 五组测试脚本化 | 1 天 | S2 |
| S4 | ISiteAdapterPlugin 契约（字节缓冲区版本）+ Gateway 示例插件冒烟（依赖 78 的 DLL 制品仓库与 plugin_manifest 引用链路就绪） | 1 天 | S3 + 78 制品仓库 |

总计约 **4.5 天**（不含 S0 的 ADR 编写时间）。依赖：DeepBase 编译环境就绪即可开工；S4 依赖本文档 S0 交付的 zip 包格式规范（含 plugin_manifest.json）与 78 的 DLL 制品仓库就绪。

