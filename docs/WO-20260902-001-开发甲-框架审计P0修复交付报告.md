# WO-20260902-001 框架审计 P0 修复交付报告

| 项 | 内容 |
|---|---|
| 工单 | `docs/WO-20260902-001-开发甲-框架审计P0修复工单.md` |
| Brief | `docs/brief-WO-20260902-001-开发甲.md` |
| 审阅源 | `CodeReview/20260902-Framework-Audit.md` |
| 交付日 | 2026-09-02 |
| 执行者 | 开发甲 |

## 逐项修复

| FIX | Bug | 文件与行号（要点） | 回归 |
|-----|-----|-------------------|------|
| FIX-1 | BUG-334 | `Persistence/DeepBase.DB.Pool.pas` Shutdown ~1099–1120：超时后仅 Delete 非 `csInUse`，in-use 故意保留（leak > UAF） | `Test.Regression.BUG334_PoolShutdownInUse` |
| FIX-2 | BUG-335 | `Persistence/DeepBase.DB.Guardian.pas` `ClassifyOpenError` ~138；ProtectConnection ~494–535：瞬态 Open 失败不 quarantine | `Test.Regression.BUG335_GuardianTransientOpen` |
| FIX-3 | BUG-326 | `Core/DeepBase.Scheduler.pas` ~1035–1117：`FRunningITask` 在 OnCompleted/OnFailed 回调后再置 nil | `Test.Regression.BUG326_SchedulerCallbackSafety`（含 Cleanup 回调扩展） |
| FIX-4 | BUG-320 | `Core/DeepBase.FileWatcher.pas` ~868–887：Debounce 捕获 `LGuard: IInterface` | 既有 BUG320 + 本工单生命周期回归 |
| FIX-5 | BUG-336 | `Core/DeepBase.WorkerQueue.pas` `Stop` ~2149–2223：snapshot → Terminate → WaitFor → 再 `Clear`；`SetWorkerCount` 用 `Extract` 避免 OwnsObjects 提前 Free | `Test.Regression.BUG336_WorkerQueueStopWait` |
| FIX-6 | BUG-327 | `Core/DeepBase.KeyManager.pas` ~1076：GCM 判定 `Length >= 1+12+16` | `Test.Regression.BUG327_KeyManagerAEAD`（含空明文 round-trip） |
| FIX-7 | BUG-332 | `Core/DeepBase.SchemaAdapter.pas` hex Validate；`WeChat4x` 前缀 `[]`；**Create 改为 virtual**（metaclass 创建必须走子类构造）；Registry 空前缀跳过注册 | `Test.Regression.BUG332_WeChatSchemaRegistryResolve` |
| FIX-8 | BUG-337 | `Core/DeepBase.Authorization.pas` UpdateUser ~1095–1128 / UpdateRole ~1244–1273：写库后同步 Live 内存态；Destroy 显式 `FStorage := nil` | `Test.Regression.BUG337_AuthorizationUpdateSync` |
| FIX-9 | BUG-338 | `Persistence/DeepBase.DB.DoQry.pas` BindJsonParams ~861–875：原值绑定，UUID 用 Trim 副本检测 | `Test.Regression.BUG338_DoQryBindTrim` |
| FIX-10 | BUG-339 | `Persistence/DeepBase.DB.DoQry.pas` Sweep/Clear ~1781–1857：`InUseCount > 0` 跳过；uses `Winapi.Windows`（DEBUG） | `Test.Regression.BUG339_DoQrySweepInUse` |
| FIX-11 | BUG-340 | `Core/DeepBase.PluginManager.pas` ~804–809：Finalize → Remove → `PluginIntf:=nil` → UnloadBPL | `Test.Regression.BUG340_PluginUnloadOrder` |

## 根因补记（调试证据）

- **FIX-5**：初版在 `OwnsObjects=True` 的 `TObjectList` 上先 `Clear` 再 `WaitFor`，触发「句柄无效」；日志显示 `before WaitFor` 后无 `after WaitFor`。改为 snapshot → WaitFor → Clear。
- **FIX-7 Registry**：`TBaseSchemaAdapter.Create` 非 virtual 时，`TSchemaAdapterClass.Create` 不跑子类构造，指纹前缀恒空。日志 `WeChat39x.Create` post-fix 显示 `prefixCount:1, prefix0:e4a7b3c9f1`。
- **BUG339 测试**：未开 `UniDbSetDirectSQLAllowed(True)` 时 6 线程全失败（把直 SQL 当命名查询）。

## 证据文件

| 证据 | 绝对路径 |
|------|----------|
| WO 过滤单测 XML 目录 | `D:\_Progs\02Business\DeepBase\TestResults\WO-20260902-001\` |
| 全量单测 XML | `D:\_Progs\02Business\DeepBase\TestResults\WO-20260902-001-full\UnitTestResults.xml` |
| 全量控制台日志 | `D:\_Progs\02Business\DeepBase\TestResults\WO-20260902-001-full\run_console.log` |
| 回归注册表 | `D:\_Progs\02Business\DeepBase\Tests\Regression\RegressionTestRegistry.pas`（BUG-334~340） |

**全量单测结果（2026-09-02）：** Found 4393 / Passed 4386 / Failed 3 / Ignored 4 / Errored 0 / Leaked 0。

WO fixture（BUG326/327/332/334–340）在全量 XML 中均为 `result="Success"`。

全量 3 失败均为工单外 / 已知环境债（与 `tasks.md` CR-606、Timeout PBT 一致）：

1. `StaticPair_InjectedReplay_Unchanged` — Perception / 图形会话
2. `InjectedBitmap_FlowsThroughFrameDifferGate` — 同上
3. `Property6_TimeoutCancelsBackgroundTask` — Resilience Timeout PBT 偶发

WO 过滤 fixture 逐项 exe 结果：BUG334–340、BUG326、BUG327、BUG332 **全部 PASS**。

## BLOCKED / 未清债务

| ID | 说明 | 状态 |
|----|------|------|
| BLOCKED-DATA-P0-001 | WeChat4x 无真实 SHA256 指纹，`FSchemaFingerprintPrefixes := []`；Registry 对空前缀跳过注册 → Bootstrap 4.x 适配暂不可用 | 未清；禁止宣告 CLOSE |
| FIX-1 取舍 | Shutdown 故意泄漏 csInUse 连接 | 已文档化 |
| BUG340 覆盖 | 无现成测试 BPL，仅测未加载/失败路径 | 债务 |
| 39x 前缀 | `e4a7b3c9f1` 仍为契约值，非真实 dump | 与 BLOCKED-DATA 同源 |

## 新发现（工单外，未修）

- `RegressionTestRegistry` 历史 `REGRESSION_TEST_COUNT=72` 与已列数组不一致；本交付改为已列合计 31，并用 `TestUnit` 校验文件存在。
- Scheduler `LTaskFailed` 等 H2077 Hint 仍在，非本工单范围。

## 生产部署状态

| 项 | 值 |
|----|-----|
| 仓库 HEAD（交付前） | `0991d716` · 2026-09-01 10:42:22 +0800 |
| 本工单改动 | 工作区未提交（待主控 Review / 授权 commit） |
| 本机时钟（交付书写时） | 2026-09-02 16:18+ |
| 生产进程 | **未部署**：无生产 DeepBase 服务随本工单热更；部署状态 = 待 commit 后按发布流程更新 |

## 门禁结论

- G1 Brief：已落盘。
- G2–G3 构建/测试：WO 范围 fixture PASS；全量单测见证据目录。
- G4 异族/主控复核：交主控。
- G5 未清债务：存在 **BLOCKED-DATA-P0-001** → **不得 CLOSE**，可转「开发完成 / 待数据解锁」。
