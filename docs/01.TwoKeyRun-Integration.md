# TwoKeyRun - DeepBase AboutFrame 集成规划

> **文档版本**: 1.0
> **创建日期**: 2025-12-11
> **状�?*: 规划�?

---

## 1. 现状分析

### 1.1 当前数据库配�?
| 项目 | 当前�?|
|------|--------|
| 数据库名�?| `TwoKeyRun.db` |
| 图片存储�?| 自定义实�?(通过 `uDM.pas` �?`DatabaseSecurity.pas`) |
| 安全模块 | `TDatabaseSecurityManager` (自定�? |
| About Frame | `FrameAboutMe.pas` (自定�?~856 �? |

### 1.2 现有功能
TwoKeyRun �?`FrameAboutMe.pas` 实现�?
- 5 个打赏页�?(微信/支付�?BTC/USDT/关于�?
- 自定义加密地址存储 (`TDonationAddressInfo`)
- 机器码生成与显示
- 主题感知�?Tab 绘制
- 图片从数据库加载 (`LoadImageFromDB`)

### 1.3 集成目标
- 迁移�?DeepBase 统一�?`aboutMeImages` 表规�?
- 使用 `TwoKeyRunConfig.db` 作为 DB1
- 复用 `DeepBase.VCL.AboutFrame` 或保留现�?UI 但切换数据源

---

## 2. 目标架构

### 2.1 数据库命�?
```
�? TwoKeyRun.db (内含多种业务数据)
�? TwoKeyRunConfig.db (专用配置/公众�?打赏图像)
```

### 2.2 aboutMeImages 表结�?
```sql
CREATE TABLE IF NOT EXISTS aboutMeImages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  image_key TEXT NOT NULL UNIQUE,
  image_data BLOB NOT NULL,      -- AES-256 加密的图像数据（SeedTool 写入�?
  address_text TEXT,
  description TEXT,
  enabled INTEGER NOT NULL DEFAULT 1,  -- 0=隐藏该Tab, 1=显示
  sha256_hash TEXT NOT NULL,     -- 解密后明文图像的 SHA-256（HEX�?
  hmac_sha256 TEXT NOT NULL,     -- 解密后明文图像的 HMAC-SHA256（HEX�?
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 2.3 标准 ImageKey 列表
| ImageKey | 对应页签 | 用�?|
|----------|----------|------|
| `official_gzh` | 公司公众�?| 公众号二维码 (可�? |
| `wechat` | 微信 | 微信收款�?|
| `alipay` | 支付�?| 支付宝收款码 |
| `btc` | BTC | BTC 二维�?+ 地址 |
| `usdt` | USDT | USDT 二维�?+ 地址 |
| `aboutme` | 关于�?| 开发者照�?介绍 |

---

## 3. 迁移方案

### 3.1 方案 A: 完全替换�?DeepBase.VCL.AboutFrame (推荐)

**优点**:
- 代码量减�?~800 �?
- 自动获得 DeepBase 安全模块更新
- 与其他工具保持一�?

**步骤**:
1. 创建 `TwoKeyRunConfig.db` 并初始化 `aboutMeImages` �?
2. 使用 SeedTool 播种 6 个标�?ImageKey
3. 在主窗体中替�?`TFrameAboutMe` �?`TUniAboutFrame`
4. 设置 `TUniAboutFrame.DatabasePath := 'TwoKeyRunConfig.db'`
5. 移除 `FrameAboutMe.pas` �?`DatabaseSecurity.pas` (如仅用于 About)

**代码示例**:
```pascal
uses
  DeepBase.AntiTamper,
  DeepBase.VCL.AboutFrame;

procedure TMainForm.CreateAboutFrame;
var
  Frame: TUniAboutFrame;
  Config: TAntiTamperConfig;
begin
  // AntiTamper 初始化（参数必须�?SeedTool 播种时一致）
  Config := TAntiTamperPackage.GetDefaultConfig;
  // TwoKeyRun 现有工程里已使用 @2241114 作为密码/口令（DB/Debug 等）；这里复用它作为 AntiTamper �?EncryptionKey
  Config.EncryptionKey := '@2241114';
  Config.Salt := 'TwoKeyRun_Salt_v1';
  Config.KdfIterations := 10000;
  Config.EnableHMAC := True;
  TAntiTamperPackage.Initialize(Config);

  Frame := TUniAboutFrame.Create(Self);
  Frame.Parent := pnlAbout;
  Frame.Align := alClient;
  Frame.DatabasePath := ExtractFilePath(ParamStr(0)) + 'TwoKeyRunConfig.db';
  Frame.ManualInitialize;
end;
```

### 3.2 方案 B: 保留现有 UI，切换数据源

**优点**:
- 保留 TwoKeyRun 特有�?UI 定制
- 渐进式迁移，风险较低

**步骤**:
1. 创建 `TwoKeyRunConfig.db`
2. 修改 `FrameAboutMe.pas` 连接新数据库
3. �?`LoadImageFromDB` 改为调用 `TAntiTamperPackage.LoadSecureImage`
4. 使用 SeedTool 播种数据
5. 保留主题/控制器集成逻辑

---

## 4. SeedTool 播种流程

### 4.1 准备图像资源
```
Assets/
├── official_gzh.png   (公司公众号二维码)
├── wechat.png         (微信收款�?
├── alipay.png         (支付宝收款码)
├── btc.png            (BTC 二维�?
├── usdt.png           (USDT 二维�?
└── aboutme.png        (开发者照�?
```

### 4.2 运行 SeedTool
```powershell
# 打开 SeedTool
cd D:\_Progs\02Business\DeepBase\Tools\SeedTool
.\SeedTool.exe

# �?SeedTool �?
# 1. 选择目标数据�? TwoKeyRunConfig.db
# 2. 导入 6 张图�?
# 3. 配置地址文本 (BTC/USDT 地址)
# 4. 设置�?ImageKey �?enabled 状�?
# 5. 执行播种
```

### 4.3 验证播种结果
```sql
SELECT image_key, enabled, LENGTH(image_data) AS size, address_text 
FROM aboutMeImages;
```

---

## 5. 解锁/公众号关�?

TwoKeyRun 已集�?`DeepBase.Unlock` 模块:
- 解锁码验�? `TDeepBaseUnlock.ValidateCode`
- 公众号关注流�? 用户扫描 `official_gzh` 二维�?�?获取解锁�?�?输入解锁

About Frame 中的公众�?Tab 可引导用户关注以获取解锁码�?

---

## 6. 测试清单

- [ ] TwoKeyRunConfig.db 创建成功
- [ ] aboutMeImages 表结构正�?
- [ ] 6 �?ImageKey 全部播种
- [ ] 图像正确解密显示
- [ ] BTC/USDT 地址复制功能正常
- [ ] enabled=0 �?Tab 隐藏
- [ ] 机器码显示正�?
- [ ] 主题切换�?Tab 颜色正确

---

## 7. 时间估算

| 任务 | 预估时间 |
|------|----------|
| 创建 TwoKeyRunConfig.db | 15 分钟 |
| SeedTool 播种 | 30 分钟 |
| 替换/修改 AboutFrame | 1-2 小时 |
| 测试验证 | 1 小时 |
| **总计** | **3-4 小时** |

---

## 8. 相关文档

- [04.01.DeepBase-4AI-数据库Schema说明-v1.0.md](./04.01.DeepBase-4AI-数据库Schema说明-v1.0.md) - aboutMeImages 表定�?- [IMPLEMENTATION_GUIDE.md](./IMPLEMENTATION_GUIDE.md) - AboutFrame 接入实施步骤
- [DeepBase-Downstream-Integration.md](./DeepBase-Downstream-Integration.md) - 下游工程标准接入指南
