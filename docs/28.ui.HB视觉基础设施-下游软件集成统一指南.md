# HB 视觉基础设施 · 下游软件集成统一指南

> **文件编号**：28.ui  
> **版本**：v1.2（2026-08-26 多显示器等比自适应 & 托盘令牌菜单增强版）  
> **适用产品**：`DeepDsh`、`Assayer`、`DeepRW`、`DeepCommerce（唤金）`、`DeepSync` 及所有基于 DeepBase 开发的 Delphi 软件  
> **编译器要求**：Delphi 13.1 on Win64（`dcc64.exe`），纯 0 Error / 0 Warning / 0 Leaked DCU  
> **设计哲学**：设计令牌驱动（Design Tokens）· VCL/FMX 孪生对等 · AI初筛人来拍板（Fail-Closed）· 动效克制高级 · 跨屏物理比例等效

---

## 1. 架构定位与二层边界原则

```
+-----------------------------------------------------------------------------------------------+
|  【应用业务层】(DeepDsh / Assayer / DeepRW / DeepCommerce / DeepSync)                          |
|  • 负责业务表单编排、DB1/DB2/API 事务绑定、业务规则校验与多步骤向导逻辑                           |
+-----------------------------------------------------------------------------------------------+
                                          │ 消费 / 组装 (只读依赖)
                                          ▼
+-----------------------------------------------------------------------------------------------+
|  【DeepBase HB 视觉基座层】(DeepBaseCore.dpk / DeepBaseVCL.dpk / DeepBaseFMX.dpk)             |
|  • 纯设计令牌系统 (10 主题 x 2 密度 x DPI 矢量缩放)                                             |
|  • 通用控件库 (THbDialog / THbSummaryBar / THbToastHost / THbVoiceDialog / THbTrayIcon 等)      |
|  • 离屏脱敏渲染器 (THbShareCardRenderer) / 全局动效调度器 (THbAnimationTicker)                 |
|  • 多显示器工作区比例锚定引擎 (THbWindowProportion) 与 Per-Monitor v2 矢量适配                  |
|  • 反事实门禁断言 (CF-VOICE-01..06) 与多线程死锁防御基础设施                                   |
+-----------------------------------------------------------------------------------------------+
```

### 核心纪律红线
1. **零硬编码色值**：所有界面绘制必须且只能从 `THbTheme.Tokens`（或控件内建令牌）获取颜色、圆角、字号与间距；
2. **严禁越界反向依赖**：DeepBase 任何单元不得引用下游产品私有 Units、SQL 语句或专属业务 DTO；
3. **动效克制高级**：金色像素占比 `< 8%`（`MotionGoldMaxRatio`），严禁全屏高频刷新，低配机稳定 60fps；
4. **决策安全闭环（Fail-Closed）**：关键变更、高风险行为与 AI 提取字段必须经人类用户在 `THbDialog` 或 `THbVoiceDialog` 中显式确认，未确认内容绝不得直接入库；
5. **线程与死锁防御**：UI 线程禁止 `Sleep`，跨线程刷新严禁使用 `TThread.Queue`，必须使用 `TThread.ForceQueue`。

---

## 2. 包引用与项目配置

### 2.1 包依赖配置 (Requires)

在下游产品的 `.dpk` 或主工程 `.dproj` 中添加依赖：

- **VCL 桌面端**：
  ```pascal
  requires
    DeepBaseCore,
    DeepBaseVCL;
  ```
- **FMX 跨平台端**：
  ```pascal
  requires
    DeepBaseCore,
    DeepBaseFMX;
  ```

### 2.2 常用单元索引表

| 单元分类 | 单元名称 | 架构职责 | 包含的核心类型与控件 |
|---|---|---|---|
| **Core 契约层** | `DeepBase.HB.Core` | 纯 RTL 设计令牌与主题引擎核心 | `THbTokens`、`THbTheme`、`THbDensity`、`THbBadgeTone`、`THbThemeDefinition` |
| **Core 契约层** | `DeepBase.HB.Palettes` | 10 套工业/轻奢/科技调色板 | `DeepBasePalettes`（Graphite、Violet、Canghai、Gold 等） |
| **Core 契约层** | `DeepBase.HB.CommandPalette.Types` | 命令面板共享契约 | `THbCommandItem`、`THbCommandGroup`、`THbCommandMatchScore` |
| **Core 契约层** | `DeepBase.HB.Gate.Types` | 门禁卡片与严重度共享契约 | `THbGateSeverity`、`THbGateItem`、`THbGateSummary` |
| **Core 契约层** | `DeepBase.HB.VirtualList.Types` | 虚拟列表共享契约 | `THbVirtualListItem`、`THbVirtualBatchAction`、`THbVirtualListStyle` |
| **Core 契约层** | `DeepBase.HB.Waterfall.Types` | 瀑布流向导共享契约 | `THbWaterfallMode`、`THbWaterfallFacet`、`THbWaterfallCard` |
| **Core 契约层** | `DeepBase.HB.Grid.Types` | 数据网格与统计聚合契约 | `THbGridColumnDef`、`THbGridColumnType`、`THbGridStats` |
| **Core 契约层** | `DeepBase.HB.AI.Types` | AI 协同座舱共享契约 | `THbAIModel`、`THbAIThoughtStep`、`THbAIDiffProposal` |
| **Core 契约层** | `DeepBase.HB.NavTree.Types` | 侧栏导航树契约 | `THbNavNodeKind`、`THbNavItem`、`THbNavTreeState` |
| **Core 契约层** | `DeepBase.HB.PageControl.Types` | 现代多风格选项卡契约 | `THbTabStyle`、`THbTabItem`、`THbTabCloseAction` |
| **Core 契约层** | `DeepBase.HB.Dock.Types` | 九宫格停靠与浮动契约 | `THbDockPosition`、`THbDockPanelState`、`THbDockLayout` |
| **Core 契约层** | `DeepBase.HB.ShareCard.Types` | 离屏脱敏分享卡契约 | `THbShareCardFormat`、`THbShareCardData` |
| **Core 契约层** | `DeepBase.HB.Dialogs.Types` | 对话框与向导共享契约 | `THbDialogKind`、`THbDialogResult`、`THbWaterfallOrientation`、`THbStepState` |
| **Core 契约层** | `DeepBase.HB.Voice.Types` | 语音录入与六态提取共享契约 | `THbVoiceState`、`THbVoiceFieldStatus`、`THbVoiceFieldItem`、`THbVoiceSessionData` |
| **Core 契约层** | `DeepBase.HB.Tray.Types` | 托盘与令牌菜单契约 | `THbTrayItemKind`、`THbTrayHeaderData`、`THbTrayMenuItemData` |
| **VCL 视图层** | `DeepBase.VCL.HB.Controls` | VCL 基础原子控件 | `THbButton`、`THbChip`、`THbBadge`、`THbAvatar`、`THbCard`、`THbSkeleton` |
| **VCL 视图层** | `DeepBase.VCL.HB.Dialogs` | VCL 现代分块对话框与展开栏 | `THbDialog`、`THbSummaryBar`（带 ModalRunner 测试接缝） |
| **VCL 视图层** | `DeepBase.VCL.HB.Voice` | VCL 语音录入波形与确认容器 | `THbVoiceWaveform`、`THbVoiceFieldCard`、`THbVoiceDialog` |
| **VCL 视图层** | `DeepBase.VCL.HB.Tray` | VCL 现代化托盘与分块菜单 | `THbTrayIcon`、`THbTrayMenu`、`THbTrayMenuForm` |
| **VCL 视图层** | `DeepBase.VCL.HB.Terminal` | VCL 终端与思考流控件 | `THbKeyValRow`、`THbStreamBlock`、`THbToastHost` |
| **VCL 视图层** | `DeepBase.VCL.HB.CommandPalette` | VCL 模糊搜索命令面板 | `THbCommandPalette`（Ctrl+K 呼出、首字母拼音模糊匹配） |
| **VCL 视图层** | `DeepBase.VCL.HB.Gate` | VCL 门禁检查手风琴面板 | `THbGatePanel`（严重度多选过滤、证据行折叠展开） |
| **VCL 视图层** | `DeepBase.VCL.HB.VirtualList` | VCL 10万级虚拟数据列表 | `THbVirtualList`（O(1) 滚动开销、多选批量动作栏） |
| **VCL 视图层** | `DeepBase.VCL.HB.Waterfall` | VCL 分面瀑布流向导 | `THbWaterfallFlow`（分面过滤、步骤高亮） |
| **VCL 视图层** | `DeepBase.VCL.HB.Grid` | VCL 类 Excel 高性能数据网格 | `THbDataGrid`（选中区域即时求和/均值/极值统计） |
| **VCL 视图层** | `DeepBase.VCL.HB.AI` | VCL AI 协同座舱面板 | `THbAIConsole`（思考流、差异提案、模型切换） |
| **VCL 视图层** | `DeepBase.VCL.HB.NavTree` | VCL 侧栏折叠导航树 | `THbNavTree`（240px ⇄ 48px Mini Rail 模式） |
| **VCL 视图层** | `DeepBase.VCL.HB.PageControl` | VCL 4种现代选项卡 | `THbPageControl`（Underline / Segmented / Pill / Card） |
| **VCL 视图层** | `DeepBase.VCL.HB.Dock` | VCL 九宫格磁吸停靠容器 | `THbDockSite`、`THbDockPanel` |
| **VCL 视图层** | `DeepBase.VCL.HB.ShareCard` | VCL 离屏脱敏成果卡渲染器 | `THbShareCardRenderer`（生成真实 PNG Magic Header） |
| **FMX 跨平台层** | `DeepBase.FMX.HB.*` | FMX 跨平台矢量对等孪生系列 | 上述所有 14 组控件的 FMX 矢量对等全实现 |

---

## 3. 主题系统与设计令牌接入

### 3.1 应用启动时初始化与换肤

在下游程序启动（`Application.Initialize`）或主窗口创建时调用：

```pascal
uses
  DeepBase.HB.Core,
  DeepBase.HB.Palettes;

// 1. 初始化主题系统 (自动注册 10 套内置调色板)
THbTheme.Initialize;

// 2. 根据产品设定默认主题与密度
// DeepDsh / 终端类: 推荐 'admin-graphite' (玄石) 或 'canghai-blue' (沧海)
// Assayer / 安全座舱: 推荐 'violet-dusk' (紫暮)
// DeepCommerce / 唤金: 推荐 'gold-luxury' (暖金) 或 'ink-gold' (墨金)
THbTheme.ApplyTheme('gold-luxury', hdComfortable);
```

### 3.2 动态响应主题切换广播

控件内部无需手动轮询。控件若继承自 `THbCustomControl`（VCL）或 `THbFmxControl`（FMX），将自动捕获 `THbThemeChangedMessage` 消息并触发平滑重绘。

---

## 4. 核心组件调用规范与代码范例

### 4.1 现代分块多按钮对话框 (`THbDialog`)

替代传统丑陋的 `ShowMessage` / `MessageDlg` / `InputBox`，全系统统一弹窗体验。

```pascal
uses
  DeepBase.HB.Dialogs.Types,
  DeepBase.VCL.HB.Dialogs;

// 场景 1: 普通操作成功/通知
THbDialog.ShowInfo('配置同步成功', '所有 5 个子组件已热加载最新有效配置。');

// 场景 2: 高风险操作拦截确认 (P0 Fail-Closed)
if THbDialog.Confirm('确认覆盖主路由表？', 
  '检测到未授权的配置文件重写尝试', 
  '⚠️ 影响范围：不可逆配置文件重写，放行将覆盖当前路由。') then
begin
  ApplyRouteOverride;
end;

// 场景 3: 单行 Prompt 输入 (如重命名)
var LNewName: string := '新建任务_01';
if THbDialog.Prompt('重命名任务', '请输入新任务名称：', LNewName) then
  RenameTask(LNewName);

// 场景 4: 带上下文展示 + 多行必填理由输入 (如治理审批驳回)
var LReason: string := '';
if THbDialog.PromptReason('驳回外部调用审批', '请填写驳回原因（必填，将存入审计日志）：', LReason) then
  RejectExecutionWithAudit(LReason);
```

---

### 4.2 通用语音录入与确认全生命周期 (`THbVoiceDialog` / `THbVoiceWaveform`)

用于口述录入、LLM 抽取与逐项人工核对确认。

```pascal
uses
  DeepBase.HB.Voice.Types,
  DeepBase.VCL.HB.Voice,
  DeepBase.VCL.HB.Terminal;

var
  Items: TArray<THbVoiceFieldItem>;
  ConfirmedCount: Integer;
begin
  // 1. 装载提取字段 (支持 Diff 原值比对与低置信度标记)
  SetLength(Items, 2);
  Items[0].FieldKey := 'phone';
  Items[0].FieldLabel := '备用联系手机';
  Items[0].OldValue := '138-0000-1122'; // 数据库已有旧值
  Items[0].ExtractedValue := '139-8877-6655';
  Items[0].OriginalQuote := '留了备用手机 139-8877-6655';
  Items[0].Status := vfsPending;

  Items[1].FieldKey := 'budget';
  Items[1].FieldLabel := '意向采购量';
  Items[1].ExtractedValue := '4 套';
  Items[1].IsLowConfidence := True; // 琥珀黄警告 badge
  Items[1].Status := vfsPending;

  // 2. 调出全生命周期确认对话框 (支持 Enter 全选、就地修改、丢弃)
  if THbVoiceDialog.Execute('唤金 · 客户档案语音提取确认', Items, ConfirmedCount) then
  begin
    // 3. 仅对用户确认项执行原子事务写库 (CF-VOICE-01 纪律)
    WriteConfirmedItemsToDatabase(Items);
    THbToastHost.Show(MainForm, Format('✓ 成功写入 %d 项客户属性到 DB1', [ConfirmedCount]), btSuccess);
  end;
end;
```

---

### 4.3 现代化系统托盘与令牌菜单 (`THbTrayIcon` & `THbTrayMenu`)

告别经典丑陋的 Win32 右键菜单，提供多显示器自适应、带运行状态呼吸灯、未读消息 Badge 与危险红项的分块菜单：

```pascal
uses
  DeepBase.HB.Tray.Types,
  DeepBase.VCL.HB.Tray;

var
  Tray: THbTrayIcon;
begin
  Tray := THbTrayIcon.Create(Self);
  Tray.Icon := Application.Icon;
  Tray.ToolTip := 'DeepCommerce 唤金 (运行中)';
  Tray.BadgeCount := 3;       // 动态在托盘图标绘制红点未读数
  Tray.BreathingDot := True;  // 动态在托盘图标绘制绿色在线微光

  // 1. 设置头部画像状态区 (Zone 1)
  Tray.Menu.SetHeader('DeepCommerce 唤金', '● 在线服务中 (Port: 8088)', 'v1.2.0', btSuccess, True);

  // 2. 添加业务快捷动作 (Zone 2 - 默认项加粗)
  Tray.Menu.AddItem('🗖 打开主控制台', HandleShowMainForm, 'Enter', True {isDefault});
  Tray.Menu.AddItem('🎤 快捷语音录入', HandleQuickVoice, 'Ctrl+Shift+V');
  Tray.Menu.AddItem('📊 今日唤醒看板', HandleShowDashboard, 'Alt+D');
  Tray.Menu.AddSeparator;

  // 3. 添加偏好设置项 (Zone 3 - 开关勾选)
  Tray.Menu.AddCheckItem('开机自动启动', HandleToggleAutoStart, True);
  Tray.Menu.AddCheckItem('托盘气泡消息通知', HandleToggleNotification, True);
  Tray.Menu.AddSeparator;

  // 4. 添加危险退出项 (Zone 4 - Hover 危险红警示)
  Tray.Menu.AddDestructiveItem('🚪 彻底退出唤金', HandleExitApp);

  // 5. 激活托盘
  Tray.Active := True;
end;
```

---

### 4.4 摘要+详情展开栏 (`THbSummaryBar`) & 瀑布流向导

支持**纵向瀑布流（`woVertical`）**与**横向手风琴流（`woHorizontal`）**：

```pascal
var
  SummaryBar: THbSummaryBar;
begin
  SummaryBar := THbSummaryBar.Create(Self);
  SummaryBar.Parent := PanelWorkflow;
  SummaryBar.Align := alTop;
  SummaryBar.StepIndex := 1;
  SummaryBar.Title := '本机端口与 DB1 基础存储';
  SummaryBar.SummaryText := ':8088 / SQLite (12ms)';
  SummaryBar.State := ssCompleted; // ssPending / ssActive / ssCompleted / ssError
  SummaryBar.Orientation := woVertical; // 或 woHorizontal
  SummaryBar.IsExpanded := False; // 点击自动平滑展开详情区
end;
```

---

### 4.5 终端流式输出与思考折叠 (`THbStreamBlock`)

用于 LLM 思考流（Thinking Fold）、Agent 日志与终端执行块：

```pascal
var
  StreamBlock: THbStreamBlock;
begin
  StreamBlock := THbStreamBlock.Create(Self);
  StreamBlock.Parent := ScrollBoxLog;
  StreamBlock.BlockTitle := 'Agent 决策思考流 (Evidence: 3 items, 0.42s)';
  StreamBlock.ContentText := '[00:01.120] Invoking Gate.ActionIntentPermissionChecker...'#13#10 +
                             '[00:01.240] Target path outside sandbox: /etc/assayer/';
  StreamBlock.IsFolded := True; // 默认折叠，点击标题栏自动展开
end;
```

---

### 4.6 浮层通知托管中心 (`THbToastHost`)

非阻塞式轻量通知，替代打断用户工作的模态框：

```pascal
uses
  DeepBase.VCL.HB.Terminal;

// 在主窗口右下角弹出 3 秒自动消失的轻提示
THbToastHost.Show(MainForm, '✓ 成功写入 4 项客户属性到 DB1', btSuccess, 3000);
```

---

### 4.7 动效基元包 (`THbMotionKit`)

严守小面积、短时长纪律，为工业座舱注入生命感：

```pascal
// 1. 仪表盘数字平滑滚动 (如耗时 120ms -> 24ms)
THbAnimatedNumber.Animate(LblMetric, 120, 24, 600 {ms}, emEaseOut);

// 2. 状态呼吸灯
BreathLight.Tone := btWarning; // 琥珀黄柔和呼吸 (1.5s 周期)
```

---

### 4.8 成果分享卡离屏渲染器 (`THbShareCardRenderer`)

任务跑通后一键生成 16:9 / 1:1 / 4:5 精美报告卡片（纯离屏矢量生成，自动隐私脱敏，导出为标准 PNG 文件）：

```pascal
uses
  DeepBase.HB.ShareCard.Types,
  DeepBase.VCL.HB.ShareCard;

var
  CardData: THbShareCardData;
begin
  CardData.Title := 'Assayer 安全治理自检报告';
  CardData.Metrics := '拦截 14 次 · 放行 2 次 · 用时 1.8s';
  CardData.Slogan := '主权在手，安全无界';
  CardData.EnableAutoMasking := True; // 自动脱敏绝对路径与用户名
  
  // 1. 复制到系统剪贴板
  THbShareCardRenderer.CopyToClipboard(CardData, scfLandscape16x9);
  
  // 2. 或保存为真实 PNG 图像
  THbShareCardRenderer.SaveToFile(CardData, scfPortrait4x5, 'report.png');
end;
```

---

### 4.9 现代化命令面板 (`THbCommandPalette`)

全局快捷呼出（Ctrl+K）、拼音/首字母模糊匹配、按分类分组高亮：

```pascal
uses
  DeepBase.HB.CommandPalette.Types,
  DeepBase.VCL.HB.CommandPalette;

var
  Palette: THbCommandPalette;
begin
  Palette := THbCommandPalette.Create(Self);
  Palette.Parent := Self;
  Palette.AddItem('cmd_scan', '全盘安全扫描', '安全', 'Ctrl+Shift+S');
  Palette.AddItem('cmd_route', '重载网关路由', '网关', 'Ctrl+R');
  Palette.OnExecuteCommand := HandleCommandExecute;
  Palette.ShowPalette; // 居中弹出浮层并聚焦输入框
end;
```

---

### 4.10 门禁检查手风琴面板 (`THbGatePanel`)

用于自动化审查、门禁状态聚合（PASS/WARN/BLOCKER）、多严重度过滤与证据展开：

```pascal
uses
  DeepBase.HB.Gate.Types,
  DeepBase.VCL.HB.Gate;

var
  Gate: THbGatePanel;
  Item: THbGateItem;
begin
  Gate := THbGatePanel.Create(Self);
  Gate.Parent := Self;
  Gate.Align := alClient;

  Item.Id := 'gate_p0_01';
  Item.Title := 'P0 零硬编码密钥扫描';
  Item.Severity := gsBlocker; // gsPass / gsNotice / gsWarning / gsBlocker
  Item.Summary := '未发现私钥明文泄漏';
  Item.DetailEvidence := '扫描 248 个单元，100% 走凭据管理器注入。';
  Gate.AddItem(Item);
end;
```

---

### 4.11 10万级虚拟数据列表 (`THbVirtualList`)

高性能大数据量虚拟滚动（内存 O(1) 恒定开销，支持多选与底部批量操作工具栏）：

```pascal
uses
  DeepBase.HB.VirtualList.Types,
  DeepBase.VCL.HB.VirtualList;

var
  VList: THbVirtualList;
begin
  VList := THbVirtualList.Create(Self);
  VList.Parent := Self;
  VList.Align := alClient;
  VList.TotalCount := 100000; // 声明 10 万行
  VList.OnGetItem := HandleGetVirtualItem; // 仅按需回调渲染当前视口 15~20 行
  VList.AddBatchAction('batch_export', '批量导出选中文档', btBrand);
  VList.AddBatchAction('batch_delete', '批量删除', btDanger);
end;
```

---

### 4.12 高性能虚拟数据网格 (`THbDataGrid`)

类 Excel 虚拟表格，支持冻结列、多列排序、选择区域即时数学统计（求和/均值/极值）：

```pascal
uses
  DeepBase.HB.Grid.Types,
  DeepBase.VCL.HB.Grid;

var
  Grid: THbDataGrid;
  Stats: THbGridStats;
begin
  Grid := THbDataGrid.Create(Self);
  Grid.Parent := Self;
  Grid.Align := alClient;
  Grid.AddColumn('order_no', '订单编号', 140, gctText);
  Grid.AddColumn('amount', '实收金额', 100, gctFloat);
  Grid.RowCount := 50000;
  Grid.OnGetCellFloat := HandleGetFloat;

  // 获取当前选中区域的聚合统计指标
  Stats := Grid.ComputeSelectionStats;
  // Stats.SumValue, Stats.AvgValue, Stats.MinValue, Stats.MaxValue
end;
```

---

### 4.13 AI 协同座舱面板 (`THbAIConsole`)

集成大模型切换、思考链折叠、代码 Diff 方案提案与确认：

```pascal
uses
  DeepBase.HB.AI.Types,
  DeepBase.VCL.HB.AI;

var
  AIConsole: THbAIConsole;
begin
  AIConsole := THbAIConsole.Create(Self);
  AIConsole.Parent := Self;
  AIConsole.Align := alRight;
  AIConsole.Width := 420;
  AIConsole.SelectedModel := aimLocal8B;
  AIConsole.AddThoughtStep('特征比对', '向量余弦相似度 0.942', 180, 4);
  AIConsole.AddDiffProposal('prop_01', 'AuthCheck', '认证守门', 'IsAdmin=False', 'IsAdmin=True', 'P0加固');
end;
```

---

### 4.14 侧栏折叠导航树 (`THbNavTree`) 与 现代多风格选项卡 (`THbPageControl`)

```pascal
uses
  DeepBase.HB.NavTree.Types,
  DeepBase.HB.PageControl.Types,
  DeepBase.VCL.HB.NavTree,
  DeepBase.VCL.HB.PageControl;

// 1. 侧栏 Mini-Rail 折叠导航
NavTree.AddSection('WORKSPACE');
NavTree.AddItem('dashboard', '仪表盘', 'Live');
NavTree.AddItem('security', '安全治理', '3');
NavTree.ToggleRail; // 240px 展开 ⇄ 48px Mini Rail 极窄图标栏平滑切换

// 2. 现代选项卡
PageControl.TabStyle := tsSegmented; // tsUnderline / tsSegmented / tsPill / tsCard
PageControl.AddTab('tab_orders', '订单明细');
PageControl.AddTab('tab_audit', '审计追踪');
```

---

## 5. 各下游系统集成映射一览表

| 下游系统 | 典型场景 | 推荐组合控件 | 主题基调 |
|---|---|---|---|
| **DeepCommerce (唤金)** | 顾客口述录入、意向抽取、托盘驻留唤醒 | `THbTrayIcon` + `THbVoiceDialog` + `THbVoiceWaveform` + `THbToastHost` | `gold-luxury`<br>`ink-gold` |
| **DeepDsh** | 终端命令行、工作流流转、托盘后台常驻、口述命令 | `THbTrayIcon` + `THbIgnitionOverlay` + `THbStreamBlock` + `THbKeyValRow` | `admin-graphite`<br>`canghai-blue` |
| **Assayer** | P0 治理拦截、主权放行、口述审批意见 | `THbDialog` (含输入区) + `THbSummaryBar` (瀑布向导) + `THbToastHost` | `violet-dusk` |
| **DeepRW** | 7 字段法源溯源、四问卡、门禁清单(GatePanel)、证据链 | 三栏壳 + `THbGatePanel` + `THbShareCardRenderer` + `THbDialog` + `THbSummaryBar` | `canghai-blue`＋`hdComfortable` |
| **DeepSync** | 数据版本同步、冲突双向消解 | `THbSummaryBar` (冲突对比) + `THbDialog` (放行确认) | `admin-graphite` |

---

## 6. 专题研究：多显示器窗口保持相同比例与视觉尺寸一致性方案

在多屏幕混合（例如 **4K 150% / 200% 主屏 + 1080p 100% 副屏 + 21:9 超宽带鱼屏**）环境下，传统 Win32/Delphi 应用跨屏移动常发生“窗口忽大忽小、文字模糊截断、布局比例变形”等问题。HB 基础设施提供 **三维比例一致性引擎**：

```
+-----------------------------------------------------------------------------------------------+
|  【第一维：Per-Monitor v2 动态 DPI 矢量自适应】                                                |
|  • Windows 消息拦截 WM_DPICHANGED，根据目标显示器 NewDPI / 96.0 动态重算所有 Tokens 尺寸       |
|  • 采用 GDI+ / Skia 浮点矢量即时重绘，杜绝位图拉伸模糊                                        |
+-----------------------------------------------------------------------------------------------+
|  【第二维：工作区相对比例锚定 (Workarea Proportional Anchor)】                                 |
|  • 引入 THbWindowProportion 描述符：设置 WidthRatio=0.65 (占屏宽65%), HeightRatio=0.75         |
|  • 跨屏移动触发 OnMonitorChange 时，基于目标 Monitor.WorkareaRect 自动等比缩放并居中吸附        |
|  • 无论在 13寸 1080p 还是 32寸 4K 屏幕上，窗口始终保持统一舒适的视觉屏幕占比                    |
+-----------------------------------------------------------------------------------------------+
|  【第三维：长宽比锁定与栅格弹性流动 (Aspect Ratio Lock & Responsive Grid)】                     |
|  • 拦截 WM_SIZING 消息，强制锁定设计基准宽高比 (如 16:10 或 4:3)，防止用户手动拖拉导致破版     |
|  • 内部栅格采用弹性 FlexLayout：小屏自动折叠次要栏 (3栏转2栏)，大屏自动展开                    |
+-----------------------------------------------------------------------------------------------+
```

### 生产级落地代码示例 (`THbWindowProportionHelper`)

```pascal
type
  THbWindowProportion = record
    WidthRatio: Single;    // 0.65: 占当前显示器可用工作区宽度的 65%
    HeightRatio: Single;   // 0.75: 占当前显示器可用工作区高度的 75%
    LockAspectRatio: Boolean; // True: 锁定宽高比
    AspectRatio: Single;   // 16.0 / 10.0
  end;

// 跨屏移动或初始化时调用
procedure ApplyProportionalBounds(AForm: TForm; const AProp: THbWindowProportion);
var
  Mon: TMonitor;
  WorkW, WorkH, TargetW, TargetH: Integer;
begin
  Mon := Screen.MonitorFromWindow(AForm.Handle);
  WorkW := Mon.WorkareaRect.Width;
  WorkH := Mon.WorkareaRect.Height;

  TargetW := Round(WorkW * AProp.WidthRatio);
  TargetH := Round(WorkH * AProp.HeightRatio);

  if AProp.LockAspectRatio and (AProp.AspectRatio > 0.01) then
  begin
    // 按高度推算宽度，若超限则按宽度推算高度
    if TargetW / TargetH > AProp.AspectRatio then
      TargetW := Round(TargetH * AProp.AspectRatio)
    else
      TargetH := Round(TargetW / AProp.AspectRatio);
  end;

  AForm.SetBounds(
    Mon.WorkareaRect.Left + (WorkW - TargetW) div 2,
    Mon.WorkareaRect.Top + (WorkH - TargetH) div 2,
    TargetW,
    TargetH
  );
end;
```

---

## 7. 国防级工程治理与多线程军规 (DeepAxis 补充条款)

所有下游系统在集成 HB 视觉体系与异步/语音能力时，必须严格遵守以下 4 大工程治理军规：

### 7.1 反事实门禁 (Counterfactual Gate · CF-VOICE 系列)
1. **CF-VOICE-01 (防越狱直接写库)**：写库层必须设置守门断言，无 `vfsAccepted` / `vfsModified` 确认态的字段一律拒绝写入并记审计日志；
2. **CF-VOICE-02 (畸形 JSON 容错)**：LLM 响应异常或截断时必须安全捕获并降级为 `[📝 原始原话全文]` 备忘卡，严禁崩溃；
3. **CF-VOICE-03 (零残留原子性)**：多字段写入必须包裹在同一数据库 Transaction 中，单项失败整单回滚；
4. **CF-VOICE-04 (草稿不复活丢弃项)**：恢复草稿时显式丢弃项保持丢弃态；
5. **CF-VOICE-05 (断电零污染)**：未完成会话被杀后，正式业务数据库零脏数据残留；
6. **CF-VOICE-06 (120s 截断保全)**：录音超时自动截断并保全已录音轨转写。

### 7.2 Delphi / VCL 多线程死锁防御铁律
1. **禁止 UI 线程 `Sleep`**：等待一律采用轻量计时器或可取消后台调度器；
2. **跨线程必须用 `TThread.ForceQueue`**：**严禁使用 `TThread.Queue`**（其在主线程内联执行时极易与 Synchronize 发生死锁，`ForceQueue` 强制入队彻底免疫死锁）；
3. **资源释放确定性**：音频采集句柄（waveIn / WASAPI）必须走 `try..finally` 确保确定性释放，异常时不得锁死声卡；
4. **编码规范**：所有含中文注释或字符串的 Pascal 源文件**必须保存为 UTF-8 BOM 编码**；
5. **统一 Command ID**：所有入口（按钮、快捷键、菜单、任务调度）绑定统一 Command ID，防止绕过审计拦截。

---

## 8. 质量门禁与验收标准 (CI Checklist)

下游产品在集成 HB 控件并发起 PR 验收时，必须全部满足以下 8 项门禁：

- [ ] **编译门禁**：Delphi 13.1 Win64 编译通过（0 Error, 0 Warning, 0 Leaked DCU）；
- [ ] **HB 自动化测试套件**：通过 `Test.DeepBase.HB.DeepRW.pas`、`Test.DeepBase.HB.Suite.pas`、`Test.DeepBase.HB.Voice.CF.pas`、`Test.DeepBase.HB.Tray.pas`、`Test.DeepBase.VCL.HB.Theme.pas`、`Test.DeepBase.FMX.HB.Dialogs.pas` 全部 42 项 DUnitX 单元测试（42/42 GREEN，0 泄漏）；
- [ ] **模态接缝双路径**：对话框类具备 `ModalRunner` 测试接缝，既支持无 UI 自动化测试驱动，又保障生产环境走原生模态窗口；
- [ ] **单一真相源编码**：所有 Pascal 源文件与测试必须为带 UTF-8 BOM 编码，杜绝无 BOM 导致中文/emoji 字面量解码乱码；
- [ ] **令牌门禁**：全文无硬编码颜色（如 `$00FFFFFF`、`clRed`），全部通过 `Tokens.*` 取色；
- [ ] **多屏比例**：在 100%、125%、150%、200% DPI 及跨屏拖拽下无文本截断、无模糊、等比缩放；
- [ ] **五态齐备**：所有交互控件具备 `Normal` / `Hover` / `Pressed` / `Disabled` / `Focus` 视觉反馈；
- [ ] **对比度合规**：所有文本与背景对比度达到 WCAG 2.1 AA 级（正文 ≥ 4.5:1，粗体大字 ≥ 3.0:1）；
- [ ] **Fail-Closed 闭环**：所有入库与配置覆写操作具备明确的人类确认态与取消回滚机制。
