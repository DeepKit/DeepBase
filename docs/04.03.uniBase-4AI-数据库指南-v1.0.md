# 03. 数据库与查询指南

> 本文档整合了 PostgreSQL 业务数据库指南和 doQry 查询框架使用说明。

---

## 1. PostgreSQL 业务数据库

> 本节适用于**业务数据库**设计。本地配置使用 UniBase 的 `config.db` (SQLite)。

### 1.1 数据库优先架构

UniBase 推崇"胖数据库"模式，充分利用 PostgreSQL 原生能力：

- **应用层 (Delphi)**：UI 交互、简单输入验证、调用数据库接口
- **数据库层 (PG)**：复杂查询、数据一致性、全文搜索、消息队列

### 1.2 推荐 PG 特性

#### JSONB - 灵活数据存储

```sql
CREATE TABLE user_profiles (
    user_id SERIAL PRIMARY KEY,
    preferences JSONB DEFAULT '{}'::jsonb
);

SELECT * FROM user_profiles WHERE preferences->>'theme' = 'dark';
```

#### 全文搜索

```sql
ALTER TABLE products ADD COLUMN search_vector tsvector;

CREATE TRIGGER tsvectorupdate BEFORE INSERT OR UPDATE
ON products FOR EACH ROW EXECUTE FUNCTION
tsvector_update_trigger(search_vector, 'pg_catalog.english', title, description);

SELECT title FROM products WHERE search_vector @@ to_tsquery('apple & phone');
```

#### 消息队列 (SKIP LOCKED)

```sql
DELETE FROM task_queue
WHERE id = (
    SELECT id FROM task_queue
    WHERE status = 'pending'
    FOR UPDATE SKIP LOCKED
    LIMIT 1
)
RETURNING *;
```

#### 实时通知 (LISTEN/NOTIFY)

```sql
NOTIFY channel_order_update, 'order_id=123';
```

Delphi 端使用 `TFDEventAlerter` 接收通知。

### 1.3 连接与事务管理

```pascal
// 显式事务管理
FDConnection1.StartTransaction;
try
  // 业务逻辑...
  FDConnection1.Commit;
except
  FDConnection1.Rollback;
  raise;
end;
```

### 1.4 推荐扩展

| 扩展名 | 用途 |
|--------|------|
| `uuid-ossp` | 生成 UUID |
| `pg_trgm` | 模糊搜索优化 |
| `pgcrypto` | 加密函数 |
| `zhparser` | 中文分词 |

---

## 2. doQry 查询框架

### 2.1 概述

`UniBase.DB.DoQry` 是统一的数据访问门面（单元 `UniBase.DB.DoQry`），提供：

- **统一**：标准化上下文、事务、执行、日志
- **安全**：参数化优先 + 治理策略（禁无 WHERE、默认 LIMIT）
- **高效**：查询模板热更新、Returning 减少往返
- **可观测**：JSON 日志 + CorrelationId 全链路追踪

### 2.2 快速开始

```pascal
uses UniBase.Manager, UniBase.DB.DoQry, FireDAC.Comp.Client;

var
  Conn: TFDConnection;
  Ctx: TUniQueryContext;
  Data: TFDMemTable;
begin
  // 1. 初始化
  UniBase.Initialize;
  UniDbInit(UniBase.RootPath);

  // 2. 建立连接
  Conn := TFDConnection.Create(nil);
  Conn.DriverName := 'PG';
  Conn.Params.Values['Server'] := '127.0.0.1';
  Conn.Params.Values['Database'] := 'mydb';
  Conn.Connected := True;

  // 3. 创建上下文
  Ctx := UniDbMakeContext(Conn, udbPostgreSQL, 30, UniDbNewCorrelationId);

  // 4. 执行查询
  var NewId := UniDbInsertReturningId('texts.insert',
    '{"user_id":2,"title":"示例","status":"pending"}', Ctx);

  var Rows := UniDbSelect('texts.list', '{"page":1,"size":50}', Data, Ctx);
end;
```

### 2.3 公开 API

```pascal
// 初始化
procedure UniDbInit(const RootPath: string);
function UniDbMakeContext(Conn: TFDConnection; DBType: TUniDBType; 
  TimeoutSec: Integer; const CorrelationId: string): TUniQueryContext;
function UniDbNewCorrelationId: string;

// 事务
function UniDbBeginTx(const Ctx: TUniQueryContext): IUniTransaction;
procedure UniDbRunInTx(const Ctx: TUniQueryContext; const Proc: TProc);

// 执行
function UniDbSelect(const ProcName, ParamsJson: string; 
  var Data: TFDMemTable; const Ctx: TUniQueryContext): Integer;
function UniDbExec(const ProcName, ParamsJson: string; 
  const Ctx: TUniQueryContext): Integer;
function UniDbInsertReturningId(const ProcName, ParamsJson: string; 
  const Ctx: TUniQueryContext): Integer;
function UniDbScalar(const ProcName, ParamsJson: string; 
  const Ctx: TUniQueryContext): Variant;

// 缓存管理
procedure UniDbClearQueryCache;
procedure UniDbInvalidateQuery(const ProcName: string);
procedure UniDbSetCacheTTL(Seconds: Integer);
procedure UniDbGetCacheStats(out Hits, Misses, EntryCount: Int64);

// 预编译语句池
procedure UniDbSetPreparedStatementPooling(Enabled: Boolean);
procedure UniDbClearPreparedStatements;
procedure UniDbGetPreparedStats(out PoolSize, ReuseCount: Int64);

// 生命周期
procedure UniDbShutdown;
```

### 2.4 参数协议 (JSON)

```json
{
  "user_id": 2,
  "title": "示例标题",
  "status": "pending",
  "created_at": "2025-11-27T12:30:00Z",
  "tags": ["a", "b", "c"]
}
```

- 日期：ISO-8601 格式
- 数组：用于 IN 子句，模板使用 `{in:ids}`

### 2.5 查询定义

在 `config.db` 的 `queries` 表配置：

```sql
-- sql_template
INSERT INTO texts (user_id, title, status)
VALUES (:user_id, :title, :status)

-- param_schema_json
{
  "user_id": {"type":"integer", "required":true},
  "title": {"type":"string", "required":true},
  "status": {"type":"string", "enum":["pending","done"]}
}
```

### 2.6 事务与安全

- **事务**：默认超时 30s，`UniDbRunInTx` 已支持嵌套 `SAVEPOINT`（SQLite / PostgreSQL）
- **安全**：
  - 禁止无 WHERE 的 UPDATE/DELETE
  - SELECT 默认 LIMIT=1000
  - 仅允许参数化绑定

### 2.7 日志

路径：`[ProjectRoot]/logs/query.log`

```json
{"ts":"2025-11-27T12:34:56Z", "level":"INFO", "corrId":"abc123", 
 "proc":"texts.list", "dbType":"PostgreSQL", "durationMs":12, "rows":50}
```

---

## 3. 方言差异

| 特性 | PostgreSQL | SQLite |
|------|------------|--------|
| 分页 | `LIMIT/OFFSET` | `LIMIT/OFFSET` |
| 返回ID | `RETURNING id` | `last_insert_rowid()` |
| 标识符 | `"col"` | `"col"` |

---

## 4. 对接清单

- [ ] 引用 `UniBase.Manager` 与 `UniBase.DB.DoQry`
- [ ] 初始化项目根路径与 ConfigDB
- [ ] 配置连接 Profile 或显式 `TFDConnection`
- [ ] 使用 `UniDbSelect/UniDbExec/UniDbScalar` 等调用（proc_name + ParamsJson）
- [ ] （可选）显式事务 `UniDbBeginTx/UniDbRunInTx`
- [ ] 添加日志查看功能
