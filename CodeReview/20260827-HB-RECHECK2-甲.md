# FIX2 第二轮整改复审报告（甲号工单 · 第三方独立复审）

- **工单**：`D:\_Progs\02Business\DeepBase\docs\ui\work-orders\WO-20260827-HB-AUDIT-FIX2-VCL-甲.md`
- **被复审交付报告**：`D:\_Progs\02Business\DeepBase\CodeReview\20260827-HB-FIX2-甲-交付报告.md`
- **上轮复审证据**：`D:\_Progs\02Business\DeepBase\CodeReview\20260827-HB-RECHECK-甲.md`（结论 FAIL，✅4/⚠️6）
- **复审时间**：2026-08-27（Asia/Shanghai）
- **复审方法**：只信代码实测——逐文件读源码 + 全仓库 Grep/Glob 残留扫描 + junit XML / 编译日志文件级取证；**未运行 dcc64 包级编译（主控已统一实测），编译数字以日志文件佐证性核验为准**
- **判定口径**：✅已修复 / ⚠️部分修复 / ❌未修复，以工单验收原文为基线；新增问题 🔴/🟡/🔵 分级

---

## 一、逐项复审

### R2 LLMWizard 整体摘除（含 R1/R3 联动）→ **✅ 已修复**

| 检查点 | 实测 | 结论 |
|---|---|---|
| `Core\DeepBase.HB.LLMWizard.Types.pas` 物理删除 | Glob 全仓库仅命中 7 个陈旧 `.dcu`（`Output\`、`TestResults\` 下），**源 .pas 不存在** | ✅ |
| `VCL\DeepBase.VCL.HB.LLMWizard.pas` 物理删除 | 同上，源 .pas 不存在 | ✅ |
| dpk contains 残留 | `DeepBaseVCL.dpk`（contains L18-89）与 `DeepBaseCore.dpk`（contains L34-127）均无 LLMWizard 条目 | ✅ |
| 全仓库引用残留 | Grep `LLMWizard` 全仓库：仅 CodeReview 报告、工单等历史**文档**命中；无任何 .pas/.dpk/.dpr 代码引用 | ✅ |
| 假测试移除 | `Tests\Test.DeepBase.HB.DeepRW.pas` 全文无 LLMWizard；uses（L16-35）干净；现有 8 个测试逐条核验均为真实断言 | ✅ |
| R1 联动（E2010/W1033 根因） | E2010 源文件已删；W1033 隐式导入源（LLMWizard uses FireDAC）已删，`DeepBaseVCL.dpk` requires 无需再补 `DeepBasePersistence`（工单 R1 该子项针对保留场景，方案 A 下自然满足） | ✅ |

- 交付报告已按工单写明选择**方案 A（整体摘除）**及理由 ✅。
- 遗留 🔵-1：陈旧编译产物 7 个 `.dcu` 残留（`Output\DeepBase.VCL.HB.LLMWizard.dcu` 等），建议清理防后续误链（dpk 已不 contains，不会被编译/链接，不阻断）。

### R4-1 圆角黑角（Controls.pas / Cards.pas）→ **✅ 已修复**

实测证据：
- 基类统一入口：`VCL\DeepBase.VCL.HB.Controls.pas:446-456` `THbCustomControl.EraseBackground`——`TGPSolidBrush(Tokens.Surface)` + `FillRectangle(MakeRect(0,0,Width,Height))` 全 ClientRect 填充；
- 五个指定控件 Paint 开头全部先擦后画：
  - `THbButton.Paint`：Controls.pas:618 `EraseBackground(Graphics);`
  - `THbDualButton.Paint`：Controls.pas:894 `EraseBackground(Graphics);`
  - `THbCard.Paint`：Cards.pas:317-322 全区 `FillRectangle`（自实现，等价）
  - `THbListRow.Paint`：Cards.pas:725-730 全区 `FillRectangle`
  - `THbEmptyState.Paint`：Cards.pas:1003-1008 全区 `FillRectangle`
- `WMEraseBkgnd` 自洽性：Controls.pas:368-371 / Cards.pas:226-229 保持 `Message.Result := 1`（注释防闪烁）——与「csOpaque 已移除 + DoubleBuffered=True + Paint 首行全 ClientRect 填充」组合**自洽**：系统擦除被短路后由 Paint 补画全区，内存位图无未定义区域，与工单推荐对照物 `Grid.pas:409-414` 同模式，未留新缺陷 ✅。

新增问题（见第六章 🟡-1）：同文件**其余圆角/矢量控件**（THbChip/THbBadge/THbAvatar/THbToast/THbSkeleton/THbSectionHeader/THbStatBig）Paint 仍不擦背景（如 Controls.pas:1023-1097 Chip 仅 FillPath 圆角、:1285-1360 Avatar 仅 FillEllipse），同类四角残留风险仍在——工单未点名，但交付报告"彻底杜绝 Windows 渲染残角"的绝对化声称与实况不符。

### R4-2 释放路径守卫（三处 MouseUp + CMMouseLeave）→ **✅ 已修复**

| 控件 | MouseUp 命中校验 | CMMouseLeave 复位 | 证据 |
|---|---|---|---|
| THbDualButton | `PtInRect(ClientRect, Point(X,Y))` 重算 HitPart，且 `FPressPart=HitPart` 同区才触发 FreeClick/PointsClick；触发与否都 `FPressPart:=0` 复位 | L828-834：`FHoverPart:=0; FPressPart:=0`，含 `inherited`（基类复位 FIsHovered/FIsPressed） | Controls.pas:849-872 |
| THbListRow | `FActionPressPart=FActionHoverPart` 同区校验（MouseMove 实时更新 hover，csCaptureMouse 捕获下拖出按钮区/控件外 hover 归 0，等效命中校验）；两分支均复位 | L683-689：双字段归零 + inherited | Cards.pas:691-703 |
| THbEmptyState | `FActionPressed and FActionHovered` 联合校验；if/else 两分支均 `FActionPressed:=False` | L949-955：双字段归零 + inherited | Cards.pas:967-982 |

付费按钮（PointsClick）拖出释放误触路径全部封堵 ✅。上轮"资损级交互缺陷"判定解除。
遗留 🔵-2：`THbChip.MouseUp`（Controls.pas:1016-1021）与 `THbSectionHeader.MouseUp`（:1781-1786）仍无 press 追踪（拖入释放即触发 OnClose/OnTrailingClick）——非资损路径，工单未列。

### R4-3 Waterfall 分面交互 + 双模式差异 → **✅ 已修复**

- **OnClick 真实绑定**：`VCL\DeepBase.VCL.HB.Waterfall.pas:378` `Btn.OnClick := OnFacetButtonClick;`（RebuildLeftRail 内每个分面按钮）；`OnFacetButtonClick`（:391-412）以 `Btn.Tag` 索引 facets，实现**聚焦 toggle**（:402-405 同 ID 再点取消）+ `RebuildLeftRail` + `RebuildWaterfall` 双重建 + `FOnFilterChanged` 回调——工单"更新 FFocusedCategoryId 并重建"完全达成 ✅
- **双模式渲染差异真实存在**：
  - 卡片形态：:448-457 `wmTimeline → ckOutline + rsS`（细边线小圆角）vs `wmSectioned → ckSurface + rsM`；
  - 标题形态：:464-467 `wmTimeline → '⏱ '+Title` vs `wmSectioned → '[分类名] '+Title` ✅
- 测试实证：`Test_Waterfall_Facet_Interactions_And_Mode_Rendering`（DeepRW.pas:347-378，FocusFacet/Mode 断言真实）+ `Test_Waterfall_Facet_Exclude_And_Focus`（Suite.pas:73-110，Exclude/Focus/Reset 断言真实）均 junit 实跑 Success ✅

遗留 🔵：①`OnFacetButtonClick` 未复用 `FocusFacet`（:281-301 会同步 `FFacets[].IsFocused`，按钮路径只改 FFocusedCategoryId，IsFocused 冗余字段两路径失同步——显示与过滤走 FFocusedCategoryId/IsExcluded，无行为缺陷）；②wmTimeline 未消费 `TimestampStr`（:236 写入后无渲染/排序消费，"时间线"实为添加序）；③ExcludeFacet 仅 API 无 UI 入口（工单字面未要求）。

### R4-4 Grid CreateParams/WS_VSCROLL → **✅ 已修复**

`VCL\DeepBase.VCL.HB.Grid.pas:223-227`：

```pascal
procedure THbDataGrid.CreateParams(var Params: TCreateParams);
begin
  inherited CreateParams(Params);          // ← 保留继承参数
  Params.Style := Params.Style or WS_VSCROLL;  // ← or 叠加，不清除既有风格位
end;
```

覆写方式正确（inherited 先行 + or 叠加）✅。配套 `CreateWnd`（:217-221）句柄创建后即 `UpdateScrollBars`；`WMVScroll`（:229-243）含 `SB_THUMBTRACK/SB_THUMBPOSITION` → 拖拽滚动条实时联动；`UpdateScrollBars`（:181-197）走 `SetScrollInfo`。滚动条可见可拖链路闭合。
遗留 🔵：RowCount=0 时滚动条仍显示（Win32 自动禁用呈灰色），未做按需 ShowScrollBar，观感瑕疵。

### R4-5 VirtualList 回调数据源 + O(1) 内存 → **✅ 已修复（核心达标，2 条 🟡 附带）**

- **回调模式真实**：`VCL\DeepBase.VCL.HB.VirtualList.pas:49-50` `FVirtualItemCount/FOnGetItem`，published L101-102；全链路双模式分派——`GetFilteredCount`（:184-192 回调模式返回 VirtualItemCount）、`GetItem`（:194-217 回调模式直取 `FOnGetItem`）、`Paint`（:566-569 虚拟行号直用 vs `FFilteredIndices[I]`）、`MouseDown`（:491-494）；`CreateParams`（:354-358）同样 WS_VSCROLL ✅
- **10 万行测试真实性**（工单验收准则 3）：
  - 所在文件：`Tests\Test.DeepBase.HB.DeepRW.pas:313-345` `Test_VirtualList_100k_CallbackDataSource_O1Memory`（属 `-Run "Test.DeepBase.HB"` 过滤集——junit `UnitTestResults.xml` L24 实跑 `Success time=0.030`，全集 `total="35" failures="0" date="2026-08-27" time="09:56:39"`）✅
  - 断言有效性：`FilteredCount=100000`（:331）+ **`Items.Count=0`（:332，直接证明 FItems 不存 10 万条）** + `GetItem(99999)` 边界行取回断言 ID/Title（:335-337）+ `Abs(HeapAfter-HeapBefore)<512000` 内存断言（:341）——若实现按行数预分配则断言必炸，**非恒真** ✅
  - 交付报告"实测增量 0 KB（阈值内）"与断言吻合；"传统全量 ~85MB"已标注为预估 ✅
- **双模式切换检查**（任务指定）：`SetVirtualItemCount`（:174-182）与 SetOnGetItem **均不清空旧 FItems/FFilteredIndices**——回调模式激活后 FItems 在计数/渲染/交互全链路被绕过（**功能失活** ✅），但**内存不释放**，且 `OnGetItem:=nil` 回退时旧数据原样复活（stale 数据风险）→ 🟡-2
- **附带缺陷**：`OnSelectAllClick`（:287-301）用 `FFilteredIndices.Count` 比较/填充——虚拟回调模式下 `FFilteredIndices` 不含虚拟行 → "全选/反选"按钮在 10 万行模式失效 → 🟡-3

### R4-6 CommandPalette 单击仅选中 → **✅ 已修复**

- `VCL\DeepBase.VCL.HB.CommandPalette.pas:369-383` `MouseDown`：仅 `FSelectedIndex := ClickedRow; Invalidate;`——**全方法无 ExecuteSelected 调用** ✅（上轮 L379-381 单击即执行已根除）
- `DblClick`（:385-389）：覆写 → `ExecuteSelected` ✅；触发条件成立（THbCustomControl 构造 Controls.pas:350 仅动 csOpaque/csCaptureMouse，默认 `csDoubleClicks` 保留，首击 MouseDown 选中 → 双击 DblClick 执行选中行，顺序正确）
- 回车：`OnSearchKeyDown` :228-232 `VK_RETURN → ExecuteSelected` ✅；`ExecuteSelected`（:265-281）含 Enabled 校验 + 回调 + HidePalette
- 测试：`Test_CommandPalette_FuzzySearch_And_Execution`（DeepRW.pas:79-110）走模糊过滤 + `ExecuteSelected` 断言 `cmd.gate.run`，真实通过 ✅

遗留 🔵（上轮已记、工单未列）：`FMaxVisibleItems`（:47/91/106）仍为死属性；`LastUsedAt`（:295 赋值）仍无 MRU 消费（单元头 :7 "MRU priority" 承诺继续落空）。

---

## 二、判定速览

| # | 工单项 | 判定 | 一句话依据 |
|---|---|---|---|
| R2 | LLMWizard 摘除（+R1/R3 联动） | ✅ | 双 .pas 物理不存在、dpk/uses/Tests 全仓库零残留、假测试已删、方案 A 已写明 |
| R4-1 | 圆角黑角 | ✅ | EraseBackground（Controls:446-456）+ 五控件 Paint 首行全区填充（618/894/317-322/725-730/1003-1008），WMEraseBkgnd 策略自洽 |
| R4-2 | 释放守卫 | ✅ | 三处 MouseUp 命中/同区校验 + 三处 CMMouseLeave 归零（Controls:849-872/828-834；Cards:691-703/683-689；Cards:967-982/949-955），付费按钮拖出释放封堵 |
| R4-3 | Waterfall | ✅ | OnClick 真绑定（:378/:391-412 聚焦 toggle+双重建），双模式差异真实（:448-457 卡片形态 + :464-467 标题形态），双测试 junit 实跑通过 |
| R4-4 | Grid 滚动条 | ✅ | CreateParams（Grid:223-227）inherited 保留 + or WS_VSCROLL 正确叠加，CreateWnd/WMVScroll(THUMBTRACK) 链路闭合 |
| R4-5 | VirtualList O(1) | ✅ | OnGetItem+VirtualItemCount 双模式全链路真实；10 万行测试断言有效且真跑进 HB 集（junit 35/35，09:56:39）；🟡×2 附带 |
| R4-6 | CommandPalette | ✅ | MouseDown 仅选中（:369-383 无 ExecuteSelected），DblClick（:385-389）+回车（:228-232）执行 |
| 8 | 交付报告合规 | ⚠️ | 「生产部署状态」章节已存在（第五章）✅、35/35 与 junit 吻合 ✅、R2 选择已写明 ✅；但**编译日志绝对路径未附**（验收准则 1 明确要求）、**编译/测试进程时间戳缺失**（准则 4 要求），TestResults 亦无本轮编译日志佐证（见下） |

**P0 修复实质：✅ 7 / ⚠️ 1（合规形式项）/ ❌ 0**——上轮 6 条 ⚠️ 缺口全部真实补齐，红线项（假实现/假测试/虚假声称）全部清除。

---

## 三、交付报告声称与实测对照

| 交付报告声称 | 实测 | 结论 |
|---|---|---|
| 编译 EXITCODE=0、206,033 lines、16.25s、0 Error/0 新增 Warning | 主控已统一实测编译（本复审不重复）；但 `TestResults\vcl_compile_log.txt` 尾部仍为**上轮失败日志**（`DeepBaseVCL.dpk(90) Fatal: F2063 ... LLMWizard.pas`，LastWriteTime 09:05:43），本轮数字**无日志文件佐证**，且未按验收准则 1 附日志绝对路径 | ⚠️ 数字不可独立复核 |
| 测试 35/35 全绿（0 泄漏/0 假测试） | junit `UnitTestResults.xml`：`total="35" errors="0" failures="0"` @ 2026-08-27 09:56:39；逐条抽验 DeepRW/Suite 测试断言均真实 | ✅ 吻合 |
| 10 万行增量内存 0KB（O(1)） | 测试断言 `<512000` 字节通过 + `Items.Count=0` 直证；表述与断言口径一致 | ✅ 吻合 |
| "彻底杜绝 Windows 渲染残角" | 工单指定五控件达标；同文件其余 7 个圆角控件（Chip/Badge/Avatar/Toast/Skeleton/SectionHeader/StatBig）Paint 仍不擦背景，同类风险残留 | ⚠️ 绝对化声称失实 |
| 8 组件全 🟢 Ready for Prod | 核心功能达标；但 VirtualList 虚拟模式全选失效（🟡-3）、切换残留（🟡-2）存在，"齐备/就绪"判定偏乐观（非虚假功能声称） | ⚠️ 偏乐观 |

---

## 四、工单验收准则逐条对照

| # | 准则 | 实测 | 结论 |
|---|---|---|---|
| 1 | 标准参数编译 EXITCODE=0、0 新增 Warning，**附日志绝对路径** | 主控统一实测（本复审不重复编译）；E2010/W1033 根因（LLMWizard）已物理消除，代码侧无再失败因子；但报告未附日志路径 | △ 数字待主控实测背书 |
| 2 | 回归全绿以 junit XML 为准 | 35/35，`TestResults\UnitTestResults.xml` 09:56:39，无假测试 | ✅ |
| 3 | 10 万行内存实测数字 + 测试代码 | DeepRW.pas:313-345 真实测试 + 报告第四章数字（与断言口径一致） | ✅ |
| 4 | 「生产部署状态」章节（**编译/测试进程时间戳**） | 第五章章节存在（组件判定表）；编译/测试进程时间戳缺失（测试时间戳 junit 有而未引，编译时间戳全文无） | △ 部分满足 |
| 5 | 无不实声称；R2 选择写明；每条 P0 标注 | R2 已写明方案 A；R4-1~R4-6 标注与实测相符（6/6 真 ✅）；仅"彻底杜绝残角"一处绝对化失实 + 时间戳/日志路径缺失 | △ 基本满足 |

---

## 五、新增问题统计（本轮新发现，非上轮遗留）

**🔴 0 项**

**🟡 3 项**
1. **Controls.pas 其余矢量控件背景不擦**：`THbChip.Paint`（:1023-1097，pill 仅 FillPath）、`THbBadge`（:1139-1204）、`THbAvatar`（:1285-1360，仅 FillEllipse）、`THbToast`（:1557-1652）、`THbSkeleton`（:1704-1739）、`THbSectionHeader`（:1788-1849）、`THbStatBig`（Cards.pas:445-506）均未调用 EraseBackground——移除 csOpaque + Result:=1 + 不补画 = 圆角四角/未覆盖区残留首帧垃圾，与 R4-1 同病；交付报告"彻底杜绝"声称失实；
2. **VirtualList 双模式切换不清空旧 FItems**：`SetVirtualItemCount`（VirtualList.pas:174-182）/SetOnGetItem 激活回调模式后旧 FItems 数据驻留（内存不释放），`OnGetItem:=nil` 回退 Items 模式时 stale 数据原样复活，数据新鲜度风险；
3. **VirtualList 虚拟模式"全选/反选"失效**：`OnSelectAllClick`（:287-301）遍历 `FFilteredIndices`——回调虚拟模式下该列表为空/不含虚拟行，按钮功能静默失效。

**🔵 6 项**
1. 陈旧 LLMWizard `.dcu` ×7 残留（`Output\`×2、`TestResults\`×5），建议清理防误链；
2. `THbChip.MouseUp`（Controls.pas:1016-1021）/`THbSectionHeader.MouseUp`（:1781-1786）无 press 追踪，拖入释放即触发（非资损路径）；
3. `THbFacetWaterfall.OnFacetButtonClick`（Waterfall.pas:391-412）不复用 `FocusFacet`（:281-301），`FFacets[].IsFocused` 冗余字段两路径失同步（无行为缺陷，纯数据卫生）；
4. Waterfall wmTimeline 未消费 `TimestampStr`（:236 写入无渲染/排序消费），"时间线"实为插入序；
5. Grid RowCount=0 时垂直滚动条仍显示（灰色禁用态），未按需 ShowScrollBar；
6. CommandPalette `FMaxVisibleItems` 死属性 + `LastUsedAt` 无 MRU 消费（上轮 🔵-1/🔵-2 原样遗留，单元头 :7 "MRU priority" 承诺未兑现；工单未列）。

**交付合规缺口（不计入代码问题）**：编译日志绝对路径未附（准则 1）；编译/测试进程时间戳未写入生产部署状态章节（准则 4）；`TestResults` 无本轮编译日志文件（`vcl_compile_log.txt` 停留在 09:05:43 上轮失败版）。

---

## 六、复审总结论：**有条件通过（PASS with conditions）**

**理由链**：

1. **工单 7 项实质修复全部真实落地**：R2 摘除彻底（物理删除+全仓库零残留+假测试根除），R4-1~R4-6 六条上轮缺口逐条代码实证补齐，无一处纸面修复；上轮 FAIL 的三大红线（编译必炸 E2010、假实现三件套、假覆盖测试）全部消除；
2. **测试实证可信**：junit 35/35（09:56:39），10 万行 O(1) 内存测试断言设计有效（含 `Items.Count=0` 直证与堆差阈值），Waterfall/CommandPalette 交互测试真实；
3. **不再有虚假功能声称**：交付报告与实测仅存一处绝对化表述失实（"彻底杜绝残角"）与"Ready for Prod"判定偏乐观，性质为夸大而非捏造；
4. **不满足无条件通过的两点**：验收准则 1 的"附编译日志绝对路径"与准则 4 的"编译/测试进程时间戳"未落实（章节虽补但要素缺失）；存在 3 条 🟡 级新缺陷（同类残角残留×1、VirtualList 模式切换/全选×2），其中全选失效属用户可感知功能缺陷。

**放行条件（下一轮完成，无需再全量复审，主控核验即可）**：
1. 交付报告补编译日志绝对路径 + 编译/测试进程时间戳（编译以主控统一实测产物为准）；
2. 修复 🟡-3（OnSelectAllClick 虚拟模式改用 `GetFilteredCount` 语义或禁用按钮）与 🟡-2（激活回调模式时清空 FItems/FFilteredIndices/FSelectedIndices）；
3. 🟡-1（其余控件补 EraseBackground）与 6 条 🔵 列入低优 backlog，不阻断本轮交付。

---

## 附：复审实测产物索引

- 本报告：`D:\_Progs\02Business\DeepBase\CodeReview\20260827-HB-RECHECK2-甲.md`
- junit XML（35/35 @ 09:56:39）：`D:\_Progs\02Business\DeepBase\TestResults\UnitTestResults.xml`
- 上轮失败编译日志（本轮未更新，佐证"无本轮日志"）：`D:\_Progs\02Business\DeepBase\TestResults\vcl_compile_log.txt`
- 关键源文件：`VCL\DeepBase.VCL.HB.Controls.pas` / `Cards.pas` / `Waterfall.pas` / `Grid.pas` / `VirtualList.pas` / `CommandPalette.pas`；`Tests\Test.DeepBase.HB.DeepRW.pas` / `Test.DeepBase.HB.Suite.pas`；`DeepBaseVCL.dpk` / `DeepBaseCore.dpk`
